curriculum:
  version: "1.0"

  # L0_0: Minimal temporal credit assignment
  # 8×8 grid, 2 bars (energy, health), 2 affordances (SLEEP, DOCTOR)
  # Focus: Learn that SLEEP increases energy and DOCTOR increases health (basic credit assignment)

  active_vision: global # Full grid observable
  vision_range: 0.5 # 4x4 (1/2) window on 8×8 grid
  active_temporal: false # No time-based dynamics
  day_length: null # Temporal mechanics disabled for this level
