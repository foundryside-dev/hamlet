---
actions:
  version: "1.0"

  substrate_actions:
    inherit: true

  custom_actions:
    - name: WAIT
      description: "Do nothing (pass turn)"
      enabled_by_default: true
    - name: REST
      description: "Passive energy recovery (test action)"
      enabled_by_default: false

  labels:
    preset: gaming
