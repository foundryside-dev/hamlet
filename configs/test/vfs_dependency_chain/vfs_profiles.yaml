# VFS Dependency Chain Test Configuration
# Tests: a → b → c dependency chain with mark-and-sweep
# Note: Using global_profile for runtime testing (agent_profile evaluation not yet implemented)

version: "1.0"

global_profile:
  variables:
    - name: a
      type: float
      initial_value: 10.0
      description: "Base value"

    - name: b
      type: float
      expression: "a + 5"
      description: "Depends on a: a + 5 = 15"

    - name: c
      type: float
      expression: "b * 2"
      description: "Depends on b: b * 2 = 30"
