# ⚠️ RECOVERED FROM ARCHIVE 2026-08-26 — read these two corrections first.
#
# 1. The comment below says this nests in "substrate.yaml". That file does not exist.
#    The real container is the pack-root **stratum.yaml**, and the substrate block is
#    nested one level deeper, under `stratum: > substrate:`. Compare
#    configs/aspatial_test/stratum.yaml for the shipped shape.
# 2. Any "Observation dim: ... = N dims" comment here is unreliable. Observation width is
#    two different quantities (allocated vs active) and moves whenever the observed surface
#    changes. Ask the compiled artifact -- CompiledUniverse.observation_spec.total_dims --
#    never a literal in a doc. See CLAUDE.md "State Representation".
#
# This file is a FRAGMENT illustrating one substrate's options, not a runnable pack.

# Example: Aspatial Substrate (No Positioning)
#
# This substrate demonstrates pure state machine mode where agents have
# NO position and NO navigation. Affordances are available without spatial location.
#
# Reveals core insight: Meters are the TRUE universe - positioning is just an overlay.
#
# Behavioral differences from grid substrate:
#   - No movement actions (UP/DOWN/LEFT/RIGHT disabled)
#   - No navigation planning (all affordances equally accessible)
#   - Pure resource management (energy, health, satiation cycles)
#   - Observation dim: 0 grid + 8 meters + 15 affordances + 4 temporal = 27 dims
#
# v2.1 Format: This file shows the substrate portion only.
# In a complete config pack, this would be nested in substrate.yaml under the top-level "substrate:" key.

type: "aspatial"

aspatial: {}
