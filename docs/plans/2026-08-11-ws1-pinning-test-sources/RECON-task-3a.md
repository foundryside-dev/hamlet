============================================================================
PROBE: mechanism-b

Mechanism B is exactly as the spec describes structurally — passive depletion already receives `MeterConfig` objects carrying `.bounds` (no threading needed), the cascade compiler genuinely has no meters and needs a new required parameter, and `_coerce_meter_bounds` is genuinely extractable from `_coerce_terminal_condition_meter`. Item 5 is answered definitively by execution: the serialized payload contains ZERO `clamp` keys, `transition_schedule` persists only `{phase_graph, program_counts, social_residue_rules}`, and both `CompiledUniverse.from_dict` paths rebuild the programs from the serialized `bars` (which do carry `bounds`) — so NO second `COMPILED_SCHEMA_VERSION` bump is required. Four spec claims are wrong: the hash does not move for every pack (10 of 25 `bars.yaml` are all-unit-bounds), the test-churn list misses two `test_vtc_jit_kernels.py` sites and understates `test_vtc_passive_depletions.py`, B2 is numerically inert on every shipped pack, and `money.bounds.max` is not uniformly 999999.0.

--- SURPRISES ---
* CONTRADICTS spec consequence (a): 'transition_graph_hash and therefore vfs_hash move for every pack' is FALSE. 10 of 25 bars.yaml declare all meters at [0.0, 1.0], so their canonical rule payload is byte-identical and their hashes do NOT move: configs/L5_multi_agent/levels/L5_multi_agent, configs/reference/model_pack/levels/L0_demo, configs/simple/levels/L0_simple, configs/test/effects_smoke/levels/L0_effects, configs/test/items_smoke (top-level bars.yaml), configs/test/vfs_bar_access/levels/L0_bars, configs/test/vfs_circular_dependency/levels/L0_circular, configs/test/vfs_dependency_chain/levels/L0_deps, configs/test/vfs_type_mismatch/levels/L0_type_mismatch, configs/test/vfs_undefined_var/levels/L0_undefined. Also the count is 25 bars.yaml files, not 24. Reword the commit message to 'moves for the 15 packs declaring non-unit bounds'.

* CONTRADICTS spec change 10 (test churn), two omissions that will land the batch red: tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:29 passes `[{"name": "energy", "depletion": {"passive": 0.1}}]` to compile_vtc_passive_depletions — no "bounds" key, so B1's required-key rule raises KeyError; and :38 passes a bare cascade mapping to compile_vtc_threshold_cascades — needs the new `meters` argument. Neither line appears in the spec's list. The list also understates test_vtc_passive_depletions.py: the spec names only the `clamp == (0.0, 1.0)` assert at :32, but the mappings BUILT at :13-19 and :42-46 (three meter dicts total) all lack "bounds" and will raise before the assert is reached.

* B2 is numerically inert on every shipped config pack, and the spec never says so. No cascade anywhere targets `money` — the distinct cascade targets across all 25 packs are exactly {energy, health, mood, social}, all declared [0.0, 1.0]. Measured consequence: the B1+B2 transition_graph_hash equals the B1-only hash on L1 (e2047c67013a05c4…), and `changed rules (B2)` is empty. The ONLY thing that exercises B2 is synthetic pinning test 3 (energy.bounds.max: 2.0). That makes test 3 the sole regression guard for change 6 — stronger than the spec's 'do not drop it as a duplicate'.

* CONTRADICTS spec consequence (b): `money.bounds.max` is NOT uniformly 999999.0 across packs. Measured: 999999.0 (all 5 default_curriculum levels + configs/test/model_config, model_config_4meter, model_config_12meter), 1000000.0 (configs/aspatial_test/levels/L0, configs/test/action_space/{grid2d,aspatial,continuous1d}, configs/test/action_masking, configs/test/gridnd_4d_pack), and 100.0 (configs/test/items_smoke). Furthermore configs/simple and configs/L5_multi_agent DO declare a `money` meter but at [0.0, 1.0]. The spec's meter-name claim ('money is the only meter declaring bounds != [0,1]') holds; the value claim does not. Any assertion pinning a specific ceiling must key off the pack it loaded.

* Not in the spec, and it will silently corrupt a red/green measurement: cache validity (compiler.py:132-140) is keyed on config YAML hash + provenance (which uses `git rev-parse HEAD`, NOT dirty state) + mtime, while transition_graph_hash is restored verbatim from the payload and never recomputed. So with uncommitted vtc.py edits, an existing configs/**/.compiled/*.msgpack is judged VALID, loads, rebuilds the programs from bars with the NEW clamps, and reports the OLD hash. Six such artifacts exist on disk right now. The spec's 'find configs -name *.msgpack -delete first' is therefore mandatory, not tidiness — and it belongs in the red-measurement step too, not just Verify.

* Change 6's 'raise when the target meter is absent' duplicates validation that already exists twice (universe/validation/semantics.py:170-200 and :239-250; universe/compilers/optimization.py:45-57) and that zero shipped packs violate. Worth writing for the no-hidden-defaults leg, but the implementer should know it is unreachable from any config pack and needs a synthetic-list test, not a config-pack test.

* docs/arch-analysis-2026-05-16-1200/temp/sg2-vfs.md:58-59 names runtime consumers `vectorized_env.py:1245 (_apply_vtc_passive_depletion)` and `:1260 (_apply_vtc_threshold_cascades)`. Those methods no longer exist — grep for either name returns nothing. The real execution path is VTCTransitionRunner._run_passive_depletions / _run_threshold_cascades in vfs/transition_schedule.py:133-162. Do not use that doc for navigation.

--- FACTS ---
[measured] NO second COMPILED_SCHEMA_VERSION bump is required: the compiled artifact never serializes a per-rule clamp, and both from_dict paths rebuild the passive-depletion and threshold-cascade programs from the serialized bars (which do carry bounds).
  EVIDENCE: Ran /home/john/.claude/jobs/4c306e19/tmp/recon/probe_serialize.py on a scratch copy of configs/default_curriculum (use_cache=False). Output: `clamp keys in serialized payload: 0`; `transition_schedule payload keys: ['phase_graph', 'program_counts', 'social_residue_rules']`; `serialized money bounds: {'min': 0.0, 'max': 999999.0, 'lethal_min': False, 'lethal_max': False}`. Source: src/townlet/vfs/transition_schedule.py:235-254 `serialize_vtc_transition_schedule` (comment in situ: "The other programs are rebuilt from their existing compiled-universe inputs; these counts are diagnostic metadata"); src/townlet/universe/compiled.py:426 `level_bars = BarsV2Config.model_validate(meta["bars"])` then :437-449 `build_vtc_transition_schedule(level=_SerializedLevel(bars=level_bars, ...))`, and the top-level twin at :513-522.
  IMPLICATION: Leave COMPILED_SCHEMA_VERSION at "1.14" (src/townlet/universe/compiled.py:53). D1 holds — task 2 owns the only bump in the batch. No payload-shape change, no new serializer field.

[measured] transition_graph_hash is restored VERBATIM from the payload and never recomputed on load, while cache validity ignores compiler source changes — so a stale .msgpack loads with NEW clamps and the OLD hash.
  EVIDENCE: src/townlet/universe/compiled.py:469 and :535 both use `_required_field(..., "transition_graph_hash")`; no `compute_transition_graph_hash` call exists anywhere in compiled.py or vectorized_env.py (grep). Cache gate: src/townlet/universe/compiler.py:132-140 accepts the cache when `config_hash == config_hash and provenance_id == provenance_id and config_mtime >= config_mtime`; `_build_cache_fingerprint` (:579-592) derives provenance from config_hash + COMPILER_VERSION + `self._get_git_sha()` + python/torch/pydantic versions, and `_get_git_sha` (:727-739) is `git rev-parse HEAD` — NOT dirty-state aware. Six artifacts exist on disk right now (configs/default_curriculum/.compiled/*.msgpack ×5, configs/aspatial_test/.compiled/universe-L0.msgpack), none tracked by git.
  IMPLICATION: `find configs -name '*.msgpack' -delete` before every measurement is load-bearing, not hygiene: with uncommitted vtc.py edits the git SHA is unchanged, the cache is considered valid, and the loaded universe silently pairs wired clamps with the pre-change hash. Any red/green pinning measurement taken without deleting them is invalid.

[measured] Widening the money clamp moves transition_graph_hash to exactly the digest the spec records, and B1 alone produces it — B2 contributes nothing on L1.
  EVIDENCE: Ran /home/john/.claude/jobs/4c306e19/tmp/recon/probe_hash.py (dataclasses.replace on the compiled rules, then compute_transition_graph_hash). baseline `b6adac05f9d224b2610ad56cd4bb185f4efcae9c1766dec510fa11be9d6baea6` (== u.transition_graph_hash); B1 only `e2047c67013a05c460b8b41514fd3ed18183601e4fc935ef41673b9a74ff8da8`; B1+B2 identical to B1. `changed rules (B1): [('passive:money', (0.0, 999999.0))]`; `changed rules (B2): []`. clamp participates via src/townlet/vfs/schema_hashes.py:188 `"clamp": list(write.clamp) if write.clamp is not None else None`.
  IMPLICATION: The spec's recorded L1 hash movement is correct and reproducible; record it unchanged in the commit message. Do not attribute any part of it to B2.

[read-only] B1 needs no call-site change: the passive-depletion compiler already receives MeterConfig objects that carry .bounds, at both production entry points.
  EVIDENCE: src/townlet/vfs/transition_schedule.py:215 `passive_depletions = compile_vtc_passive_depletions_with_phase_graph(level.bars.meters, phase_graph)` — the same `level.bars.meters` handed one line earlier (:214) to `compile_vtc_terminal_conditions_with_phase_graph`, which already reads `.bounds` via `_coerce_terminal_condition_meter`. src/townlet/config/bars_v2_config.py:92 `bounds: MeterBoundsConfig = Field(...)` is a required field on MeterConfig. The from_dict rebuild passes the same type (compiled.py:426/437-449).
  IMPLICATION: For B1, edit only vtc.py: add `bounds` to the `VTCPassiveDepletionSource` Protocol (src/townlet/vfs/vtc.py:51-58, which today declares only `name` and `depletion`) and extend `_coerce_passive_depletion`. transition_schedule.py:215 stays byte-identical, as the spec says.

[read-only] _coerce_passive_depletion currently drops everything but name and passive rate; its Mapping branch is the only place a required `bounds` key can be enforced.
  EVIDENCE: src/townlet/vfs/vtc.py:2400-2412 — Mapping branch: `name = str(meter["name"])`, `depletion = meter["depletion"]`, `passive = float(depletion["passive"] if isinstance(depletion, Mapping) else getattr(depletion, "passive"))`; object branch: `name = meter.name`, `passive = float(meter.depletion.passive)`; returns `{"name": name, "passive": passive}`. The clamp literal it feeds is at :2384 `clamp=(0.0, 1.0)` inside compile_vtc_passive_depletions_with_phase_graph (:2359-2397).
  IMPLICATION: Add a third key to the returned dict (e.g. "bounds": (min, max)) and emit `clamp=meter["bounds"]` at :2384. Mapping fixtures without a "bounds" key will raise KeyError — see the test-churn surprise.

[read-only] The threshold-cascade compiler genuinely has no access to meters today; the spec's B2 diagnosis is correct.
  EVIDENCE: src/townlet/vfs/vtc.py:2277-2279 `def compile_vtc_threshold_cascades(cascades: Sequence[VTCThresholdCascadeSource | Mapping[str, Any]]) -> VTCThresholdCascadeProgram` and :2282-2285 the `_with_phase_graph` twin take `cascades` (+ phase_graph) only. `VTCThresholdCascadeSource` Protocol (:35-48) exposes source/target/threshold/strength — no bounds. `_coerce_threshold_cascade` (:2331-2351) returns `{rule_id, source, target, threshold, strength}`. Hardcoded clamp at :2314. Target identity is confirmed correct for bounds lookup: `VTCThresholdCascadeProgram.apply` clamps `phase_values[rule.variable_id]`, i.e. `cascade["target"]` (:966-979).
  IMPLICATION: Add the required `meters` parameter as specified and key bounds off `cascade["target"]`. Note that both the bare and `_with_phase_graph` forms are re-exported public API (src/townlet/vfs/__init__.py:95-96, 179-180), so this is a public break — fine under the zero-backcompat rule.

[measured] Complete call-site inventory for both compilers (src/, tests/, scripts/, docs/).
  EVIDENCE: grep across the tree. THRESHOLD CASCADES — production: src/townlet/vfs/transition_schedule.py:217 (only one); re-export: src/townlet/vfs/__init__.py:95-96,179-180; tests: tests/test_townlet/unit/vfs/test_vtc_threshold_cascades.py:16 and :35, tests/test_townlet/unit/vfs/test_vtc_passive_dynamics_equivalence.py:108, tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:38, tests/test_townlet/unit/vfs/test_schema_hashes.py:775. PASSIVE DEPLETIONS — production: transition_schedule.py:215 (only one); re-export: __init__.py:87-88,171-172; tests: test_vtc_passive_depletions.py:13 and :42, test_vtc_jit_kernels.py:29, test_vtc_passive_dynamics_equivalence.py:102, test_schema_hashes.py:437 and :460. No callers in scripts/. docs/ mentions are prose only (docs/arch-analysis-2026-05-16-1200/temp/sg2-vfs.md:58-59, already stale — it cites `vectorized_env.py:1260 (_apply_vtc_threshold_cascades)`, a metho
  IMPLICATION: Nine test call sites need touching, not the six the spec's churn list names. Every production edit is one line in one file.

[read-only] transition_schedule.py:215 and :217 read exactly as the spec predicts; only :217 needs a call-site change.
  EVIDENCE: src/townlet/vfs/transition_schedule.py, inside `build_vtc_transition_schedule` — line 215: `passive_depletions = compile_vtc_passive_depletions_with_phase_graph(level.bars.meters, phase_graph)`; line 217: `threshold_cascades = compile_vtc_threshold_cascades_with_phase_graph(level.bars.cascades, phase_graph)`.
  IMPLICATION: Change 7 is a one-line edit at :217 adding `level.bars.meters`. The spec's line numbers happen not to have drifted for this file.

[read-only] A shared _coerce_meter_bounds(bounds) -> (min, max) is genuinely extractable; the two shapes do not differ for min/max, they differ only in that the terminal coercer additionally needs the lethal flags.
  EVIDENCE: src/townlet/vfs/vtc.py:2687-2712 `_coerce_terminal_condition_meter`: it first resolves `bounds` (Mapping → `meter["bounds"]`, object → `meter.bounds`), then branches again on `isinstance(bounds, Mapping)` reading min/max/lethal_min/lethal_max. The min/max half is identical to what B1 needs; only `lethal_min`/`lethal_max` are extra. `VTCTerminalConditionSource` (:103-110) already declares `bounds` as the Protocol member B1 must copy onto `VTCPassiveDepletionSource`.
  IMPLICATION: Extract a helper taking the already-resolved `bounds` object (not the meter), returning `(min, max)`; `_coerce_terminal_condition_meter` calls it and keeps its own lethal-flag reads. Do not try to share the outer meter->bounds resolution alone — that is the part that is only two lines.

[read-only] clamp cannot be set to None to 'disable' bounds: both programs require it at execution time.
  EVIDENCE: src/townlet/vfs/vtc.py:2983-2986 `_required_clamp` raises `ValueError(f"Generated VTC rule '{telemetry_label}' requires explicit clamp bounds for scripted execution")` when clamp is None; it is called at :966 (threshold cascade), :1045 (passive depletion) and :1116, feeding clamp_low/clamp_high as scalars into the TorchScript kernels `vtc_kernels.apply_threshold_cascade` / `apply_passive_depletion`.
  IMPLICATION: Both compilers must always emit a concrete (min, max) tuple from authored bounds. There is no 'unbounded' escape hatch, and a float ceiling of 999999.0 flows into the kernels unchanged.

[measured] The runtime executes these programs only through VTCTransitionRunner, not through any env-level method — so B1/B2 bind, and OptimizationData.cascade_data is a dead second compilation.
  EVIDENCE: src/townlet/vfs/transition_schedule.py:105-106 calls `_run_passive_depletions` then `_run_threshold_cascades` (:133-162), each rebuilding a phase-filtered program and calling `.apply(...)`. src/townlet/environment/vectorized_env.py:288/290 assign `vtc_passive_depletion_program` / `vtc_threshold_cascade_program`, and grep finds ZERO other references to those attributes anywhere in src/. `cascade_data` (src/townlet/universe/compilers/optimization.py:96) has no runtime reader.
  IMPLICATION: No second runtime path to wire. Also: `docs/arch-analysis.../sg2-vfs.md` claims consumers `vectorized_env.py:1245/1260 (_apply_vtc_passive_depletion / _apply_vtc_threshold_cascades)` — those symbols do not exist; do not navigate by that doc.

[measured] The legacy-equivalence test survives B1+B2 numerically — it needs the `meters` argument at line 108 and nothing else.
  EVIDENCE: Ran /home/john/.claude/jobs/4c306e19/tmp/recon/probe_equiv.py: for all 5 levels × depletion_multiplier ∈ {1.0, 1.75}, wired-clamp output vs `_legacy_decay_and_cascades` (which hardcodes min=0.0, max=1.0 at tests/.../test_vtc_passive_dynamics_equivalence.py:42-46 and :61) gives `mismatched meters = []` in every case. Why: SAMPLE_BAR_VALUES max is 1.0 (:22), passive depletion only decreases, cascade deltas are only negative, and money's `depletion.passive` is 0.0 in all five default_curriculum levels.
  IMPLICATION: Do not rewrite the legacy reference formula. Only the signature at :108 changes. If a future fixture ever raises a sample value above a meter's ceiling or gives money a negative passive rate, this assertion becomes the canary.

[measured] Change 6's 'raise when the target meter is absent' is defensive only — cascade source/target are already validated against declared meters at compile time, in two places, and zero shipped packs violate it.
  EVIDENCE: src/townlet/universe/validation/semantics.py:170-200 validates environment.yaml cascade_graph edges against known meters and rejects cycles; :239-250 requires each level's `bars.cascades` edge set to equal the environment cascade_graph exactly. src/townlet/universe/compilers/optimization.py:45-57 raises `ValueError("Invalid cascade entry in bars.yaml. Unknown source/target meter ...")`. Survey over all 25 configs/**/bars.yaml: `cascades whose source/target is NOT a declared meter in the same pack: 0`.
  IMPLICATION: Write the raise (it is cheap and it is the no-hidden-defaults leg), but expect it to be unreachable from any shipped pack — a pinning test for it must construct a synthetic cascade list, not a config pack.

[measured] The five VTC test files affected by this change are green today.
  EVIDENCE: `uv run pytest tests/test_townlet/unit/vfs/test_vtc_passive_depletions.py test_vtc_threshold_cascades.py test_vtc_jit_kernels.py test_vtc_modulations.py test_vtc_passive_dynamics_equivalence.py -q --no-cov` → `25 passed` in 1.38s.
  IMPLICATION: That is the red/green baseline for the churn edits; any failure after the change is caused by this unit.

[measured] Working tree untouched by this recon.
  EVIDENCE: `git status --porcelain` returns empty at the end of the session; all probes ran from /home/john/.claude/jobs/4c306e19/tmp/recon/ against a scratch copy of configs/default_curriculum with use_cache=False. The pre-existing configs/**/.compiled/*.msgpack artifacts were left in place (untracked; absent from `git ls-files`).
  IMPLICATION: The six on-disk .msgpack artifacts are still there and are the staleness hazard described above — delete them as the first step of implementation.

============================================================================
PROBE: literals

All six RECON B items are measured against a real four-site fix built in a throwaway worktree (now removed; repo tree clean). The spec's post-fix literals are all correct — 0.490000, 1.785840 (±1e-6 holds, actual drift 8.5e-8), 22.500000 — but three of its four "Red today" literals are wrong because the author forgot the passive-depletion tick when predicting unpatched values, and its mechanism-A/mechanism-B discriminator story is structurally wrong: tests 1 and 2 both need A+B1, not B alone. Item 3's unstated value is 1.720000, and item 6 produces an inversion: lethal_max:true kills the agent PRE-fix, not post-fix, so "keep lethal_max: false" protects the red leg, not the green one.

--- SURPRISES ---
* THREE OF FOUR 'Red today' LITERALS IN THE SPEC ARE WRONG. Measured on unpatched HEAD: test 1 red = 0.990000 (spec: 1.000000); test 2 red = 0.990000 / 0.980000 / 0.970000 (spec: 1.000000); test 3 red = 0.995840 (spec: 1.000000). Only test 4/money's red = 1.000000 is right, and only because money's passive depletion is 0.0. The author forgot the passive tick when predicting the red values, even though they correctly accounted for it in the GREEN value of test 1 ('the ceiling minus the 0.01 passive tick'). An implementer who writes the red baseline as ==1.0 will see 0.99, conclude the harness is broken, and debug a non-bug. Test 3's red of 0.995840 has a specific mechanism worth stating: passive drives energy 1.8 -> 1.79, the hardcoded (0.0,1.0) passive clamp crushes it to 1.0, THEN the cascade subtracts 0.00416.

* THE MECHANISM-A / MECHANISM-B DISCRIMINATOR PARTITION IS STRUCTURALLY WRONG. The spec says test 2 is 'the mechanism-B discriminator' and that 'a mechanism-A-only fix passes test 1 and fails this one'. Measured with B1 applied and the A-sites still hardcoded: test 1 = 0.500000 (not 0.490000 -- FAILS), test 2 = 0.990000/0.980000/0.970000 (unchanged from red -- FAILS). Neither test isolates a mechanism; both require A+B1, because apply_instant_interaction's hardcoded A-site clamp crushes 1.75 -> 1.0 before the passive rule ever sees it. The genuinely clean single-site discriminators, measured: A1 alone <- test 4's MOVEMENT arm (B1-only 1.0, full 22.5); B1 alone <- test 4's WAIT arm (already 22.5 under B1-only); B2 alone <- test 3 (B1-only 1.000000, B1+B2 1.785840). Only the B2 leg is as the spec describes. If the suite is meant to prove a four-site fix rather than a partial one, that is the partition to put in the test file's docstring.

* ITEM 6 IS AN INVERSION, NOT A CONFIRMATION. The spec's rationale for 'keep lethal_max: false explicitly' is that 'a lowered ceiling with lethal_max: true would kill the agent at the ceiling and mask the assertion'. Measured, the kill happens PRE-fix (energy 0.990000 >= 0.5, dones=[True]) and NOT post-fix (energy 0.490000 < 0.5, dones=[False], because terminal conditions evaluate after passive depletion). The instruction is right, the reason is backwards: lethal_max:false protects the RED leg from ending the episode on tick 1, not the green one. Left unstated, an implementer who measures post-fix survival will conclude the instruction is bogus and drop it -- at which point the red baseline for test 1 becomes a dead agent instead of 0.990000.

* tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:29 is missing entirely from the spec's change-10 test-churn list; it fails with KeyError: 'bounds'. And test_affordance_engine.py has 17 AffordanceEngine(...) construction sites, not the 20 the spec states -- 20 is the number of failing tests, inflated by shared fixtures.

* Item 5's can_afford assertions are vacuous as written: on unpatched HEAD, seeding money := 22.5 and calling can_afford WITHOUT stepping returns True for all six affordances, identical to post-fix. The assertion only discriminates if it runs after the env.step that used to destroy the money.

* Cache-invalidation probe (worth keeping as a harness assertion): compiling the same pack twice with different energy.bounds.max produced identical clamp tuples on unpatched HEAD -- (0.0,1.0) both times, correctly, because they are hardcoded -- and correctly divergent tuples post-fix (max=1.0 -> passive (0.0,1.0)/cascade (0.0,1.0); max=2.0 -> passive (0.0,2.0)/cascade (0.0,2.0)). Deleting .compiled and *.msgpack plus use_cache=False is sufficient invalidation; no stale-cache artefact was observed.

--- FACTS ---
[measured] Item 1 CONFIRMED post-fix: energy.initial 0.4 / bounds.max 0.5, L0_0, parked on SLEEP, one INTERACT env.step -> 0.490000 exactly (float32 repr 0.49000000953674316, |delta| from 0.49 = 9.5e-9).
  EVIDENCE: Worktree harness: UniverseCompiler().compile(pack, primary_level='L0_0_minimal', use_cache=False); VectorizedHamletEnv(num_agents=1, device=cpu); positions := env.affordances['SLEEP']; env.step(INTERACT id 8). Printed repr=0.49000000953674316 f6=0.490000.
  IMPLICATION: Write the assertion as pytest.approx(0.49, abs=1e-6) or ==0.490000 at six decimals. The spec's mandated value is right; do not substitute PDR-0014's 0.5.

[measured] Item 1 ORDERING claim CONFIRMED, but 0.490000 is NOT the evidence for it. The clean proof is a no-clamp arm: L0_0 stock bounds, energy.initial 0.4, parked on SOCIALIZE (declared cost energy 0.1), one INTERACT -> 0.240000 = 0.4 - 0.05 (interact depletion, ActionExecutor._execute_actions) - 0.10 (declared cost, apply_instant_interaction) - 0.01 (passive). Second proof: in item 4's run satiation goes 0.1 -> 0.092000, i.e. passive ran before the cascade read it.
  EVIDENCE: Harness t1c: repr=0.23999999463558197 f6=0.240000. Harness t3: satiation repr=0.09200000017881393. Phase strings read at src/townlet/vfs/vtc.py: 'apply_passive_depletion', 'apply_threshold_cascades', 'evaluate_terminal_conditions'.
  IMPLICATION: 0.490000 is consistent with either ordering while the clamp binds, so it cannot pin the ordering. If an ordering lock is wanted in the file, use the SOCIALIZE/0.240000 arm or assert satiation==0.092 alongside energy in test 3.

[measured] Item 2 CONFIRMED: MeterConfig.validate_initial_in_bounds rejects initial 1.0 against bounds.max 0.5. Two distinct rendered texts exist and the pinning tests will hit the WRAPPER one, not the bare pydantic one.
  EVIDENCE: Bare model: 'ValidationError: 1 validation error for MeterConfig\n  Value error, initial value (1.0) must be within bounds [0.0, 0.5]. Got: initial=1.0 [type=value_error, ...]'. Via pack compile: 'CompilationError: Stage 1: Load v2.1 Configs failed:\n  - [LEVEL_LOAD_ERROR] <path>/levels/L0_0_minimal - Failed to load level L0_0_minimal: bars.yaml VALIDATION FAILED\n\n1 validation error for BarsV2Config\nmeters.0\n  Value error, initial value (1.0) must be within bounds [0.0, 0.5]. Got: initial=1.0 ...'. Source: src/townlet/config/bars_v2_config.py:95-103.
  IMPLICATION: Lower `initial` alongside every lowered `bounds.max` as the spec says. If any test asserts the failure with pytest.raises(match=...), match against CompilationError / 'must be within bounds', not the bare MeterConfig text.

[measured] Item 3 ANSWERED (spec gave no value): energy.bounds.max 2.0, initial 1.0, SLEEP INTERACT then two WAIT steps -> 1.740000, 1.730000, 1.720000. Final = 1.720000 (repr 1.7200000286102295).
  EVIDENCE: Harness t2 on the patched worktree. Derivation matches: 1.0 - 0.05 (interact depletion) + 0.8 (SLEEP on_start) = 1.75, clamp(0,2.0) no-op, - 0.01 passive = 1.74; then -0.01, -0.01.
  IMPLICATION: Pin all three values, not just the last. The 1.740000 mid-value is what proves the ceiling above 1.0 admitted the SLEEP effect at all; the two decaying WAITs prove the passive rule's clamp is (0.0, 2.0).

[measured] Item 4 CONFIRMED on all three legs, and the spec's +/-1e-6 tolerance is achievable in float32: unpatched 0.995840, B1-only 1.000000, B1+B2 1.785840 (repr 1.7858399152755737; |delta| from 1.785840 = 8.5e-8). The declared satiation->energy cascade demonstrably fires.
  EVIDENCE: Three separate worktree runs (unpatched HEAD; B1 applied; B1+B2+A applied). Cascade firing proven by satiation 0.1 -> 0.092000 and energy delta -0.01416 = 0.01 passive + 0.00416 cascade, where 0.00416 = 0.006 * ((0.3 - 0.092) / 0.3).
  IMPLICATION: Keep +/-1e-6; do not loosen it. B1-only=1.000000 is the whole justification for B2 being a separate change: with B1 the passive clamp is (0,2.0) so energy survives to 1.79, then the cascade's hardcoded (0.0, 1.0) crushes it to 1.0.

[measured] Item 5 CONFIRMED on both arms, and the spec's claim that the movement arm is the only thing pinning A1 is exactly right. Stock L1, money := 22.5: WAIT arm post-fix 22.500000 (red 1.000000), movement arm post-fix 22.500000 (red 1.000000). Under B1-only the WAIT arm is ALREADY 22.5 while the movement arm is still 1.0.
  EVIDENCE: Harness t5 on unpatched / B1-only / full. L1 action id 0 = 'UP', verified moved=True by comparing env.positions before/after. L1 bars.yaml declares money.bounds.max 999999.0, money.depletion.move 0.0.
  IMPLICATION: Both arms are mandatory. Use action id 0 ('UP') or any of ids 0-7; ids 8 (INTERACT) and 9 (WAIT) do not set movement_mask, so picking one of those makes the arm vacuous. The movement debit for money is 0.0 -- the arm pins the A1 CLAMP, not a debit.

[measured] TRAP in item 5's can_afford leg: calling can_afford on a freshly-seeded env WITHOUT stepping returns True for all six affordances even on unpatched HEAD. The leg is vacuous unless it runs after the env.step.
  EVIDENCE: Harness t5 baseline run on unpatched HEAD printed can_afford(EAT/SHOWER/LAUNDRY/COOK/ENTERTAINMENT/DOCTOR) -> [True] for all, identical to the patched run. The discriminator is that money is destroyed by the step, not that can_afford is wrong.
  IMPLICATION: In test_shipped_economy_is_solvent, assert can_afford AFTER the WAIT/movement step, using post-step env.meters. Asserting on the pre-step tensor proves nothing and passes red.

[measured] Item 6 CONFIRMED that bounds.max feeds a terminal condition -- but the runtime effect is the INVERSE of the spec's stated rationale. compile_vtc_terminal_conditions_with_phase_graph emits `bar.<name> >= <bounds.max>` when lethal_max is true. With energy bounds.max 0.5 / initial 0.4 / lethal_max TRUE, SLEEP INTERACT: PRE-fix dones=[True] (energy 0.990000 >= 0.5, agent dies); POST-fix dones=[False] (energy 0.490000 < 0.5, agent survives because terminal evaluation runs after passive depletion drops it below threshold).
  EVIDENCE: src/townlet/vfs/vtc.py:2652-2675 (_coerce_terminal_condition_meter reads bounds.max/lethal_max). Runtime rule list printed: [('terminal:energy:min','bar.energy <= 0.0'), ('terminal:energy:max','bar.energy >= 0.5'), ('terminal:health:min','bar.health <= 0.0')]. Harness t6 both arms, both trees.
  IMPLICATION: Keep `lethal_max: false` explicitly, but for the opposite reason the spec gives: with lethal_max true the RED leg ends the episode on tick 1 and contaminates the before-measurement. Say so in the commit message so nobody 'corrects' the instruction after observing post-fix survival.

[read-only] No COMPILED_SCHEMA_VERSION bump is required -- the spec's assumption is correct. serialize_vtc_transition_schedule persists only phase_graph, social_residue_rules and a diagnostic program_counts dict; the passive-depletion and threshold-cascade programs carry no serialized clamp.
  EVIDENCE: src/townlet/vfs/transition_schedule.py:235-254, read in full.
  IMPLICATION: Do not touch COMPILED_SCHEMA_VERSION. Task 2 remains the only bump in the batch (D1 stands).

[measured] Hash movement CONFIRMED exactly as the spec records: transition_graph_hash b6adac05f9d224b2610ad56cd4bb185f4efcae9c1766dec510fa11be9d6baea6 -> e2047c67013a05c460b8b41514fd3ed18183601e4fc935ef41673b9a74ff8da8. Additional facts the spec omits: transition_graph_hash is PACK-level and identical for L0_0 and L1; vfs_hash moves per level (L1 65ba046e134a2e90... -> 578405d46db90103..., L0_0 9ddda35aebfb2357... -> 17c3a976235403ff...); drive_hash and brain_hash are UNCHANGED.
  EVIDENCE: Compiled configs/default_curriculum at both levels with use_cache=False on unpatched HEAD and on the patched worktree; printed universe hash attributes. clamp enters the canonical form at src/townlet/vfs/schema_hashes.py:189 and :230.
  IMPLICATION: Quote the pack-level nature in the commit message so a reviewer does not read 'measured on L1' as level-specific. drive_hash/brain_hash stability bounds the blast radius for tasks 4 and 5.

[measured] Test churn (spec change 10) is 37 failing tests across 6 files, and the spec's list is both over- and under-counted. Measured per-file: test_affordance_engine.py 20, test_vtc_passive_dynamics_equivalence.py 10, test_schema_hashes.py 2, test_vtc_passive_depletions.py 2, test_vtc_threshold_cascades.py 2, test_vtc_jit_kernels.py 1. Zero failures in test_meters.py, test_observations.py, test_vectorized_env.py, properties/test_environment_properties.py.
  EVIDENCE: Patched worktree: pytest tests/test_townlet/unit/environment/ tests/test_townlet/unit/vfs/ tests/test_townlet/properties/test_environment_properties.py -q --no-cov -p no:randomly -> 37 failed, 600 passed, 6 skipped. Failure reasons all mechanical: TypeError missing keyword-only args, TypeError missing positional 'meters', KeyError 'bounds'.
  IMPLICATION: Use this breakdown as the authoritative version of change 10. Two corrections: (a) tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:29 is MISSING from the spec's list -- the hand-built [{'name':'energy','depletion':{'passive':0.1}}] mapping needs a bounds key (line 69 in the same file already has one); (b) test_affordance_engine.py has 17 AffordanceEngine(...) construction sites, not 20 -- 20 is the failing-test count via shared fixtures.

[measured] The integration suite needs no changes: 384 passed, 3 skipped, 33 deselected on the fully patched worktree.
  EVIDENCE: pytest tests/test_townlet/integration/ -q --no-cov -p no:randomly on the patched worktree, 105s.
  IMPLICATION: Budget the test churn as unit-only. No integration file needs editing beyond the two new pinning-test files.

[measured] Recorded consequence (d) CONFIRMED: with the fix, money = 22.5 enters the L1 observation vector unnormalized. obs shape [1, 124], max observation value 22.5, and 22.5 is literally present in the tensor.
  EVIDENCE: Patched worktree: stock L1, money := 22.5, one WAIT step, printed obs.shape and obs.max().
  IMPLICATION: State this in the commit message as a real training-signal change; do not add a hidden normalization. Any downstream test asserting L1 obs values in [0,1] will break once a money payout occurs.

[measured] The public API surface of B2 is wider than the spec's change 6 implies: compile_vtc_threshold_cascades AND compile_vtc_threshold_cascades_with_phase_graph are both re-exported from src/townlet/vfs/__init__.py (imports at lines 95-96, __all__ at 179-180). Adding a required `meters` parameter is a public breaking change -- fine under the zero-backcompat policy, but it must be applied to both names.
  EVIDENCE: grep of src/townlet/vfs/__init__.py; both names appear in imports and __all__. My worktree patch changed both signatures and every caller then compiled clean.
  IMPLICATION: Change both function signatures in the same edit; leaving the thin wrapper unchanged makes it uncallable and the two vfs unit-test files that call the wrapper form will stay red.

[read-only] Spec change 1's ValueError for an uncovered meter index -- and therefore pinning test 7, test_meter_without_declared_bounds_is_fatal -- is UNVERIFIED against the tree. Every shipped level covers every metadata meter index, so the raise never fired in any run.
  EVIDENCE: My minimal patch implemented it as a set-difference over range(meter_count) minus covered indices; the branch was never entered across roughly 20 compiles of L0_0 and L1.
  IMPLICATION: Test 7 pins the implementer's own new code, not existing behaviour. It needs a synthetic condition (a metadata meter absent from bars.yaml) to reach the raise -- budget for that, and expect pack-level validators to possibly reject such a config before the env constructor is reached.

============================================================================
PROBE: churn

Enumerated every construction/call site and test assertion WS-1(e) touches, verified by grep and by running the suites at HEAD 30c433e3. There are 18 `AffordanceEngine(...)` sites (1 production + 17 test, not the spec's 20), 6 threshold-cascade call sites (spec names 4, omits `test_vtc_jit_kernels.py:38` and the thin wrapper `vtc.py:2279`), 5 hand-built meter-mapping sites needing a `bounds` key (spec names 2), and 8 meter/observation ceiling assertions (spec names 1) — all 8 survive the fix, six of them only because `money.initial == 0.0` and the sampler excludes INTERACT. Confirmed measured: `obs_meters` really is an unnormalized pass-through (money=22.5 → obs value 22.5, `normalization: None`), `serialize_vtc_transition_schedule` does not persist `clamp` so no schema bump is needed, and the current suite baselines are 351/1 (unit/environment) and 283/5 (unit/vfs), not the spec's stale 353/1.

--- SURPRISES ---
* The spec says '20 AffordanceEngine(...) sites'; there are 17 in tests plus 1 in production. Task 3 deleted the four get_affordance_cost tests and rewrote five onto can_afford. Worse for planning, :413 is a factory helper covering four tests, so 'sites' and 'tests' diverge — 17 edits touch 21 test functions.

* The spec's cascade-compiler churn list omits tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:38, and its hand-built-meter-mapping list omits the same file's line :29. That file exercises the JIT kernel path with monkeypatched Evaluator.evaluate that RAISES if the interpreter is touched — so it fails loudly, but only if you remember it exists. Both are spec omissions that land the batch red.

* The spec flags test_schema_hashes.py:437,460 as needing a bounds key but never mentions that the SAME test asserts a whole canonical-rule dict containing "clamp": [0.0, 1.0] at line 450. If the added fixture bounds are anything other than [0.0, 1.0], that assertion silently becomes wrong and fails with a full-dict diff, not a clear message.

* The spec's hold-constant baseline of '353 passed, 1 skipped' for unit/environment/ is stale — the tree is at 351 passed, 1 skipped. An implementer treating 353 as the target will hunt for two tests that no longer exist.

* money is NOT uniformly 999999.0. It is 1000000.0 in six packs and 100.0 in configs/test/items_smoke/levels/L0_smoke — three distinct ceilings. And configs/test/items_smoke/bars.yaml (pack root) is dead pre-v2 legacy with `id`/`min_value`/`max_value` and no bounds key at all; it would not parse under BarsV2Config, so it is a trap for anyone shopping for a spec-test-7 fixture.

* CLAUDE.md says L0_0_minimal has 1 affordance and obs_dim 29. Measured: L0_0_minimal deploys all 14 affordances INCLUDING WORK, and L1's obs_dim is 124, not 29. This matters because the property test at test_environment_properties.py:97 survives on the sampler (randint 0..6, INTERACT is 8), NOT on 'L0_0 has no money affordance' — the affordance is right there. The comment the spec mandates should say so.

* The spec's item 10 lists exactly one meter-ceiling assertion. There are eight. Six of them (test_meters.py:65,77,270; test_vectorized_env.py:289; test_observations.py:137,702) pass only because money.initial is 0.0 at reset and nothing steps INTERACT. Two of those — test_observations.py:137 and :702 — carry comments explicitly asserting 'Meters: [0, 1]' and 'normalized to [0, 1]', which become false statements about the system the moment this lands, even though the assertions stay green.

* Recorded consequence (d) checked out but was worth verifying: observation_builder.py:127 DOES contain a min/max normalization clamp, which looked like it might contradict the pass-through claim. It does not — that clamp is confined to the log_scaled/clipped_log_scaled branch. Measured directly: obs_meters has normalization None and money=22.5 emerges as 22.5, taking obs.max() from ~2.0 to 22.5.

* CompiledUniverse.from_dict REBUILDS the depletion and cascade programs from level.bars (so no schema bump — spec confirmed) but READS transition_graph_hash and vfs_hash straight from the cached payload without recomputing. Six .msgpack caches are sitting in the working tree right now, gitignored. Post-fix, any of them serves a pre-fix hash alongside post-fix clamps. The spec's '-delete first' is load-bearing, not hygiene.

* The one thing that looked like it should break and does not: test_vtc_passive_dynamics_equivalence.py compares compiled output against a legacy reference that hardcodes clamp [0,1], over all 5 levels and all 8 meters including money. It survives purely because SAMPLE_BAR_VALUES tops out at 1.0 and every cascade delta is non-positive. Genuinely UNAFFECTED, but it is the sharpest edge in the suite and deserves a note rather than silence.

--- FACTS ---
[measured] There are 18 AffordanceEngine(...) construction sites, not 20: one in production and seventeen in tests/test_townlet/unit/environment/test_affordance_engine.py. That file is indeed the only file constructing the engine.
  EVIDENCE: grep -rn 'AffordanceEngine(' src/ tests/ scripts/. Production: src/townlet/environment/vectorized_env.py:700 (positional: affordance_config, num_agents, device, meter_name_to_idx, then kwargs). Tests, all in tests/test_townlet/unit/environment/test_affordance_engine.py: 257, 267, 276, 284, 309, 325, 342, 356, 376, 389, 402, 413, 542, 588, 660, 715, 766. `grep -c` returns 17. tests/test_townlet/integration/test_affordance_interaction_gating_runtime.py:123 imports the class but only calls inspect.signature/hasattr — it never constructs.
  IMPLICATION: MECHANICAL x18. Budget 18 edits, not 20. Site :413 is the `TestDictCostAffordances._build_engine` factory — one edit there covers four tests (:415, :434, :459, :476), so 17 test edits cover 21 test functions. Sites 257-413 use the kwarg form `meter_name_to_idx=bars_config.meter_name_to_index`; sites 542-766 are multi-line calls. AffordanceEngine.__init__ (affordance_engine.py:54-67) currently takes 4 positional params then 8 optional kwargs; the two new required keyword-only tensors go after a `*`.

[measured] There are 6 threshold-cascade compiler call sites, not the 4 the spec lists. It omits tests/test_townlet/unit/vfs/test_vtc_jit_kernels.py:38 and the thin wrapper inside vtc.py itself.
  EVIDENCE: grep -rn 'compile_vtc_threshold_cascades' over the whole repo (excluding docs/ and .venv). Production: src/townlet/vfs/vtc.py:2279 (the `compile_vtc_threshold_cascades` wrapper delegating to the phase-graph variant) and src/townlet/vfs/transition_schedule.py:217. Tests: unit/vfs/test_vtc_threshold_cascades.py:16 and :35; unit/vfs/test_vtc_jit_kernels.py:38 (`vtc.compile_vtc_threshold_cascades([{"source": "satiation", "target": "energy", "threshold": 0.3, "strength": 0.006}])`); unit/vfs/test_vtc_passive_dynamics_equivalence.py:108; unit/vfs/test_schema_hashes.py:775. Re-exports at src/townlet/vfs/__init__.py:95,96 and __all__ at :179,180 need no edit.
  IMPLICATION: MECHANICAL x6. test_vtc_jit_kernels.py:38 is a spec omission — if the implementer works only from item 10's list, that file lands red. Its cascade target is `energy` and the bars_state it passes has no meters config, so it needs a synthetic `meters` argument declaring energy bounds [0.0, 1.0] to keep its expected `[0.997, 1.0, 1.0]` result. transition_schedule.py:217 gets `level.bars.meters` (line 215 already passes them to the depletion compiler, so B1 needs no call-site change there).

[measured] Five test sites hand-build meter mappings/objects fed to a passive-depletion compiler; three of them are dicts that will need a `bounds` key, and the spec names only two.
  EVIDENCE: Dicts (need `bounds`): unit/vfs/test_vtc_passive_depletions.py:13-19 (one dict) and :42-46 (two dicts, `energy` and `health`); unit/vfs/test_vtc_jit_kernels.py:29; unit/vfs/test_schema_hashes.py:434-435 (`meter` and `changed_meter`, consumed at :437 and :460). Real MeterConfig objects (already carry .bounds, UNAFFECTED): unit/vfs/test_vtc_passive_dynamics_equivalence.py:102 (`bars.meters`) and unit/vfs/test_schema_hashes.py:783 (`compiled.get_level(...).bars.meters`).
  IMPLICATION: MECHANICAL x3 files (5 dict literals total: 1 + 2 + 1 + 2). test_vtc_jit_kernels.py:29 is a spec omission. The Mapping branch of `_coerce_passive_depletion` (vtc.py:2400-2412) will read `meter['bounds']['min']/['max']` as required keys, so every dict must gain them. Declare `{"bounds": {"min": 0.0, "max": 1.0}}` in all five so the downstream clamp assertions stay byte-identical.

[measured] Exactly three test assertions pin a clamp literal that this change touches. Two are METER bounds, one is the MODULATION multiplier and must stay (0.0, 1.0). Everything else that mentions `clamp` in tests is either an authored fixture input or a non-meter rule family.
  EVIDENCE: grep for `.clamp ==` across tests/. METER: unit/vfs/test_vtc_passive_depletions.py:32 `assert rule.clamp == (0.0, 1.0)` (passive_depletion of `energy`); unit/vfs/test_vtc_threshold_cascades.py:31 `assert rule.clamp == (0.0, 1.0)` (threshold_delta targeting `energy`). MODULATION (STAYS): unit/vfs/test_vtc_modulations.py:37 `assert rule.clamp == (0.0, 1.0)` — verified its variable_id is `affordance.WORK.multiplier`, i.e. B3. UNAFFECTED: unit/vfs/test_schema.py:419 (authored WriteSpec round-trip); unit/vfs/test_vtc_terminal_conditions.py:52 and test_vtc_affordance_gates.py:34 (`clamp is None`); all `clamp=` values in test_vtc_action_writes.py, test_vtc_affordance_occupancy.py, test_action_config_extension.py and test_vectorized_env.py:416,443 are fixture inputs to hand-built WriteSpecs, not compiler outputs.
  IMPLICATION: MECHANICAL if the added `bounds` in each fixture declares [0.0, 1.0] — both assertions then stay byte-identical. SEMANTIC (new literal required) for any other bounds choice. Recommend [0.0, 1.0] everywhere. Note `_required_clamp` (vtc.py:2983) raises for passive-depletion and threshold-cascade rules, so `clamp` must stay a concrete tuple for those two families — never None.

[measured] tests/test_townlet/unit/vfs/test_schema_hashes.py has 14 `clamp` literals in canonical-schema dicts; exactly ONE of them is a meter bound. The spec's item 10 flags the input dicts at :434-435 but never mentions the asserted output literal.
  EVIDENCE: grep -n clamp on the file gives lines 234, 261, 336, 360, 414, 450, 489, 504, 539, 590, 618, 645, 689, 714. Classified by enclosing test and nearest `kind` key: :450 is inside `test_transition_graph_hash_binds_passive_depletion_rules`, in the dict whose `"kind": "passive_depletion"` sits at :442 — this is THE meter bound. :414 is the modulation test (STAYS). :234/:261 are action-ABI writes; :336/:360 are authored action writes; :489/:504 interaction progress; :539 terminal condition; :590/:618/:645 reward components; :689/:714 social residue — all authored or None, all UNAFFECTED.
  IMPLICATION: MECHANICAL if the fixture at :434-435 declares bounds [0.0, 1.0] — the asserted `"clamp": [0.0, 1.0]` at :450 is then unchanged. SEMANTIC otherwise. Do not overlook it: the assertion is an exact whole-dict `==` comparison, so a mismatch fails with a full-dict diff.

[measured] Eight tests assert a meter or observation upper bound, not the one the spec names. All eight still pass after the fix, but for six of them the reason is `money.initial == 0.0` at reset, not any structural guarantee.
  EVIDENCE: grep for `<= 1.0` / `.max() <=` / `torch.all(` across tests/. (1) properties/test_environment_properties.py:97 `torch.all(meters <= 1.0)` — sampler is `torch.randint(0, 6, ...)`; measured `env.action_ids['INTERACT'] == 8`, so INTERACT is unreachable. (2) unit/environment/test_meters.py:270 — sets `env.meters = torch.ones(1,8)` and asserts immediately with no step: already vacuous. (3) test_meters.py:65 — one passive-depletion tick, and the assertion explicitly excludes index 3; money's passive rate is 0.0. (4) test_meters.py:77 — energy-only. (5) unit/environment/test_vectorized_env.py:289 — reset only. (6) unit/environment/test_observations.py:137 `(meters <= 1.0).all()` — reset only. (7) test_observations.py:702 `obs.max() <= 2.0` — reset only. (8) unit/effects/test_sample_command.py:38 — energy only. Measured initial_meter_values for L0_0 and L1 = [1.0, 1.0, 1.0, 1.0, 0.0, 0.5, 1.0, 1
  IMPLICATION: All eight are UNAFFECTED. Add the spec's inline comment at properties/test_environment_properties.py:97, and add the same warning to test_observations.py:137 and :702 — those two now sit one `env.meters[0, money] = 22.5` away from failing, and their comments currently claim 'Meters: [0, 1]' / 'normalized to [0, 1]', which is false post-fix. Do NOT delete or weaken any of the eight.

[measured] Recorded consequence (d) is CONFIRMED by measurement: obs_meters is a raw pass-through with no normalization spec, so money=22.5 enters the 124-dim L1 observation as the literal 22.5.
  EVIDENCE: Built L1_full_observability via make_vectorized_env_from_pack, set env.meters[0, money_idx]=22.5, called env._get_observations(). Field obs_meters spans [93,101) with `normalization: None`; the slice came back [1.0, 1.0, 1.0, 1.0, 22.5, 0.5, 1.0, 1.0] and obs.max() == 22.5 against obs_dim 124. The generic normalization clamp at src/townlet/vfs/observation_builder.py:127 is inside the `log_scaled`/`clipped_log_scaled` branch and never runs for obs_meters.
  IMPLICATION: The commit message's training-signal claim is correct and must be stated. It also means the observation vector's dynamic range jumps by ~5 orders of magnitude the moment WORK pays out, which will move any checkpoint's behaviour. Do not add a hidden normalization (§ the spec forbids it), but do flag test_observations.py:137/:702 as landmines per the fact above.

[measured] No test pins a literal transition_graph_hash or vfs_hash value. The four 16-hex digests in the tree are docstring prose.
  EVIDENCE: grep -rnE '[0-9a-f]{16,}' over tests/. Hits at integration/test_compile_cache_level_identity.py:12-16 and unit/universe/test_compiled_universe_primary_level.py:10 are both inside module docstrings. The only hash comparison, test_compile_cache_level_identity.py:128, is relational (`after.vfs_hash != before.vfs_hash`). unit/training/test_checkpoint_utils.py compares against `compiled_universe.vfs_hash`/`.drive_hash` computed live, and its bad values are `deadbeef`-style sentinels.
  IMPLICATION: UNAFFECTED — no test churn from the hash move. Those five docstring lines become stale documentation; correcting them is optional and separate from the batch.

[measured] CompiledUniverse.from_dict reads transition_graph_hash and vfs_hash straight out of the cached payload rather than recomputing them, while the passive-depletion and threshold-cascade programs ARE rebuilt from level.bars. Six stale .msgpack caches are live in the working tree right now.
  EVIDENCE: src/townlet/universe/compiled.py:469 and :535 use `_required_field(payload, 'transition_graph_hash')`; :437 and :514 call `build_vtc_transition_schedule(...)` which rebuilds via transition_schedule.py:215/217. serialize_vtc_transition_schedule (transition_schedule.py:235-254) persists only phase_graph, social_residue_rules and diagnostic program_counts — no clamp, confirming the spec's no-schema-bump claim. `find configs -name '*.msgpack'` returns 6 files (5 under default_curriculum/.compiled/, 1 under aspatial_test/.compiled/); `git check-ignore` confirms *.msgpack is gitignored at .gitignore:141.
  IMPLICATION: Confirms no COMPILED_SCHEMA_VERSION bump (D1 intact). But a stale cache serves the PRE-fix hash alongside POST-fix clamps — a silent inconsistency. The spec's `find configs -name '*.msgpack' -delete` before verification is mandatory, not hygiene, and the six files are present today.

[measured] No shipped config pack has a metadata meter without a declared bar, so change 1's new ValueError will not fire on any pack — but the code path that permits the mismatch is still live and silently defaults.
  EVIDENCE: Compared environment.yaml meter vocabulary against per-level bars.yaml meters for all 24 level packs: every one is an exact match (env_only=[] and bars_only=[] in all 24). The permissive path is src/townlet/universe/compilers/metadata.py:39-56 — `build_meter_metadata` iterates `environment.environment.meters` and does `bar_cfg = meter_lookup.get(meter.name); initial = bar_cfg.initial if bar_cfg else 0.0`. Symmetrically, vectorized_env.py:239-243 iterates bars and does `self.meter_name_to_index.get(bar.name)` with an `if idx is not None` skip.
  IMPLICATION: UNAFFECTED for all existing packs and fixtures — no VectorizedHamletEnv construction breaks. Spec test 7 (`test_meter_without_declared_bounds_is_fatal`) has no ready-made fixture; the implementer must author a tmp_path pack that adds a meter to environment.yaml without adding it to bars.yaml. Note that metadata.py:39-56 is itself a silent-default of exactly the kind the no-defaults principle forbids; leaving it is in scope-creep territory but worth a comment.

[measured] `money` is the only bar name declaring non-[0,1] bounds, but it does so at three different magnitudes across packs, and one non-level bars.yaml is dead legacy that will not parse under BarsV2Config.
  EVIDENCE: Scanned all 25 bars.yaml. Non-[0,1]: money 999999.0 in the 5 default_curriculum levels plus test/model_config, model_config_4meter, model_config_12meter; money 1000000.0 in aspatial_test/L0, test/action_masking, test/action_space/{aspatial,continuous1d,grid2d}, test/gridnd_4d_pack; money 100.0 in test/items_smoke/levels/L0_smoke. configs/test/items_smoke/bars.yaml (pack root, not a level) uses the pre-v2 schema (`id`/`min_value`/`max_value`/`decay_rate`) and declares no `bounds` at all; tests load `primary_level='L0_smoke'`, i.e. the levels/ file, so the root file is unreferenced.
  IMPLICATION: The spec's consequence (b) is directionally right but understates it — the 100.0 ceiling in items_smoke is a real, reachable ceiling, and integration/test_items_integration.py:128-147 asserts a money delta of ~0.1 through it (safe: stays far below 100). Do not reach for configs/test/items_smoke/bars.yaml as the spec-test-7 fixture; it is dead and would not even parse.

[measured] unit/vfs/test_vtc_passive_dynamics_equivalence.py compares the compiled programs against a legacy formula that hardcodes torch.clamp(..., min=0.0, max=1.0) for both depletion and cascades, across all 5 default_curriculum levels and all 8 meters including money — and it stays green only because of the sample data.
  EVIDENCE: Read the helpers: SAMPLE_BAR_VALUES = [0.0, 0.05, 0.15, 0.25, 0.35, 0.55, 1.0] (max 1.0), and `_legacy_decay_and_cascades` clamps to [0,1] at both stages. Measured L1 cascade strengths are all >= 0 (0.004, 0.006, 0.001, 0.003, 0.0) and the compiled expression negates strength, so every cascade delta is <= 0; passive depletion only subtracts. No value can ever exceed the sampled 1.0 ceiling.
  IMPLICATION: UNAFFECTED — leave the legacy formula's hardcoded (0.0, 1.0) alone. But say so in the commit message: this is the closest near-miss in the suite. If anyone ever raises SAMPLE_BAR_VALUES above 1.0 or introduces a negative-strength cascade, this test becomes a false red against the legacy side, not the VTC side. Line 108 still needs the new `meters` argument (MECHANICAL).

[measured] `env.money_idx` no longer exists anywhere in src/ — task 3 already removed the name-keyed money special case that the owner's non-negotiable forbids.
  EVIDENCE: `grep -rn 'money_idx' src/townlet/` returns nothing. The only surviving reference is the vestigial `money_idx = None` attribute on the EnvStub at tests/test_townlet/unit/environment/test_vectorized_env.py:1005.
  IMPLICATION: UNAFFECTED, and good news: the implementer is not editing next to a live antipattern. The stub attribute is harmless dead weight; deleting it is optional and out of scope for this unit.

[measured] Both mechanism-A clamp pairs live inside exactly the functions spec test 6 inspects, so `test_no_hardcoded_meter_bounds_remain` genuinely covers all four sites.
  EVIDENCE: action_executor.py: `_execute_actions` spans lines 20-159 and contains both `torch.clamp(env.meters, 0.0, 1.0)` at :60 and :150 (the next def, `_handle_interactions`, starts at :160). affordance_engine.py: `apply_instant_interaction` spans 154-234 and contains the clamp at :232; `apply_vtc_multi_tick_effects` spans 257-334 and contains the clamp at :332. `grep -rn 'torch.clamp' src/townlet/` confirms no fifth meter-tensor clamp anywhere — every other hit is substrate positions, expression builtins, RNG guards, curriculum averages or the log-scaled normalization branch.
  IMPLICATION: Spec test 6 is sound as written; no extra function needs adding to its inspect list. The vtc.py literals at :2314 (threshold cascade), :2384 (passive depletion) and :2456 (modulation, STAYS) are the B-side, and only the first two are inside the two compilers test 6 names.

[measured] The spec's per-suite baselines have drifted. Current numbers at HEAD 30c433e3 are lower than the 690a88a7 figures the spec tells the implementer to hold constant.
  EVIDENCE: CUDA_VISIBLE_DEVICES="" UV_CACHE_DIR=.uv-cache uv run pytest -q --no-cov. tests/test_townlet/unit/environment/ → 351 passed, 1 skipped (spec says 353 passed, 1 skipped). tests/test_townlet/unit/vfs/ → 283 passed, 5 skipped. The spec's full verify set (unit/environment + unit/vfs + properties/test_environment_properties.py + integration/test_custom_actions.py) → 640 passed, 6 skipped in 47s.
  IMPLICATION: Use 351/1 and 640/6 as the hold-constant baselines, not 353/1. The 2-test drop is task 3 deleting the four get_affordance_cost tests and rewriting five onto can_afford — expected, not a regression.

[measured] tests/test_townlet/integration/test_temporal_mechanics.py is already 15-failed/2-passed and is excluded from the default gate. Do not attribute its failures to this change.
  EVIDENCE: The file carries a module-level `pytestmark = pytest.mark.slow` at :92 and pyproject.toml addopts include `-m not slow`, so it shows as '17 deselected' in a normal run. Forcing `-m slow`: 15 failed, 2 passed in 2.1s, with errors like 'Bed affordance not deployed in test config', 'Job affordance not deployed', FileNotFoundError, and 'assert torch.Size([1, 124]) == (1, 93)'. Its money assertions at :338/:349/:372/:434-448 index `env.meters[0, 3]`, but measured money is index 4 in default_curriculum (index 3 is hygiene), and it sends action id 4 as INTERACT when INTERACT is 8.
  IMPLICATION: UNAFFECTED and pre-existing red. Its absolute money literals (0.55625, 0.6125, 0.66875, 0.78125) look like they would go SEMANTIC, but the file does not execute in the gate and is broken on config drift independent of bounds. State the pre-existing status in the commit message so the batch is not blamed.

============================================================================
PROBE: blast-radius

Confirmed by measurement: obs_meters is a raw pass-through of env.meters, so with bounds wired money enters the observation unnormalized — obs[0,97] == 22.5 exactly, and a WORK-maximising oracle policy on L1 reaches 9281.98 inside the 1000-step cap while the other 123 features stay in [0,1]. The declared normalization surface (VariableDef.normalization / VFSObservationField.normalization) is doubly inert: it is hardcoded None for obs_meters and apply_normalization has zero production callers, while the compiled field description literally claims "(normalized)". The training-signal risk is worse than the spec implies because the shipped brain.yaml sets layer_norm: false, so raw money propagates linearly to Q-values (|Q|max 0.15 → 141.5) and quadratically into RND novelty (MSE 0.013 → 11820), and the blast radius is all five default_curriculum levels, not L1 alone.

--- SURPRISES ---
* The shipped brain is NOT SimpleQNetwork and has NO LayerNorm. configs/default_curriculum/brain.yaml declares `type: feedforward` with `layer_norm: false`, and `grep -rn SimpleQNetwork src/` finds zero production construction sites — the class exists only as dead code plus a docstring reference in rnd.py. CLAUDE.md's 'SimpleQNetwork (Full observability - L0, L0.5, L1): MLP ... Simple MLP Q-network with LayerNorm' and its claim that L2/L3 use RecurrentSpatialQNetwork are both false against the shipped configs. This inverts the severity of the observation finding: there is no normalization layer anywhere to absorb a 22.5+ input, and measured |Q|max scales 0.15 -> 141.5 with money.

* The compiled obs_meters field description literally reads "8 meter values (normalized)" while nothing normalizes it. `apply_normalization` in vfs/observation_builder.py has ZERO production callers — the whole VFS normalization ABI (minmax, zscore, log_scaled, one_hot, cyclical_sin_cos...) is implemented, tested, hashed into schema hashes, and never executed. dynamic_needs.py even attaches `_unit_interval_normalization()` to its VariableDefs, and it is still never applied. So the 'right place to fix the observation issue' exists as a declared surface and is inert twice over.

* Spec consequence (e) is literally false. `configs/L5_multi_agent/levels/L5_multi_agent/drive.yaml` and `configs/simple/levels/L0_simple/drive.yaml` each reference money twice: a `money_crisis` range modifier (`bar: money`) applied to the intrinsic weight, and a `bar_bonus {bar: money, center: 0.0, scale: 0.3}` inside constant_base_with_shaped_bonus. Behaviourally inert today only because both packs declare money `bounds.max: 1.0`. It is a latent trap: dac_engine.py:200 computes `bonus = scale * (bar_value - center)` with no clip (every shipped pack has `composition.clip: null`), and dac_engine.py:135 initialises the modifier multiplier to `ranges[-1].multiplier`, so a value above the last declared range silently inherits the TOP range's multiplier rather than erroring.

* Spec consequences (b) and (c) understate scope. All FIVE default_curriculum levels declare money.bounds.max = 999999.0 and all five enable all 14 affordances including WORK — L0_0_minimal is not 'one affordance' as CLAUDE.md claims. Measured wired peaks: L0_0 9281.98, L2 9281.98, L3 534.83 (WORK's opening_hours 9-17 binds only once temporal mechanics are enabled). The economy comes alive across the whole curriculum, and the L0 'Low Energy Delirium' teaching level acquires an unbounded money feature at the same time.

* Wiring bounds materially changes SURVIVAL, not just money. Under an identical oracle policy the max episode length goes from 92 (unwired) to 1000, the 1000-step cap, and mean episode length from 19.8 to 26.7 — because EAT/DOCTOR become affordable. That directly moves the input to the intrinsic-weight annealing gate (adaptive_config threshold 100.0, documented as requiring mean survival > 50 steps), so exploration annealing behaviour changes as a second-order effect of this commit.

* RND novelty becomes a money odometer. RNDNetwork has no input normalization and normalizes only its output MSE by a running std; money is monotone non-decreasing on L1 (all three depletion channels 0.0, natural recovery 0.0, and money is neither source nor target of any of the five declared cascades). Measured MSE 0.0130 -> 11820.67 as money goes 0 -> 9282, roughly quadratic. The agent is therefore paid a large, permanent, never-annealing intrinsic bonus for banking money. Given the project's stated love of 'interesting failures' this may be a feature, but it is not currently anyone's stated intent.

* Every shipped config pack uses `drive.yaml`, not `drive_as_code.yaml`. CLAUDE.md states in two places that `drive_as_code.yaml` is REQUIRED for all config packs; the only file with that name in the repo is configs/test/items_smoke/drive_as_code.yaml. A grep written against the documented filename would have returned zero hits and falsely 'confirmed' that no level references money.

* Frontend money display is already wrong by 100x by design: formatting.js maps money to dollars as `value*100` on the assumption money is [0,1]. WORK's 22.5 payout will render as "$2250" and a banked 9282 as "$928200", while the meter bar silently saturates (getMeterPercentage clamps to 100). Separately, video_renderer.py's hardcoded meter_names ordering ([energy, hygiene, satiation, money, health, ...]) does not match the compiled L1 meter order ([energy, health, satiation, hygiene, money, ...]), so recorded videos already mislabel four meters.

--- FACTS ---
[measured] obs_meters is a verbatim pass-through of env.meters into the 124-dim observation. Money sits at absolute observation index 97 (obs_meters field start_index 93 + money meter index 4). Setting env.meters[:, money] = 22.5 yields obs[0,97] == 22.5 exactly, with obs row min/max = 0.0 / 22.5.
  EVIDENCE: MEASURED on stock HEAD (no patch). Script built L1 via UniverseCompiler(use_cache=False) + VectorizedHamletEnv.from_universe, then env._get_observations(). Output: `obs_meters field obs_meters 93 101 8 semantic bars desc 8 meter values (normalized)`; `obs slice meters: tensor([1.0,1.0,1.0,1.0,22.5000,0.5,1.0,1.0])`; `value at money index: 22.5`. Path: environment/observation_encoder.py:_sync_meter_observation_to_vfs does `meter_values = env.meters.to(...)` then `vfs_registry.set("obs_meters", ...)` with no transform.
  IMPLICATION: Spec consequence (d) is correct and must be stated in the commit message as written. Do not add normalization in this task.

[measured] The shipped Q-network for the whole default curriculum has NO LayerNorm, and SimpleQNetwork (the class CLAUDE.md describes) has zero production construction sites. population/vectorized.py:_build_network dispatches only to NetworkFactory.build_*; configs/default_curriculum/brain.yaml declares `type: feedforward` with `layer_norm: false`, producing Sequential(Linear(124,256), ReLU, Linear(256,128), ReLU, Linear(128,15)). Q-value magnitude therefore scales linearly with money.
  EVIDENCE: MEASURED. `grep -rn SimpleQNetwork src/` returns only networks.py itself plus a docstring mention in exploration/rnd.py:72 — no constructor call. Built the real config-driven network via NetworkFactory.build_feedforward(FeedforwardConfig(hidden_layers=[256,128], activation='relu', dropout=0.0, layer_norm=False), 124, 15) on a random obs in [0,1] with only index 97 varied: money=0.0 -> |Q|max 0.1516; 0.5 -> 0.1506; 22.5 -> 0.3227; 100.0 -> 1.4848; 9282.0 -> 141.5074 (argmax flips from 13 to 1 between 22.5 and 100).
  IMPLICATION: The implementer's statement of the training-signal change must not lean on 'LayerNorm will absorb it'. It will not — there is no LayerNorm on the shipped path. CLAUDE.md's 'SimpleQNetwork ... MLP with LayerNorm' is false and its correction is a separate concern from this task.

[measured] Measured peak money on the wired build, L1, under a WORK-maximising oracle policy: 9281.98 in the single episode that reached the 1000-step cap; mean per-episode peak 81.30 over 112-115 episodes. Identical script on unwired HEAD: peak 1.00, mean 0.02. Episode length also moves: unwired max 92 / mean 19.8; wired max 1000 / mean 26.7.
  EVIDENCE: MEASURED, two arms of the same script (3000 env steps, reset on done, scripted policy: EAT if satiation<0.4, DOCTOR if health<0.4, WORK if energy>0.45 and mood>0.15, else SLEEP, teleporting env.positions onto the target). Wired arm run twice, reproducible: `max=9281.98 mean=83.08 / max=9281.98 mean=81.30`. Unwired arm (main src): `max=1.00 mean=0.02`. Caveat: this is a hand-written oracle, not a learned policy — the defensible claim is that the runtime PERMITS ~9.3e3, not that a trained agent will reach it.
  IMPLICATION: State an order of magnitude in the commit message, not just '22.5'. The observation feature can reach ~1e4 while every other feature is in [0,1].

[measured] The declared normalization surface is inert in two independent ways, and this is the locus question 4 asks about. (i) `apply_normalization` (vfs/observation_builder.py:83) has ZERO production callers — the only references in the repo are its own definition, the two __init__ re-exports, and tests/test_townlet/unit/vfs/test_observation_builder.py. build_vfs_observation never calls it. (ii) For obs_meters specifically the spec is never even populated: universe/compilers/observation.py build_vfs_variables emits `normalization=None` as a hardcoded literal for every synthesized observation primitive, and build_vfs_observation_fields sets `norm = env_norm_by_name.get(field.name)` which is None because no pack declares an environment variable named obs_meters.
  EVIDENCE: READ + MEASURED. grep for apply_normalization across src/ and tests/ (only test callers). Read universe/compilers/observation.py:362-414. Runtime probe printed `VFSObservationField obs_meters normalization: None` and `VariableDef obs_meters normalization: None`. Contrast: vfs/dynamic_needs.py:148 DOES attach `_unit_interval_normalization()` to its VariableDefs — and it is still never applied, which is what proves (i) independently of (ii).
  IMPLICATION: Report this locus and its inertness; do NOT wire or design a normalization inside WS-1(e) — the spec explicitly forbids papering over the training-signal change here. It is a separate unit.

[measured] RND intrinsic reward becomes a monotone proxy for accumulated money. RNDNetwork is a bare 3-layer MLP (Linear(obs,256)-ReLU-Linear(256,128)-ReLU-Linear(128,embed)) with no input normalization; only the OUTPUT MSE is divided by a running std. Money is monotone non-decreasing within an episode on L1 (passive/move/interact depletion all 0.0, recovery.natural 0.0, and money appears in no cascade as source or target), so novelty grows without bound as the agent banks money.
  EVIDENCE: MEASURED. rnd.py:68-83 (no normalization layer), rnd.py:219 `normalized = mse_per_sample / (sqrt(reward_rms.var)+1e-8)` — reward-side only. Probe with two fresh RNDNetwork(124,128) on a [0,1] obs varying only index 97: money=0.0 -> mse 0.0130; 0.5 -> 0.0129; 22.5 -> 0.0845; 100.0 -> 1.4004; 9282.0 -> 11820.6680 (~9e5x). L1 drive.yaml uses `intrinsic: {strategy: adaptive_rnd, base_weight: 0.1, apply_modifiers: [energy_crisis]}`, so this path is live. L1 bars.yaml money block: passive 0.0 / move 0.0 / interact 0.0 / natural 0.0; the five declared cascades are satiation->health, satiation->energy, mood->energy, hygiene->mood, hygiene->social — money is neither source nor target.
  IMPLICATION: This is the concrete 'training collapse next week' mechanism: the agent gets a permanent, never-annealing intrinsic bonus for hoarding money. It is arguably a pedagogically valuable 'interesting failure', but it must be named in the commit message, not discovered later.

[measured] Gradient clipping is GLOBAL (torch.nn.utils.clip_grad_norm_ over all parameters, max_norm from training.yaml `max_grad_norm: 10.0`), so a dominating money input column starves every other parameter rather than only distorting money's own weight.
  EVIDENCE: MEASURED + READ. population/vectorized.py:881 and :958 call clip_grad_norm_(self.q_network.parameters(), max_norm=self.max_grad_norm). configs/default_curriculum/levels/L1_full_observability/training.yaml:67 `max_grad_norm: 10.0`. Probe on the first Linear layer at money=22.5: money column grad L2 = 31.82 vs median other column L2 = 0.7388 (43.1x). At money=9282 the ratio is ~400x larger again.
  IMPLICATION: When the implementer states the training-signal change, the mechanism is 'one input column dominates the global grad norm and the clip scales everything else down', not merely 'money's weight learns faster'.

[measured] Blast radius is the WHOLE default curriculum, not L1. All five levels (L0_0, L0_5, L1, L2, L3) declare money.bounds.max = 999999.0 and all five enable all 14 affordances including WORK (+22.5) with 7 money-costed affordances. Measured wired peaks: L0_0 9281.98, L2 9281.98, L3 534.83 (lower because WORK's opening_hours 9-17 binds once temporal mechanics are on). money is confirmed the only meter NAME with non-unit bounds anywhere (18 hits across 24 bars.yaml, all money; test packs use 1000000.0, items_smoke 100.0).
  EVIDENCE: MEASURED (same oracle script, wired build, per level) plus a YAML scan of every configs/**/bars.yaml printing meters whose bounds != [0.0,1.0]. Compiled L1 bounds read back exactly: money 0.0/999999.0, lethal_min False, lethal_max False; every other meter 0.0/1.0 (energy and health have lethal_min True).
  IMPLICATION: Spec consequences (b)/(c) understate scope. 'The shipped L1 economy comes alive' should read 'the shipped economy comes alive at every default_curriculum level'. CLAUDE.md's 'L0_0_minimal: 3x3 grid, 1 affordance' is also false against training.yaml.

[measured] Terminal conditions are safe: bounds.max drives a lethal condition ONLY when lethal_max is true, and no pack anywhere sets it. There is no generic out-of-range or non-finite guard independent of lethal_*.
  EVIDENCE: READ. vfs/vtc.py compile_vtc_terminal_conditions_with_phase_graph emits a rule only inside `if meter["lethal_min"]` / `if meter["lethal_max"]`. `grep -rn "lethal_max: true" configs` returns zero hits. Compiled L1 read-back: money lethal_min False, lethal_max False.
  IMPLICATION: No terminal-condition work is needed. The pinning-test note about keeping `lethal_max: false` explicit remains correct but is belt-and-braces.

[read-only] Frontend and video telemetry hardcode the [0,1] assumption and will silently misreport money. formatting.js formatMeterValue special-cases money as `$${Math.round(value*100)}` — money=22.5 renders as "$2250", money=9282 as "$928200". getMeterPercentage clamps to [0,100] so the bar saturates without warning. video_renderer._render_meters draws barh on an xlim(0,1) axis and labels `value*100:.0f%`, so the money bar overruns the plot.
  EVIDENCE: READ. frontend/src/utils/formatting.js:25-42; frontend/src/components/MeterPanel.vue:232-236 (`isCritical` also uses value*100 < 20); src/townlet/recording/video_renderer.py:211-237; src/townlet/recording/data_structures.py:61 comment 'meters, normalized [0,1]'. Separately, video_renderer's hardcoded meter_names list is [energy, hygiene, satiation, money, health, fitness, mood, social] while the compiled L1 order is [energy, health, satiation, hygiene, money, fitness, mood, social] — a pre-existing mislabel.
  IMPLICATION: Out of scope for WS-1(e) but should be named as a known consequence so the first demo run's "$2250" is not mistaken for a runtime bug.

[measured] The RecurrentSpatialQNetwork meter-encoder risk is CONDITIONAL, not live. Its meter_encoder is Linear(8,32)+ReLU with no LayerNorm, feeding the LSTM directly alongside a LayerNormed vision branch — embedding L2 norm goes 1.79 (money=0.5) -> 18.996 (22.5) -> 7864.0 (9282) against a vision branch at 8.33. But configs/default_curriculum/brain.yaml is pack-level with `type: feedforward` and there is no per-level brain.yaml, so no shipped curriculum level instantiates it.
  EVIDENCE: MEASURED (encoder probe) + READ (`find configs -name brain.yaml` shows one brain.yaml per pack; default_curriculum's is `type: feedforward`; L2/L3 training.yaml contain no architecture override).
  IMPLICATION: Do not report this as a live L2/L3 risk. Report it as 'if brain.yaml is switched to recurrent'. Note in passing that CLAUDE.md's claim that L2/L3 use RecurrentSpatialQNetwork is false against brain.yaml.

[read-only] No other consumer assumes [0,1]. Replay buffer and training/state.py carry no meter statistics; curriculum adversarial.py/static.py `active_meters` is a static per-stage NAME list, not a computed statistic; there is no obs clamp/scale anywhere in the population rollout path; tensorboard_logger.log_meters only writes scalars (its docstring says 0.0-1.0 but nothing enforces it).
  EVIDENCE: READ. grep for 'meters' across training/replay_buffer.py, training/state.py, curriculum/*.py, and grep for obs clamping (`clamp(obs`, `torch.clamp(observations`) across src/ — zero hits.
  IMPLICATION: No additional wiring needed for these consumers; the implementer can state the consumer audit is closed.

[measured] The three test files most likely to be tripped by unbounded money stay green under the wiring: tests/test_townlet/properties/test_environment_properties.py, unit/environment/test_observations.py, unit/environment/test_meters.py — 53 passed. They survive because money stays at its 0.0 initial in those fixtures, not because the assertions are safe.
  EVIDENCE: MEASURED. Ran from inside a wired git worktree with /home/john/hamlet/.venv/bin/python -m pytest ... -q --no-cov -p no:randomly -> `53 passed`. Verified the wired src was actually loaded (warning paths pointed at the worktree). The at-risk assertions are test_observations.py:137 `(meters <= 1.0).all()` in test_meters_are_included_and_normalized, properties/test_environment_properties.py:97, and unit/environment/test_meters.py:270 (vacuous — sets meters to ones and asserts <= 1.0 without stepping).
  IMPLICATION: The spec's test-churn item 10 correctly flags properties:97; test_observations.py:137 deserves the same inline comment, since its name asserts a normalization that does not exist.

[measured] PROVENANCE CAVEAT: every 'wired' number above came from wired.patch, a snapshot lifted from another agent's in-progress worktree at HEAD 30c433e3, not from the implementation that will land. It wires vectorized_env (two [meter_count] tensors + ValueError on uncovered indices), action_executor A1/A2, affordance_engine A3/A4 (required kw-only params), vtc.py B1 passive depletion and B2 threshold cascades (new required `meters` param), transition_schedule.py:217, and leaves B3 modulation at (0.0, 1.0).
  EVIDENCE: Diff read directly. Self-validating on the load-bearing leg: money surviving across ticks at 9281.98 is only possible if the passive-depletion clamp (composition='overwrite', runs every tick on every meter) is wired — a mechanism-A-only patch would re-ceiling money to 1.0 on the next tick. Only site remaining hardcoded in that build: vtc.py:2475, the modulation multiplier (correct per B3). Both scratch worktrees have been removed; `git worktree list` shows only /home/john/hamlet and `git status` is clean.
  IMPLICATION: Re-measure 9281.98 against the landed implementation before quoting it in a commit message or a pinning test. The qualitative findings do not depend on the exact literal.
