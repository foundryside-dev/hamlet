# PDR-0126 — Trigger 3 is RULED: the cap and the constants stay, and 9.43× is carried as recorded debt into unit 5

Date: 2026-08-29   Status: **accepted** (owner-ruled at the `/own-product` resume: option 4 of
`PDR-0124`, chosen from the four costed options)
Author: Claude (standing product owner)
Resolves: the **ESCALATED** half of `PDR-0124`. `PDR-0114` (the token design and its trigger 3)
is closed again on the cap question; `PDR-0123` (the measurement brought forward) stands.
Related: `PDR-0124` (the escalation and the three levers), `PDR-0114`, `PDR-0123`
Evidence: the owner's selection at the 2026-08-29 resume; the measurement is `PDR-0124`'s
(L1 `total_dims` = 1132 = 9.43× the pre-cut 120; 82% affordance block)

## Context

`PDR-0124` fired reversal trigger 3 (≥ 8× the pre-cut observation width) at 9.43×, drilled all
three width levers with data, found none that gets under 8× while keeping spec §1's
identity-by-declared-parameters property, and escalated four costed options: (1) move the cap
with reasoning written down; (2) take K=3 for 8.15× with a loud advisory; (3) reopen spec §1's
fixed-width invariant and trade cross-substrate transfer for 210 dims; (4) keep both the cap and
the constants and carry 9.43× as debt against unit 5.

## The call

**Option 4.** The 8× cap stays as written. `MAX_POSITION_RANK`, K=4 and the meter signature stay
as compiled. The 9.43× reading is **recorded debt against migration unit 5**, to be re-measured
after the pack migration moves `default_curriculum`'s census — item and `variable_element`
capacities are near zero today and will not stay that way, and the affordance block's 82% share
is a property of fourteen affordances and eight meters, not of the framework.

## Rationale

Every other option spends something real to satisfy a number: option 1 moves a trigger to fit a
measurement (the anti-pattern triggers exist to prevent); option 2 buys 1.28× for zero headroom
and a silent-drop risk at K; option 3 trades the property the padding exists to buy
(`token_type_schema_hash` is identical across a 2-D grid, a 3-D cubic grid and an aspatial
universe **today** — re-measured at HEAD 2026-08-29: `428982ef5d81dd26` on all three). Option 4
spends nothing now and keeps the trigger honest: it fired, it is on the books, and the next
census change is the moment the reading means something again.

## Consequences

- Unit 4/5 sequencing is **no longer gated** on the width question.
- `metrics.md`'s trigger-3 block reads *fired → escalated → ruled, carried*, not *unresolved*.
- Unit 5's acceptance must include the re-measurement; landing unit 5 without reading L1's
  `total_dims` against 960 is a breach of this record.

## Reversal trigger

If the unit-5 re-measurement lands **≥ 8×** on the migrated `default_curriculum`, the debt is
called: `PDR-0124`'s options 1–3 reopen as a fresh escalation with the new census attached, and
none of them may be taken silently. If it lands under 8×, trigger 3 is retired for this pack
with the reading recorded.
