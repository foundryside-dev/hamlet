# Metrics — HAMLET / Townlet        Last read: 2026-09-02 (fifth merge) · gates read at `1eb347f7` and `main@ea3648db` (`PDR-0145`, `PDR-0146`)

> **Unit 5 accepted — 2026-09-02, `PDR-0144`, `project-recovery-3@a07b889b`.**
>
> - **Config-surface coverage / declared-but-inert (input):** `observation_mode` — a stratum key
>   with zero consumers — is deleted from the DTO and all 30 packs; the inert-guard is now
>   mechanical: `test_pack_smoke.py` discovers every pack from the tree (31 pack/level cases)
>   and every live token type and scope has a committed config-in/behaviour-out exercise
>   (`effect`, item-arena `variable_element` new this unit; `agent` asserted structurally absent).
> - **Pre-release hygiene (guardrail):** nine retired trial packs and their probe scripts deleted,
>   two promoted to test-bound fixtures (`PDR-0142`); the 2026-10-06 disposition clock is
>   discharged. No new whitelist entries; the no-defaults gate is clean.
> - **Provenance integrity (guardrail):** `DIV-012` records four hash movers with a bisected
>   causing commit each, bound on all ten cpu matrix cells; runs `20260902-100802` (full cpu
>   matrix) and `20260902-110926` (`items_smoke`) are `DIVERGED_AS_REGISTERED` throughout,
>   exit 0. One false claim introduced mid-unit ("environment.yaml unedited") was caught by
>   re-review and corrected before it was trusted.
> - **Gates green (guardrail), read on `a07b889b`:** Ruff ✅, Black ✅, mypy ✅ (176 files),
>   no-defaults ✅ (715 whitelisted), compiler CLI ✅ (26 packs), `git diff --check` ✅,
>   **pytest 3,846 passed / 11 skipped** (unit tier 3,221 + 8 in 195 s; every other tier 625 + 3
>   in 382 s — run in two foreground tiers because two background runs were killed by the host).
> - **Documentation truth:** `docs/config-schemas/items.md` and `vfs-profiles.md` no longer
>   teach refused shapes (nested `spawn_effect` / `spawn_item`, refused expression exposure,
>   `time_of_day_phase`); STRATA.md, README and CLAUDE.md no longer describe `observation_mode`.
> - **Measured widths after `day_phase`:** `default_curriculum` L1/L2 compact 118 (was 115),
>   fixed boundary 4,142; `div003_cubic_partial` 152. `items_smoke` compact 474 after
>   `durability` exposure (was 435).

> **Milestone 4 accepted — 2026-09-02, `PDR-0141`, training source `project-recovery-3@9d4e942f`,
> evidence harness `@e1615648`.** Both recurrent cells were resumed from the exact pinned snapshot
> to the frozen budget and evaluated once under the frozen protocol; the repaired harness
> re-validated all 3,200 raw outcomes and one cohort identity. `summary.json`
> `all_cells_passed: true` (sha256 `5100097790007a9e`).
>
> | cell | parameters | realized / requested transitions | shortfall | raw greedy mean | verdict |
> | --- | ---: | ---: | ---: | ---: | --- |
> | feedforward / mean | 62,095 | 2,278,634 / 2,278,640 | 6 | **98.9925** | pass |
> | feedforward / attention | 128,143 | 2,278,639 / 2,278,640 | 1 | **99.0** | pass |
> | recurrent / mean | 194,191 | 2,278,638 / 2,278,640 | 2 | **97.315** | pass |
> | recurrent / attention | 260,239 | 2,278,637 / 2,278,640 | 3 | **99.0** | pass |
>
> Floor 79.19466666666668; every mean is the exact arithmetic mean of 800 raw agent outcomes under
> 100 episodes, eight agents, eval seed 12345, cap 1000. The **unit-4 regression floor** input
> metric reads **4 of 4 cells pass**. The recurrent rows each carry two training attempts, the
> second `resume: true`. Durable copies of `summary.json`, every `greedy_eval.json`, `meta.json`,
> `curves.csv` and `transitions.csv` live under `docs/product/baselines/2026-09-m4-token-regression/`.
>
> **Evidence guardrail (`PDR-0140`):** the three `PDR-0138` defects are closed at `e1615648`.
> `transitions.csv` is now derived only from the checkpoint-persisted `completed_live_agent_steps`
> (digest-verified, monotone, ending at the realized count) and `summarize` refuses a cell
> without it; `curves.csv` names the database column `batch_episode_steps`; the runner records
> `failed` / `completed` / `interrupted`. The overstatement of the retired TensorBoard extractor
> was measured at 402 → 93,478 transitions across feedforward/mean. Engine root cause
> (`env.step_counts` unmasked for dead agents) filed as `hamlet-d6fc84d147`, P1 — not an M4 fix
> because it feeds rewards and curriculum under the oracle.
>
> **Gates green** (guardrail, read on `project-recovery-3@e1615648`, the `PDR-0127` local set):
> Ruff ✅, Black ✅ (557 files), mypy ✅ (176 source files), no-defaults ✅ (716 whitelisted),
> compiler CLI validation ✅ (every shipped pack and level compiles), `git diff --check` ✅,
> **3,837 passed / 11 skipped in 1,166.41 s**. Tool health per `PDR-0139` at resume: both doctors
> clean, filigree probe 200, index fresh.
>
> **Pre-release hygiene:** the `9d4e942f` snapshot worktree was removed after use; no new
> dead code. **Documentation truth:** unchanged (`CLAUDE.md:116` observation still pending).

> **Milestone 4 partial engineering reading — 2026-09-01, `PDR-0138`, implementation
> `project-recovery-3@9d4e942f`.** This is a pause checkpoint, not M4 acceptance.
>
> | cell | parameters | realized / requested transitions | shortfall | raw greedy mean | state |
> | --- | ---: | ---: | ---: | ---: | --- |
> | feedforward / mean | 62,095 | 2,278,634 / 2,278,640 | 6 | **98.9925** | cell pass |
> | feedforward / attention | 128,143 | 2,278,639 / 2,278,640 | 1 | **99.0** | cell pass |
> | recurrent / mean | 194,191 | 1,181,395 / 2,278,640 | 1,097,245 | not run | paused, resumable |
> | recurrent / attention | 260,239 | 1,204,116 / 2,278,640 | 1,074,524 | not run | paused, resumable |
>
> Both feedforward readings are the exact arithmetic mean of 800 raw agent outcomes under 100
> episodes, eight agents, eval seed 12345 and cap 1000. They exceed the fixed
> 79.19466666666668 floor. The recurrent rows have no evaluation artifact and make no score claim.
> All four training rows have one attempt; a later recurrent continuation will truthfully append
> `resume=true` rather than overwrite this pause.
>
> **Implementation gate:** 3,822 passed / 11 skipped in 1,235.60 seconds; Ruff and Black (570
> files), mypy (176 source files), no-defaults (176 source files), compiler CLI validation and
> `git diff --check` passed. The source commit is pushed and the working tree was clean at launch.
>
> **Evidence guardrail:** `completed_live_agent_steps` persisted in checkpoint/metadata is the
> budget authority. The inherited TensorBoard curve totals overstate it (2,372,112 versus
> 2,278,634 for feedforward/mean; 2,396,920 versus 2,278,639 for feedforward/attention) and are
> diagnostic only. The direct-script curve import and misleading early-stop database status are
> open M4 defects, not deferred debt. No qualification process remains active at this checkpoint.

> **Resume reading — 2026-09-02, `PDR-0139`, branch tip `project-recovery-3@9318d86f`.** No
> milestone moved. Readings taken:
>
> - **Gates green:** branch CI on the `PDR-0138` checkpoint commit `439e0cd1` — Lint ✅, Tests ✅
>   (32m46s), Config Validation ✅ (run `33423733181`/`029`/`056`); scheduled Full Test Suite on
>   `main@9efadd3c` ✅ 2026-09-01 (`33500458822`). Three commits pushed since (`dc92e8ba`,
>   `b2a10f3e`, `9318d86f`); none touches `src/townlet/` or configs, so the `PDR-0127` local gate
>   set was not re-run for them.
> - **M4 evidence integrity (`PDR-0138`):** both paused recurrent checkpoints present on disk with
>   sha256 sidecars, no later checkpoint in either cell, all four `meta.json` pin
>   `git_sha 9d4e942f` with `resume: false` and the feedforward pair `budget_compliant: true`.
>   Both GPUs idle. Nothing resumed since the pause.
> - **Documentation truth:** +1 newly confirmed false claim, **unfixed**: `CLAUDE.md:116` still
>   says no workflow has ever run on `project-recovery`; CI has run on every push since
>   `PDR-0130`. Filed as observation for the gated rewrite. Count is now 1 known-open.
> - **Weft tool federation (new, guardrail-adjacent, under Gates green):** loomweave→filigree
>   link was DEAD (port 8766, nothing listening) since 2026-08-15 while both doctors reported
>   healthy; fixed to 8749 and probed 200. Standing rule from `PDR-0139`: health = doctor clean
>   **and** probe 200 **and** index fresh.
> - **Authority-trial disposition clock:** 2026-10-06, 34 days out, unchanged (`PDR-0111`).


> **Milestone 3 engineering reading — 2026-08-31, `PDR-0136`, implementation
> `project-recovery-3@d554fb7f`.** The current replay ABI is compact dynamic state; the fixed
> projection is compiler context attached one token type at a time inside `TokenSetQNetwork`.
> There is no complete fixed-observation encoder, allocation, reconstruction callable, old-format
> reader or compatibility path.
>
> | substrate | rank | compact floats | fixed projection floats |
> | --- | ---: | ---: | ---: |
> | Grid2D / `L1_full_observability` | 2 | **115** | **4,090** |
> | Grid3D / `L2_partial_observability` | 3 | **149** | **4,090** |
> | aspatial / `L0` | 0 | **19** | **394** |
>
> The ranks share one `token_type_schema_hash` and carry rank-specific `layout_hash` values.
> Aspatial is true rank zero. At capacity 100,000 the L1 observation/next-observation pair is
> exactly **92,000,000 bytes** of float32 storage. The earlier 118-float value is the one-scalar
> target (94,400,000 bytes), not the current census. The historical 1,132/4,090 readings below
> remain evidence for why the representation changed; they are not current replay widths.
>
> **Engineering matrix:** batch 256 forward/backward/update passes for feedforward, dueling,
> token-set mean, token-set attention and RND. A real `[256,4,115]` recurrent BPTT run threads
> hidden state, masks terminal boundaries, bootstraps from the final next observation and changes
> LSTM parameters. Standard, prioritized and sequential replay push/store, sample, exact-current
> round-trip and refuse the immediately previous version. Grid2D, Grid3D and aspatial compile,
> artifact-round-trip, reset/step, preserve visibility/presence and reconstruct exact per-type
> fixed rows at the network boundary.
>
> **Artifact versions:** compiled artifact `1.26`; projected schema `token-1.1`; transport
> `compact-1`; outer checkpoint `5`; population `4`; standard replay `4`; prioritized replay `4`;
> sequential replay `5`. Version type/value, replay kind, key/schema, tensor/config and nested
> state are exact. Failed restore leaves network, optimizer, scheduler, counters, replay and
> exploration byte-identical.
>
> **Pre-release hygiene:** the non-buildable public `set_encoder` architecture,
> `SetEncoderQNetwork`, its production-dead dynamic-need helpers and tests are deleted. The
> serializer that omitted absent `token_set` solely to preserve pre-cut `brain_hash` values is
> deleted. Current dumps and artifacts have one exact schema. The whole-observation ABI guard
> scans production references and traces compile → reset/step → replay → flat/token forward;
> it permits only per-type fixed assembly.
>
> **Acceptance reading:** `uv run pytest` = **3,824 passed / 11 skipped / 84% coverage** in
> 948.63 seconds on the final stable tree. Ruff passed; Black checked 568 files; mypy checked 176
> source files; no-defaults checked 176 source files with 725 existing whitelist entries and no
> new violations; compiler CLI validation and `git diff --check` passed. Import provenance was
> `/home/john/hamlet/src/townlet/__init__.py`.
>
> **Clean pushed-SHA benchmark, run 1 (verbatim JSON):**
>
> ```json
> {
>   "agents": 4,
>   "commit": "d554fb7fdda27ea5f6d2ca516dbc9bd0ab7b981c",
>   "config": "configs/default_curriculum",
>   "cpu": "x86_64",
>   "denominator": 1079432.0,
>   "dirty": false,
>   "encoding_ratio": 0.1618647585026199,
>   "iterations": 200,
>   "level": "L1_full_observability",
>   "median_observation_ns": 174722.0,
>   "median_step_ns": 1079432.0,
>   "numerator": 174722.0,
>   "platform": "Linux-6.8.0-137-generic-x86_64-with-glibc2.39",
>   "python_version": "3.13.1",
>   "seed": 1337,
>   "thread_count": 1,
>   "torch_version": "2.11.0+cu130",
>   "warmup": 20
> }
> ```
>
> **Clean pushed-SHA benchmark, run 2 (verbatim JSON):**
>
> ```json
> {
>   "agents": 4,
>   "commit": "d554fb7fdda27ea5f6d2ca516dbc9bd0ab7b981c",
>   "config": "configs/default_curriculum",
>   "cpu": "x86_64",
>   "denominator": 1016339.0,
>   "dirty": false,
>   "encoding_ratio": 0.16272129673268468,
>   "iterations": 200,
>   "level": "L1_full_observability",
>   "median_observation_ns": 165380.0,
>   "median_step_ns": 1016339.0,
>   "numerator": 165380.0,
>   "platform": "Linux-6.8.0-137-generic-x86_64-with-glibc2.39",
>   "python_version": "3.13.1",
>   "seed": 1337,
>   "thread_count": 1,
>   "torch_version": "2.11.0+cu130",
>   "warmup": 20
> }
> ```
>
> Accepted performance reading: `max(run1, run2) = 0.16272129673268468 < 0.25`. This measures
> observation encoding as a share of `env.step`; it is not the milestone-4 raw 79.19 engineering result.

> **Milestone 2 engineering reading — 2026-08-31, `PDR-0134`:** meter `range_type` is no longer
> declared-but-inert. Its current token surface admits exactly four bounded transformations:
> `minmax` with `clip: true`, `log_scaled` with `clip: true`, `cyclical_sin_cos`, and `binary`.
> The meter surface deletes `none`, `zscore`, `one_hot`, `rank_scaled` and `masked_value`; it does
> not alias, translate or fall back to them. All 39 current config declarations (38 pack
> `environment.yaml` files plus the complete reference) are updated. Frozen oracle fixtures retain
> old declarations only as historical evidence. Compiled schema 1.25 and token encoding
> `token-1.1` carry the new contract.
>
> Each level's compiled `TokenSpec` carries meter static signatures and the complete executable
> affordance/effect identity ruled in `PDR-0135`. The selected environment, encoder, population and
> token network consume that level's spec with no primary-level fallback. Artifact loading
> reconstructs compiler-owned bindings rather than trusting a self-consistent stored hash chain.
>
> The former **1,580-float** line was an intermediate meter-only reading. The 34-positive-pack
> census found two affordances with five executable writes, so the exact fixed summary is K=5.
> After the exposed-initializer correction, the current full L1 serialization is **4,090 floats**:
> **3,272,000,000 bytes = 3,120.4 MiB** per 100,000 float32 observation pairs. The current
> `variable_element` count is **0**: expression-backed exposure is refused until milestone 3 static
> context can encode executable initializer identity, while the time variable remains live but
> unexposed. Milestone 3 owns the compact dynamic-state split and replay measurement; milestone 2
> does not claim that downstream result early.
>
> **Acceptance reading:** 3,675 passed / 11 skipped / 84% coverage in the full default suite;
> Ruff, Black (565 files), mypy (175 source files), no-defaults, compiler-pack validation and diff
> integrity green. All current positive packs validate and the three negative VFS fixtures refuse
> exactly as designed. Milestone 2 is closed at `project-recovery-3@c6c6b524`; milestone 3 is
> atomically claimed and `in_progress`. Its dynamic-state measurements remain prospective.

> **Milestone 1 engineering reading — 2026-08-31, `PDR-0133`:** one declared-but-inert config
> field and all of its residual runtime branches are deleted. Current DTOs reject the old key;
> positions are bounded to `[0,1]`, egocentric deltas to `[-1,1]`, and the active code/config
> census has zero references outside the rejection regression. The vacuous `div003_scaled`
> evidence cell is replaced by `boundary_wrap`. Local gates are green: Ruff, Black, mypy (173
> source files), no-defaults, shipped-pack validation, and the full suite at 3,307 passed / 11
> skipped / 84% coverage. No replay-width or encoding-share metric moved in this milestone.

> **Plan checkpoint — 2026-08-31, `PDR-0132`:** no engineering metric was re-read. The accepted
> constraints below are unchanged; they are now assigned to five separately checkpointed
> milestones, beginning with canonical bounded positions and meter normalization before the
> compact-replay reading is taken.

> **Current engineering reading — 2026-08-31, `PDR-0131`:** the 9.43× result is no longer
> carried to unit 5. It is assigned to compact replay engineering before unit 4; the concrete
> constraints are recorded in the trigger block below.

> **Partly measured.** The bootstrap seed was superseded the same session: the maturity assessment
> (`wf_4ca82820-274`) executed compiles, `env.reset()`, and stepping, and **Trial 001** was run by
> hand. Rows now carry a mix of *executed*, *source-traced*, and `UNMEASURED` readings — each says
> which. Targets marked `<owner sets>` still need a real number and date before they can gate
> acceptance or fire a PDR reversal trigger. A target without a number and a date is not
> falsifiable and must be rejected.
>
> **Scale note:** `src/townlet/` is **45,274 LOC** across 14 packages (assessment §7). Earlier
> figures in this session said ~37k — a ~20% underestimate. No conclusion depends on it.

> ### 🔴 READING THAT CROSSED A REVERSAL TRIGGER — 2026-08-26, unit-3 Task 11
>
> **`PDR-0114` trigger 3 was FIRED at 9.43× and is re-ruled as an engineering layout problem
> (`PDR-0131`, superseding `PDR-0126`).** The network-facing token schema stays fixed for
> cross-substrate transfer, but replay will no longer store immutable descriptors or rank padding
> in every transition. The raw 8× proxy is replaced by a dynamic replay target of ≤120 floats,
> ≤96,000,000 bytes for a 100,000-transition float32 observation pair, batch-size viability and
> the existing 25% encoding guardrail. The original measurement,
> retained as taken: measured on the compiled post-cut artifact, which is the
> historical pre-milestone-2 measurement `PDR-0123` brought forward: `configs/default_curriculum` @
> `L1_full_observability`, **`token_spec.total_dims` = 1132 = 9.43×** the pre-cut allocated
> 120. (Not the 1080 / 9.0× read at design time — the L3 temporal declaration added one
> `variable_element` token, 52 dims, uniformly on all five levels.) Census: `self` 1×18,
> `meter` 8×12, **`affordance` 14×66 = 924, i.e. 82% of the total**, `item` 2×21,
> `variable_element` 1×52, `agent` and `effect` capacity 0.
>
> The three original pruning levers `PDR-0123` named were drilled with data and **none is
> sufficient on its own**: the
> meter signature is refuted as dead width (7 of 8 declared parameters vary; all 8 meters
> have distinct signatures), rank-adaptive position padding would save 210 dims → 7.68× but
> breaks spec §1's fixed-per-type width — measured, `token_type_schema_hash` is identical
> across 2-D grid, 3-D cubic and aspatial universes, which is exactly what the padding buys —
> and K=4→3 is right-sized by the fleet's declared data (175 affordance declarations, max 3
> deltas, nothing above) but only reaches 8.15×.
>
> That analysis assumed replay serialization and network-facing input must be the same object.
> `PDR-0131` removes that assumption: immutable descriptors live once in compiled context,
> actual-rank dynamic state lives in replay, and fixed-rank reconstruction happens at the network
> boundary. The owner escalation is resolved; `PDR-0126` is superseded.
>
> **Adjacent reading, trigger NOT fired:** obs encode is **16.8–19.5% of `env.step`**
> post-cut (measured CPU, 4 agents, 200 timed steps, at 1132 and 1766 dims), under the 25%
> line — and measured against a step no longer padded by clone traffic in the observation
> path (`hamlet-c7084169f7`: clones per `_get_observations` 20 → 0).

## North-star

The vision says the product is **trivial authoring**: idea → running DRL gym without Python. The
north-star must therefore measure *authoring cost*, not runtime quality. Runtime quality is a
guardrail — a fast, correct substrate nobody can author against has failed at the thing it is for.

| Metric | Target (falsifiable) | Current | Read on | Trend |
|--------|----------------------|---------|---------|-------|
| **Zero-Python authoring rate (world)** — of N representative new mechanic/interaction ideas, the fraction expressible as a config pack alone, with **zero lines changed under `src/townlet/`** | **≥ 80% — 8 of 9 — by 2026-10-06** (7 of 9 is 77.8% and misses). Set 2026-08-17 by the owner **before the corpus was written**, which is what makes it a target rather than a description of the answer. Per `PDR-0078` this is the **standing bar on the metric**, NOT this bet's pass/fail. | **⛔→📁 2026-08-22 (forty-first checkpoint): THIS ROW IS RETIRED AS RECORD (`PDR-0111`, owner-ruled fork (c)) — TERMINAL FOR THIS CORPUS.** Nothing below ever publishes as a rate; the readings stand as the record of what was measured and the by-catch stands as the product signal. Reading the metric again requires a redesigned instrument over a revised corpus (`PDR-0111` consequence 5). Quoting any number from this row as a published reading is a breach of `PDR-0111`. Prior state, retained: **2026-08-22 (fortieth checkpoint): the recommended evidence audit is DONE — Branch A, SOUND (`PDR-0110`).** Run 2's B-F2 PASS stands; the rejection below stands as genuine search variance; the row remains fully unpublishable; the instrument fork (amend / accept-headline-only / retire) is escalated to the owner. ⛔ **UNPUBLISHABLE AS OF 2026-08-20 (`PDR-0106`) — READ THIS BEFORE QUOTING ANY NUMBER IN THIS ROW, INCLUDING THE "6 of 9" AND THE SPLIT.** The instrument is **NOT ACCEPTED**. Criterion 3 required 2 of 9 trials to be re-run blind and reproduce their verdicts; idea O reproduced (the cheap one, by its own comparison's admission) and **idea B — the discriminating one — did NOT**. §7's reject branch has fired on substantive classification disagreement across three of five mapped core pairs, two of which invert. `PDR-0095`'s reversal trigger (*"no north-star reading publishes until idea B's blind re-run agrees"*) is **FIRED**. Nothing in this row publishes — not the rate, not the denominator, not the ABSENT/INERT/BLOCKED split, and not the ≥80% arithmetic — until the owner rules on what happens to the instrument. The recommended first step is a commissioned evidence audit of run 2's `tensorNd` result on the `PDR-0098` pattern, because it forks everything: if sound, run 1's central finding is wrong and four filed tickets need re-reading; if unsound, the largest disagreement collapses and the rejection is re-adjudicated on the remaining two. **The readings below are retained as the record of what was measured, NOT as a publishable result.** **CONSTRUCT PREAMBLE (`PDR-0086`, 2026-08-18 — read before quoting any number from this row): this row measures EXPERT-CEILING EXPRESSIBILITY — an expert executor with full engine-source access finding ANY declarative surface, at a pinned commit, within one session. It is an upper bound on the vision's novice-authorability claim, not a reading of it: FAILs here are strong evidence (expert-can't ⇒ novice-can't); PASSes are weak evidence for the novice claim (three of four passes to date rode a second surface found by source archaeology). The novice-facing row below begins prospectively at trial five.** ⚠️ **TARGET NOW ARITHMETICALLY UNREACHABLE FOR THIS CORPUS (2026-08-19, thirty-fifth checkpoint): with 2 of 9 ideas FAILED (B, K), at most 7 of 9 can pass = 77.8%, and the target is 8 of 9 = ≥80%. The bar is missed for this corpus no matter what D, E and J do.** Per `PDR-0078` this is a **finding about the substrate, not a failed bet** — the ≥80% bar is the standing bar on the metric, and acceptance was deliberately placed on the instrument precisely so a low reading could not be gamed by picking an easy corpus. The remaining three trials still run: they determine *how far* below the bar the substrate sits and *which* axes fail, which is the actual product signal. **No reading publishes until the ninth trial and both blind re-runs (O + B, `PDR-0089`) are recorded.** **2026-08-19 (thirty-fifth checkpoint): 6 of 9 SETTLED — 4 PASS, 2 FAIL — 3 pending (D, E, J; denominator 9, no voids). Idea split: **0 ABSENT / 0 INERT / 2 BLOCKED (B, K)** — the protocol gap `PDR-0092` left open is CLOSED, owner-ruled 2026-08-20 and pre-registered as protocol **A.6.1** before the first blind re-run dispatched: the idea's bucket is the **most severe class among its failing facets, INERT > BLOCKED > ABSENT**, a facet carrying two classes contributing its most severe one. K therefore counts **BLOCKED** on F7 (BLOCKED on the named surface, ABSENT for the capability), not ABSENT on F1. The rule is deliberately mechanical so a blind re-run derives the same bucket from the same facet table; the decisive-facet alternative (which would have bucketed K ABSENT) was heard and rejected because it makes bucketing an executor judgment. The INERT escalation counter is unaffected at 0 either way, which is what made the ruling takeable without touching the escalation clause. INERT SURFACES encountered as by-catch: **4 in 6 trials** (new: item `on_drop` is parsed, compiled and dispatchable but invoked by no call site — `hamlet-628e202bf7`).** Trial K (responding to the universe — the world gets cold; owner-supplied, owner-chosen at resume as the only untouched axis) — headline **FAIL** at pin `3434b2fa` (record `docs/product/trials/0001/K-20260819.md`, `PDR-0092`): F2/F3/F4/F5/F6/F8 PASS, **F1 (ABSENT), F7 (BLOCKED+ABSENT) and F9 FAIL**. The pre-registered prediction (PARTIAL, both responses doubtful) **SPLITS**: the equipped-item modifier chain — "a modifier chain I have not seen a path for" — **works** first-reach with exact arithmetic (falsified); `zone` scope **fails, and worse than predicted** (confirmed) — it validates and compiles clean, then hard-crashes at env construction, and so do `group` and `message`, because `num_zones`/`num_groups`/`num_message_slots` are registry kwargs defaulting to 0 that `vectorized_env.py:621` never passes and no YAML sets (`hamlet-9e1ae3b7a2`, P1); and the *pressure* is **not** straightforwardly authorable (falsified in an unexpected direction) — its rule is declarative and exact but its **ignition** is not, so over 12 all-WAIT ticks the world never moved. **The corpus's own summary line is inverted: an author can express two of the three answers, and cannot make the problem happen on its own.** Second P1: effects cannot read position or time — `executor.py:642-726` threads neither `agent_positions` nor `temporal`, so the only engine-ticked surface a pack can declare is blind to both (`hamlet-a737e444c0`), the second confirmed instance of the `hamlet-1b9af9088c` pattern. **Structural caveat recorded on the record and repeated here: F2/F3/F4/F8 were all probed downstream of the cheat-#2 ignition F1 rules out, so four of the six passes are conditional on an ignition the idea forbids — "seven of nine facets pass" would materially overstate this substrate.** Coverage finding: the suite ran 3281/16/0 green while three declared scopes hard-crash any pack using them, possible only because no test instantiates them — the gate is green over a hole, which bears on the strangler bet's exit condition 3. Discovery paths (A.3): every winning surface example- or docs-reachable, first-reached surface worked each time; both failures needed source reading only to establish they are engine-wide. Leg-(c): reward assertion exact; **double-reset FAIL** (third pack to reproduce `hamlet-d76684f549`, first to isolate that global VFS variables do not reset while meters do); **obs-bounds FAIL** (42 out-of-`[0,1]` readings — raw global counters at offsets 86/87, widening `hamlet-bf42ac60b5` from item slots to global-profile variables); random-policy smoke clean. Prior: **2026-08-19 (thirty-fourth checkpoint): 5 of 9 SETTLED — 4 PASS, 1 FAIL — 4 pending (D, E, J, K; denominator 9, no voids). Idea split 0 ABSENT / 0 INERT / 1 BLOCKED; INERT SURFACES encountered as by-catch: 3 in 5 trials (new: `max_items_per_agent: 0` silently nullifies the whole items catalog at load — `hamlet-45f501e15b`).** Trial B (the spreading organism, owner-supplied, owner-chosen at resume — the FIRST trial under protocol Appendix A: countersigned facets adopted verbatim, search pre-registered and CONFIRMED, discovery paths annotated, leg-(c) column carried) — headline **FAIL** at pin `1ef1d950` (record `docs/product/trials/0001/B-20260819.md`, `PDR-0087`): facets 1/4/5 (5-D `gridnd`; declared 5-D fixed-position warehouse with membership-gated absorption, exact to the declared amounts; approach gradient present, reward moves by its declared component) PASS — facets 2/3 (mass-not-point; rooted outward growth) **BLOCKED**: every declarative route to a durable organism-occupied cell is refused loudly — `spawn_item` is unreachable end-to-end from config (coordinates refused at parse by both DTOs, string strategies refused at runtime because no production pipeline passes agent positions; `hamlet-1b9af9088c`, P1), no per-cell/spatial-field scope exists (`hamlet-3f97369711`), agent spawn positions are undeclarable (`hamlet-4857e6824b`), N-D static item placement is refused at parse (`hamlet-6c49488b22`). The pre-registered prediction (**FAIL, or a heavy PARTIAL via a group-of-agents workaround**) is **CONFIRMED** — the corpus's first confirmed FAIL, first failed idea, and first BLOCKED classification; the workaround was reachable exactly as pre-named and insufficient exactly as the countersigner pre-ruled (the probe shows the occupied set shrinking at tick 1). Diagnostics: B1 localizes the failure as substrate-independent (same refusal on 2-D); B3 shows exclusive permanent membership, per-organism observation-encoded extents, and shared-source absorption all work — contested CELL occupancy inherits the gap. Discovery paths (A.3): every PASSING facet's winning surface was docs-reachable with the first-reached surface working — the first counter-evidence to the "0 of 4 docs-first" prior. Leg-(c) first readings: 6 of 7 probes clean; **double-reset FAIL** (3 effect instances survive `env.reset()`, stale global in the first post-reset observation — reproduced in a second independent pack, comment 169 on `hamlet-d76684f549`). INERT escalation counter (threshold 3): **0** — the BLOCKED idea does not move it. 4 trials + 2 blind re-runs remain before any reading publishes. Prior: **2026-08-18 (thirty-second checkpoint, restated per Appendix A.7): 4 of 9 SETTLED, 5 pending (denominator 9, no voids). Idea split 0 ABSENT / 0 INERT / 0 BLOCKED; INERT SURFACES encountered as by-catch: 2 in 4 trials (`recovery.natural` — `hamlet-d45331a367`; effect `scope: global` — `hamlet-4cd664a955`) — published beside the idea counter per A.7, because the idea-level counter is structurally suppressed by executor workaround skill (the executor hits an INERT surface, finds a second surface, and the idea passes).** Trial O (adversarial bidding, owner-supplied) — headline **PASS** on all six pre-committed facets, both legs, at pin `a3318624` (record `docs/product/trials/0001/O-20260818.md`, commit `2dcc2273`, `PDR-0085`): per-agent bids from affordance `on_start`; simultaneous collection over a declared 3-tick window; clearing by `for_each: all_agents` running max in an engine-ticked effect (swap-tested — the outcome follows the amounts); award and charge exact; no-bid guard holds; all auction state including the global `highest_bid` observation-encoded. Pack compiled and probe passed on the FIRST attempt of each. The pre-registered prediction (**FAIL, ABSENT**) is **FALSIFIED** — the fourth of four, and the first *structural* prediction to fall (L/F/M were surface-choice falsifications): the effects command vocabulary IS a declarative clearing surface. The `PDR-0082` shape recurred in half — the first-reached surface (`scope: global`) is genuinely unreachable (every spawn path hardcodes agent scope, `hamlet-4cd664a955`; no reset-time spawn exists, `hamlet-77e4f8b3e3`) and a second declared surface expresses the whole idea. INERT escalation counter (threshold 3): **0** — by-catch defects do not count ideas. 5 trials (B, D, E, J, K — three multi-agent) + 2 blind re-runs remain before any reading publishes. **A three-lens methodology review (construct validity, RL practice, statistical inference) was dispatched this session at the owner's direction; per `PDR-0085`'s reversal trigger, no reading publishes over an open confirmed validity defect.** Prior: **2026-08-18 (thirty-first checkpoint) — THIRD READING: 3 of 3 trials run (denominator 9, no voids), split 0 ABSENT / 0 INERT / 0 BLOCKED.** Trial M (combo actions, agent-drafted from the owner's concept) — headline **PASS** on all five pre-committed facets, both legs, at pin `a519f312` (record `docs/product/trials/0001/M-20260818.md`, commit `790dcb7e`, `PDR-0084`): three operations and two per-agent event traces compile from config alone; A sets its trace while idle ticks move nothing; B's whole effect is gated on the A-trace (nothing leaks pre-A); the chain composes to depth 3; the encoded observation tracks both traces including the mid-chain state. The pre-registered prediction (PARTIAL, possibly INERT) is **FALSIFIED** — the third falsification in the `PDR-0082` shape: the first-reached surface (custom actions) is genuinely incapable (`hamlet-f1dec55b9d` — no effect, no precondition declarable, `reads`/`writes` reachable from no YAML), and the affordance surface expresses the whole idea. **The AGGREGATE pre-registration ("1, possibly 2, pass; INERT 1–2") is now formally FALSIFIED: 3 passes of 3 run, INERT count 0 (threshold 3), six trials remaining** — noting the passes so far are all single-agent territory and four of the six remaining are multi-agent. 6 trials + 2 blind re-runs remain before any reading publishes. Prior: **2026-08-18 (thirtieth checkpoint) — SECOND READING: 2 of 2 trials run (denominator 9, no voids), split 0 ABSENT / 0 INERT / 0 BLOCKED.** Trial F (tool durability, agent-drafted) — headline **PASS** on all four pre-committed facets, both legs, at pin `e5f7dd7a` (record `docs/product/trials/0001/F-20260818.md`, commit `fb56fbbd`, `PDR-0083`): the wear state compiles from `vfs_profiles.yaml` alone, USE decrements it 3→2→1→0 while idle ticks leave it untouched, at zero the guarded effect stops firing, and `obs_item_slots` tracks the value at its compiled offset. The pre-registered prediction (PASS) is **CONFIRMED** — the corpus's first confirmed prediction (1 confirmed, 1 falsified, of 2 run). **The aggregate pre-registration ("1, possibly 2, pass; INERT 1–2") is now at its ceiling: any further pass falsifies it.** INERT escalation counter (threshold 3): **0**. 7 trials + 2 blind re-runs remain before any reading publishes. Prior: **2026-08-18 (later) — FIRST READING: 1 of 1 trials run (denominator 9, no voids), split 0 ABSENT / 0 INERT / 0 BLOCKED.** Trial L (cooldown management, owner-supplied) — headline **PASS** on all four pre-committed facets, both legs, at pin `fb8c6148` (record `docs/product/trials/0001/L-20260818.md`, commit `484976d3`, `PDR-0082`). The pre-registered prediction (PARTIAL, landing INERT) is **FALSIFIED**: a live INERT surface was hit (`recovery.natural`, filed `hamlet-d45331a367`) but a second declared surface expresses the same facet, so the idea passes. INERT escalation counter (threshold 3): **0** — by-catch defects do not count ideas. 8 trials + 2 blind re-runs remain before any reading publishes. Prior: **2026-08-18 — THE INSTRUMENT IS WHOLE: the trial protocol is ACTIVE (`PDR-0081`, `docs/product/prds/0001-trial-protocol.md`), so the metric is now not just readable in principle but executable — any executor can run a trial and a second can re-run it blind. Corpus hash re-verified byte-identical this session. Still `UNREAD` as a rate — no trial has run; L is the designated first.** Prior: **2026-08-17 — THE METRIC IS NOW READABLE IN PRINCIPLE: it has a denominator, a scoring unit, and a frozen corpus. Still `UNREAD` as a rate — no trial in this corpus has run.** The instrument (`PDR-0077`, PRD-0001): 15 ideas frozen at SHA256 `48840cc3…8de935d9` — 7 owner-supplied, 3 owner-riffed, 5 agent-drafted, each with a named external source and a **pre-registered prediction**; trial set **N=9 (B, D, E, F, J, K, L, M, O)** **drawn** mechanically and stratified, seeded by the corpus's own hash (`PDR-0080`). Scoring: **binary headline per idea** (this metric) plus facet detail underneath — which repairs the defect that made the two prior readings incomparable. Every miss classifies **ABSENT / INERT / BLOCKED** (`PDR-0079`); readings report the split, because 3-of-9-all-ABSENT is a build list and 3-of-9-all-INERT is a substrate that lies to authors. **Agent's pre-registered prediction over the drawn nine: 1, possibly 2, pass; INERT count 1–2.** The draw excluded both ideas the agent predicted would pass most easily. Prior: **2026-08-15 — the Trial 002 failure is REPAIRED, and the repair was verified by execution rather than by schema.** Trial 002 (`PDR-0051`) measured the owner's two money designs as 3-for-4 authorable; the failing leg was normalization — a meter could not declare *how* it is observed, so 8 of 9 VFS kinds were unreachable from any pack (`hamlet-3d3039f340`). Closed at `2535a306`/`43b4f33e`: `range_type` is now the meter's complete parameterized type, and an adversarial review **executed all nine members end to end on two levels — 9/9 compile, 9/9 observe, 9/9 step**. Before this session the tree had never exercised more than two. Money at 1000 under `log_scaled` now observes **0.500000** where it observed **0.000999** — the ticket's headline number, met exactly. **Still not a rate**: the corpus is undefined, so this is a second existence proof and a repaired one, not a fraction. Prior: **1 of 1** — first trial passed (see below). | 2026-08-19 | withheld until the denominator is exhausted (A.7) |

### Prospective rows opened by `PDR-0086` (2026-08-18) — both `UNREAD`, neither moves the headline for this corpus

| Metric | Target (falsifiable) | Current | Read on | Trend |
|--------|----------------------|---------|---------|-------|
| **Zero-Python authoring rate (novice-facing, discovery-path annotated)** — same trials, scored by HOW the winning surface was found (docs-reachable / error-message-guided / source-reading-required) and whether the first-reached surface worked (Appendix A.3) | `<owner sets at the next corpus revision>` — no bar until the annotation has a full corpus behind it | **CONSTRUCT CAVEAT (owner-ruled 2026-08-20, protocol B.6 — read before quoting any number from this row): DISCOVERY PATH IS A PROPERTY OF THE (EXECUTOR, PACK-DESIGN) PAIR, NOT OF THE FACET.** The first blind re-run measured this directly (`docs/product/trials/0001/O-comparison-20260820.md`, SD-7): the *same* observability facet reads **docs-reachable, first reach worked** for run 1 and **source-reading-required** for run 2 — same capability, same engine, same commit — because a pack-design choice made three facets earlier (bars vs agent-profile VFS variables) decided which pipeline had to carry the observation. Two competent executors produced different readings of the same facet at the same pin. Consequences, all of which bind any future reading: (1) this row is a **lower bound on discoverability for the route the executor happened to take**, never a property of the facet or of the substrate; (2) the retroactive L/F/M/O derivation A.3 calls for is **kept but read under the same caveat** — the owner ruled caveat-and-continue over dropping the row, the same move `PDR-0086` made for the north-star; (3) a number from this row can only be compared against another number produced under the *same* pack-design choices, which for a nine-idea corpus means it is not yet comparable to anything. A second, structural confound is already recorded below (example-reachability partly measures the growing trial-pack corpus rather than the shipped docs) and compounds this one. **SECOND READING (Trial K, 2026-08-19, live annotation per A.3): every PASSING facet's winning surface was example- or docs-reachable, and the first-reached surface worked every time.** F2/F3 rode a shipped trial pack (`trial_o_bidding`) though the effects command vocabulary itself needed `effects_config.py` — **source-reading-required**; F4 **docs-reachable**; F5/F6 **example-reachable** (`trial_f_durability` for both the item-profile shape and level-scoped placement); F8 **docs-reachable**. Both FAILING facets needed source reading only to establish the refusals are engine-wide, not local mistakes. **Two readings now run the same direction, against the "0 of 4 docs-first" prior: passing surfaces have been reachable without source archaeology in both trials since the annotation opened.** Note the countervailing structure — K's passes are concentrated in *already-proven* surface families (effects, items, bars), so example-reachability partly measures the growing trial-pack corpus rather than the shipped docs. Prior: **FIRST READING (Trial B, 2026-08-19, live annotation per A.3): every PASSING facet's winning surface was docs-reachable (shipped pack examples + config-schema docs) and the first-reached surface worked each time; the failing facets' refusals required source reading only to establish they are engine-wide.** Retro-derivation for L/F/M/O from their authoring logs still owed and cheap; the "0 of 4 docs-first" prior now has its first counter-instance. | 2026-08-18 | — |
| **Trains-without-incident (leg c)** — per trial pack: reward delta asserted at a declared-component tick; double-reset returns mechanic state to declared initial; obs components within declared bounds; ~5 random-policy episodes complete with finite, non-constant rewards (Appendix A.4) | recorded per trial from trial five; gates only from the next corpus revision | **SECOND READING (Trial K, 2026-08-19): 5 of 7 clean — two FAILs, both reproductions.** Reward assertion **exact** (reward delta `-0.050000` against comfort delta `-0.100000` at declared scale 0.5); N≥3 **PASS** (4 agents, `for_each: all_agents` resolves identically); random-policy smoke **PASS** (5 episodes, finite, non-constant, one terminated, no exceptions); boundary cases **mixed** (equality exact, over-mitigation floors correctly, unequip residual exact on the direct contrast but FAILS on the item verb — traced to the INERT `on_drop`, `hamlet-628e202bf7`; four zone-dependent cases **deferred, not waived**). **double-reset FAIL** — `world_temp` 0.4 (initial 1.0), `season_clock` 6.0 (initial 0.0), `winter_ticks` 6.0 (initial 0.0) after `env.reset()` while meters DO reset, and a post-reset WAIT ticks the surviving effect: **third pack to reproduce `hamlet-d76684f549`, and the first to isolate the global-VFS half from the effect half** (comment 172). **obs-bounds FAIL** — 42 distinct out-of-`[0,1]` readings over 20 ticks, including raw unbounded global counters climbing 2.0→11.0 at offsets 86/87 and `obs_meter_comfort` at −0.1; **widens `hamlet-bf42ac60b5` from item slots to global-profile variables** (comment 174), and the −0.1 is separate evidence for `hamlet-f46e2b381a` (comment 173). **The column's predictive value is now demonstrated: the O-shape reset leak was pre-stated as a known result and reproduced exactly, in a pack authored independently of it.** Prior: **FIRST READING (Trial B, 2026-08-19): 6 of 7 clean — reward assertion exact to the declared component, obs-bounds 0 violations, boundary cap exact, N≥3 exercised, random-policy smoke clean (2000 finite non-constant rewards); double-reset FAIL — 3 effect instances survive `env.reset()` with a stale global in the first post-reset observation, the pre-stated O shape reproduced in a second pack (comment 169 on `hamlet-d76684f549`).** Known result if retro-run today: O fails (cross-episode effect leak, `hamlet-d76684f549`; stale obs at episode start) and F fails the bounds loop (raw `3.0` in a [0,1] vector, `hamlet-bf42ac60b5`); L and M expected clean. Stated so the column's readings surprise nobody. | 2026-08-18 | — |

### Trial 001 — "Sims in six dimensions" (PASSED, 2026-08-11)

Owner's proposed demo: take the Sims universe and re-substrate it into a 6-dimensional world.
**Executed and verified end-to-end. Zero lines changed under `src/townlet/`.**

- **Diff: one file, ~6 lines** — `stratum.yaml` `type: grid` → `gridnd`, `width/height` →
  `dimension_sizes: [4,4,4,4,4,4]`, `topology: square` → `hypercube`.
- Compiles; `env.reset()` OK; **50 `env.step()` calls OK**; agents carry 6-D positions
  (`positions` shape `(4, 6)`, e.g. `[0,3,3,1,0,3]`).
- The action vocabulary **auto-expanded from the dimensionality**: `DIM0_NEG … DIM5_POS`
  (12 movement actions) + `INTERACT, WAIT, REST, MEDITATE, GET, USE_SLOT_0, DROP_SLOT_0` = 19.
- The **entire Sims domain carried over untouched** — bars, affordances, items, effects, DAC
  rewards, curriculum. Only the substrate changed.

**One caveat, and it is a real authorability gap:** gridnd does **not** support partial vision, so
the pack's POMDP levels (L2/L3) must be switched to `active_vision: global` or the whole-pack
compile fails — the compiler builds *all* levels, not just the primary. The failure is loud and
precise, which is the good news; the limitation is genuine, and belongs in the authorability
ledger. Also noted: `grid3d` is in the `stratum.yaml` type Literal but has **no factory branch**
(`substrate/factory.py` handles grid, continuous, gridnd, continuousnd, aspatial only), so 3-D
specifically is the broken case while 4-D-and-up works.

This is the strongest available evidence for the substrate axis of the grammar, and it satisfies
`PDR-0003` obligation B (generality needs a second witness) better than a from-scratch universe
would, because holding the domain fixed isolates the variable being demonstrated.

**Scope note:** this measures authoring a *world* (UAC). Authoring a *mind* (BAC) is measured
separately in the input metrics below, because BAC Layers 1 and 3 are unbuilt — folding them into
one rate would hide which half of the thesis is failing.

**Best available proxy until the N-idea corpus exists:** the **Demo dogfooding** input metric.
Townlet Town is the first-class tech demo and is bound by the dogfooding rule in `vision.md` — it
must be authored through the same door as any user. Every privileged Python path it relies on is a
proven-load-bearing authorability gap. Unlike the N-idea corpus, this is measurable **today**
against code that already exists, which makes it the cheapest honest read on the central claim.

**Why this and not "students taught" or "coverage":** it is the only number that falsifies the
central claim. If a new mechanic still needs Python, the substrate-as-code thesis is false for that
mechanic, regardless of how green the gates are. It is measurable today with a fixed corpus of
candidate ideas and a `git diff --stat src/townlet/` check after each authoring attempt.

**2026-08-12 — A SECOND AXIS IS NOW NAMED AND UNMEASURED (`PDR-0024`, proposed).** The owner named a core use case the north-star does not cover: *game devs prototype here, then take a model and an interface contract to their own game code.* This metric measures **authoring** cost only. A product could score perfectly on it and still be useless to that user, because **export cost is measured by nothing** — and `grep` finds no model-export entry point in `src/townlet/` at all (`hamlet-0cdb8a6d1a`; `TASK-008` has been *Planned* since 2025-11-05). Whether export becomes a second north-star input or stays subordinate to authoring is the owner's call, pending on `PDR-0024`.

**2026-08-13 — `PDR-0024` RESOLVED, audience WIDENED.** The owner accepted the amendment and broadened it: not just game devs but *"anyone interested in game dev, simulations, or modelling the real world in an abstract way."* `vision.md` now names **the prototyping modeller** as a core use case served at release. Export cost enters the input table below as `UNMEASURED`; whether it is elevated into the north-star itself is deferred until `TASK-008` is re-specified — measuring a path with no entry point would be theatre.

**Instrumentation still not built.** Trial 001 was run by hand. Defining the N-idea corpus and a
repeatable trial protocol remains a bet (`roadmap.md` → Next). Until it exists, `1 of 1` is an
existence proof, not a rate — it proves the substrate axis works, and proves nothing about the
other axes. No bet may be accepted on north-star grounds from a single hand-run trial.

## Input metrics (the levers that move the north-star)

| Metric | Target | Current | Read on |
|--------|--------|---------|---------|
| **Time-to-first-training-step** for a brand-new config pack authored from a template | ≤ `<owner sets>` min by `<owner sets date>` | `UNMEASURED` | — |
| **Config-surface coverage** — declarative subsystems needing no Python to extend (VFS variables, VTC transitions, DAC rewards, effects, items, substrate topology, curriculum) | 7 of 7 by `<owner sets date>` | **2026-08-17 (latest, twenty-fifth checkpoint) — sixth movement: VFS PROFILE variables can declare their observation group (`PDR-0075`, DIV-006 `built`).** Every exposed global/agent profile variable now compiles to its own field carrying a required `semantic_type` from the one vocabulary, and the runtime reads it by declared scope; proven by execution — a `temporal` global lands in the temporal slice and the broadcast registry value is read back at the compiled offset (`test_profile_variable_observation_fields.py`). Item variables are observed through one `obs_item_slots` feature and deliberately take no declaration (layout question `hamlet-1ad6383186`). Still not a count that moves the ~2-of-7 figure — a fourth authoring surface made real, not a subsystem closed. Prior: **2026-08-16 (twenty-second checkpoint) — fifth movement: the SEMANTIC-TYPE surface is authorable and authoritative (`PDR-0066`, DIV-005 `built`).** An `environment.yaml` variable declares `semantic_type` from one closed vocabulary (`townlet.vfs.semantic_type`) and the compiler emits exactly that value, laying the field out with its group; proven by execution — a `temporal` declaration compiles, lands in the temporal slice, and the runtime tensor carries the variable's value at the compiled offset. `effects` admitted; `bars` reserved to meters at compile time. Scope honesty: this wires the surface for environment variables only; VFS profile variables are still one `obs_vfs` block and cannot declare it until `hamlet-f0ed709ecf` splits the block. Still not a count that moves the ~2-of-7 figure — a third authoring surface (after `range_type` and `clip`) made real, not a subsystem closed. `interaction_type` is now required from one vocabulary in the same unit. Prior: **2026-08-15 — fourth movement, and the first that a POINTED INSTRUMENT rather than a reading produced.** The normalization surface closed: `range_type` became a nine-member parameterized type declaration, the meter block became one observation field per meter, and all nine VFS normalization kinds are now authorable *and executed* (9/9 compile / observe / step, verified by adversarial review on two levels — `PDR-0057`). The clamping gap found by fixing `hamlet-1dba1910c0` closed as a PARAMETER, deleting `clipped_log_scaled` and taking the kind vocabulary from ten to nine distinct members (`PDR-0055`). **Cost recorded, not hidden: ~21% on `env.step`, ~32% on `_get_observations`**, reproduced twice — eight per-meter registry reads per tick instead of one vectorized pair, accepted because this product trades throughput for authorability by design. **Method note that is the real finding: a five-surface pre-cut census changed the DESIGN three times** (a VTC namespace collision that would have broken every bar-writing pack at runtime; a silent group-contiguity hazard that W6 would have turned into a wrong tensor shape; a checkpoint obs_dim mismatch that WARNED and loaded anyway). None was in the plan. Prior: **2026-08-15 — third movement, from the strangler side (`b7574132`, `PDR-0041`):** the three declared-and-crashing substrate configs (`observation_encoding: scaled`; `cubic` + `partial`; `width != height`) now compile AND run — the declared surface an author could already reach stopped lying at `env.reset()`. This closes the assessment's line-227 item (delegate substrate observation dims to the substrate instance) that `PDR-0035` subsumed out of WS-4. The **count stays ~2 of 7**: substrate *topology authoring* is still a documented 4-step Python edit, and the seven subsystems' YAML doors are unchanged — this movement is breadth of the *working* surface, not a new door. Prior: **~2 of 7** — the earlier `6 of 7` estimate was wrong. Assessment §4 ledger: VTC action-writes have no YAML path at all (`compilers/actions.py:205` hardcodes `writes=()`); custom actions are structural no-ops; 3 of 4 effect scopes inert; curriculum stages are a Python literal; a new topology is a documented 4-step Python edit. Substrate *selection* works (Trial 001); substrate *authoring* does not. **2026-08-11 — the cognition surface is revealed narrower still, not improved:** per-level `architecture` selection was assumed authorable and is not (`PDR-0009`, `hamlet-0d0115383e`). Curriculum remains the weakest of the seven. **2026-08-11 (later) — FIRST MOVEMENT, and it is real.** Task 3 (`30c433e3`) made every declared affordance cost gate the live interaction; previously only a hardcoded `money` index was consulted, so `energy`/`mood`/`satiation` costs were validated and ignored. Measured shift: 35-58% of interactions that used to complete now correctly do not. **2026-08-12 — second movement.** Task 3a (`7065729a`) wired `bars.*.bounds` at all six runtime sites **and** gave the VFS observation-normalization ABI its first production callers. An author can now change a meter's runtime ceiling *and* its observation scaling from `bars.yaml` alone. Measured on L1: money `1.000000 → 22.500000` per tick, money affordances `1 of 7 → 7 of 7`. Still not 7 of 7 overall — curriculum remains the weakest, and `hamlet-e979f2ba37` now records that the five shipped levels are three universes. | 2026-08-12 |
| **Failure loudness** — an authoring mistake (missing variable, stale artefact, hash mismatch) produces a clear compile-time error rather than a silent no-op | 100% of known classes | **2026-08-17 (later, `PDR-0075`) — three more classes are LOUD at compile time:** a global/agent profile variable without `semantic_type` fails to load; one declaring `bars` is a compile error naming the reserved rule; an exposed profile variable whose name collides with an environment variable, a meter field, a compiler block or another profile variable is a compile error naming both declarations. And a stale-cache class closed: `COMPILED_SCHEMA_VERSION` 1.16 refuses a pre-cut `.compiled` artifact whose config fingerprint would not otherwise have changed (item-only profile packs). Prior: **2026-08-17 (`PDR-0069`) — a new class enters LOUD on arrival: a `presentation.yaml` naming a meter or affordance the compiled universe does not declare raises `PresentationError` at server startup, naming the offender and the declared set; malformed files fail on the DTO (`extra="forbid"`, discriminated format, `symbol` only on `currency`). And a silent lie left the frontend: a wide-range meter no longer pegs its bar at 100% nor prints `value×100%` — it renders from declared bounds. Prior: **2026-08-16 (`PDR-0066`) — two mistakes moved from runtime to COMPILE time, one from silent to loud.** (1) An authored variable declaring the meter block's semantic type used to raise at the FIRST OBSERVATION (`_sync_meter_observation_to_vfs`); it is now a compile error naming the rule. (2) An affordance omitting `interaction_type` used to be silently coalesced to `instant` at runtime (the field's own description admitted it); it is now a parse error. (3) A compiler emitting an off-vocabulary `semantic_type` (`"effects"`, for months) is now impossible: the compiled DTO is typed and membership-checked, so the schema constrains the compiler, not only authors. Prior: Partially enforced, **now with measured evidence on both sides**. LOUD: VTC missing-target and checkpoint hash mismatch (`hamlet-2254316f44` / `hamlet-d5cb2dd4e7`); the item-VFS unresolved-profile guard raises rather than emitting zeros — it is loud enough that a *stale benchmark fixture* predating it now fails the suite, which is the metric working, not breaking. SILENT: the three provenance paths in the guardrail below, plus the gridnd/`grid3d` gaps. | 2026-08-17 |
| **Mind-authoring surface (BAC)** — Brain-as-Code layers declarable and *live*, of the 3 specified in `hld/02-brain-as-code.md` | 3 of 3 by `<owner sets date>` | **2026-08-22 — Layer 2 WIDENS twice in one day, count unchanged (still 1 of 3; Layers 1/3 unbuilt): `brain.yaml` is level-overridable with the fork stated at load (`PDR-0027` landed, `hamlet-0d0115383e` closed), the token path is proven config-in/behaviour-out (`PDR-0109`), and the set-encoder AGGREGATOR is a declared required choice — mean | attention — authored in a committed pack (`PDR-0112`, `ba2766e6`).** Prior: **1 of 3, and narrower than that reads (`PDR-0009`).** Layer 2 is live **at pack scope only** — `apply_training_overrides` merges exactly five fields and `architecture` is not one, so the most basic cognitive choice in a curriculum (*does this agent have memory?*) is not authorable per level at all. Every shipped pack is feedforward; the documented L0-MLP → L2-LSTM progression is inexpressible. Layers 1 and 3 remain **unbuilt**. Tracker `hamlet-0d0115383e`. **2026-08-13 — the recurrent TRAINING path is fixed (WS-1 b+c, `97e4b16b`):** the LSTM's recurrent weights now receive gradient (was 230/230 updates at exactly zero) and sampled-window boundaries bootstrap (was 100% collapsed to bare reward). The sequencing constraint that held `hamlet-0d0115383e` behind WS-1(b)/(c) (`PDR-0027`) is now satisfied — per-level `architecture` authoring is unblocked. The surface count is unchanged (still 1 of 3): this fixed the runtime the surface will drive, not the surface. | 2026-08-13 |
| **Declared-but-inert config surfaces** — schema fields that validate and are documented but do not drive runtime behaviour | 0 | **2026-08-16 (`PDR-0066`) — SIX declared-but-inert `semantic_type` sites CLOSED, by wiring one and deleting five, plus one hidden default.** Wired: `environment.yaml` variables (new, required, read by the compiler). Deleted as unreachable: `VariableDef.semantic_type`, `Global/Agent/ItemVFSVariableConfig.semantic_type` (three), `CompiledVariable.semantic_type` — each validated, was serialized, and drove nothing (the profile block carries one value). The compiler's private `allowed_semantic` remap and `default="custom"` on five classes gone; `interaction_type` `default=None` → `"instant"` coalesce gone. `no_defaults_lint` whitelist consumption fell 749 → 735 as a side effect. Running tally since the assessment: 7 found / 2 closed → **13 found / 8 closed** on this row's own accounting (the six semantic-type sites were one finding, `hamlet-2fe1c34ebb`, counted here per site). The `obs_vfs` block itself remains the largest single inert surface for profile variables (`hamlet-f0ed709ecf`). Prior: **~40 confirmed** across all 8 subsystems (assessment §3 P1). Highest-consequence: `drive.composition.normalize`/`clip`; `bars.recovery.natural` (**ships at `0.001`**, zero readers); `replay_buffer.min_size` (2 validators, real gate hardcoded); the whole `curriculum.adversarial:` block (in every shipped level, read by nobody); the recurrent encoder spec; `effects.yaml` `scope:`; `recording.enabled`. Nearly all ship at their **no-op value** — which is why it was invisible. **Count unchanged 2026-08-11, but a sibling category is now named:** four per-level content hashes (`bars`, `affordances`, `curriculum`, `training`) are computed, serialized and consumed by nobody (`hamlet-ae6601e463`). These are inert **outputs**, not inert config fields, so they are deliberately *not* added to this count — but the failure shape is identical and the fix is cheaper than any item above it. **2026-08-11 (later) — the count GREW before it shrank, and the new entries are larger than the old ones.** Newly confirmed inert: (1) the entire **VFS normalization ABI** — `apply_normalization` implements `minmax`/`zscore`/`log_scaled`/`one_hot`/`cyclical_sin_cos`, is tested, is hashed into the schema hashes, and has **zero production callers**, while the compiled `obs_meters` field description literally reads *"8 meter values (normalized)"*; (2) `clamp_and_validate`, a **declared-but-empty transition phase** — it appears exactly once in the codebase, as a string in `DEFAULT_TRANSITION_PHASES` (`hamlet-f46e2b381a`); (3) **2 of 4 brain architectures** (`recurrent`, `set_encoder`) implemented, authorable, documented, used by zero of 21 packs (`hamlet-fa6bb6da4a`, `PDR-0017`). Against that, `bars.*.bounds` was worse than inert — **contradicted**: L1 declares `money.bounds.max: 999999.0` and the runtime held it at `1.0`, killing the entire declared economy. **2026-08-12 — TWO CLOSED, ONE ADDED.** Task 3a (`7065729a`) closed both `bars.*.bounds` (contradicted → enforced at six sites) and the VFS normalization ABI (zero production callers → applied to every field that declares a spec). Newly confirmed inert in the same pass: `deficit_energy`, `deficit_satiation`, `time_since_last_eat`, `time_since_last_sleep` are declared observation fields with declared normalization and **zero writers anywhere in `src/`** — 4 of 124 observation dimensions are permanently `0.0` (`hamlet-dc8f887cd5`). The pattern `PDR-0016` named holds again: **an inert declared surface is rarely inert alone.** **2026-08-12 (later) — TWO MORE FOUND, both from the same root.** (1) `range_type` (`normalized`/`unbounded`/`integer`, declared per meter in every pack) has **exactly one hit in all of `src/townlet/`** — its own Pydantic field definition. Nothing reads it, which is why an *unbounded* money meter received a *bounded* minmax normalizer (`PDR-0020`, `hamlet-365e996511`). (2) Five pack-level hashes (`experiment`, `stratum`, `environment`, `actions`, `items`) are computed, serialized, round-tripped and compared by nobody — the dead-hash set is **nine, not four** (`hamlet-2dde1015fe`). Both filed, neither folded into WS-1 (`PDR-0021`). **2026-08-12 (later) — a SEVENTH: the declared PRESENTATION surface.** `environment.yaml` declares `cues:` with a `display:` block (icon, colour); there is a `CuesCompiler`, a config schema and a symbol-table entry — and `CuesCompiler` is **instantiated at `compiler.py:69` and never called** (two references in the codebase: the import and the constructor). `grep -rn "cue" frontend/src/` returns **zero hits**. So presentation is hardcoded by variable name (`if (name === 'money')`) while the declared surface for it sits unused (`PDR-0025`, `hamlet-0dd4ac24d9`). **Running count of confirmed inert declared surfaces: 7.** The rate of discovery still exceeds the rate of repair. **2026-08-13 — COUNTER CANONICALIZED.** This cell had been accumulating two incompatible figures (the `~40` opening and the running 7) and had become unreadable — and a guardrail nobody can read is not a guardrail, which is the lesson `Gates green` already cost three months to learn. Canonical reading from here: **assessment baseline ~40** (§3, subsystem-level estimate, never itemized); **post-assessment discoveries: 7 found, 2 closed** (bounds — contradicted→enforced, and the VFS normalization ABI), net **5 open** of the named set (`clamp_and_validate`, dead observation writers ×4-dims, `range_type`, five pack-level hashes, `cues`/`CuesCompiler`). The baseline needs one itemized recount before the two figures can merge; until then they are stated separately, on purpose. **2026-08-22 — ONE CLOSED, by deletion.** The four zero-writer observation fields (`hamlet-dc8f887cd5`) are deleted from `configs/default_curriculum/environment.yaml` (and its byte-identical differential/oracle-fixture copies): allocated width 124 → 120, `variables: []` in the shipped pack. Deletion, not wiring, because the per-tick side is not expressible for agent-scope variables on today's surface (effects need a spawner and finite duration; agent-profile expressions evaluate only at init; VTC passive dynamics are bars-only) — and Trial L then proved the counter mechanic is authorable *as a bar* (negative passive rate + `on_start` reset), so the declarations were not even the right home for the intent. Post-assessment tally: 7 found, 3 closed, net **4 open** of the named set (`clamp_and_validate`, `range_type`, five pack-level hashes, `cues`/`CuesCompiler`). | 2026-08-22 |
| **Subsystem maturity established** — subsystems with an evidence-backed disposition (KEEP / REPAIR / RESPEC / REBUILD / DELETE) rather than an unknown state | 8 of 8 | **8 of 8 — COMPLETE.** All eight assessed REPAIR (run `wf_4ca82820-274`, `PDR-0004`) | 2026-08-11 |
| **Demo dogfooding — privileged-Python count** — places where Townlet Town (the first-class tech demo) uses a Python path an outside author could not use | 0 | **2026-08-17 (later, `PDR-0076`) — not a count change, a class closed: the runtime no longer branches on ANY observation field name.** The unit-3 follow-on (`hamlet-39e1fe3c6d`) removed the nine remaining `obs_<x>` name-syncs in the encoder, the recurrent network's name-keyed slice lookup, the two demo sites sizing the window by name, and the compiler's name-set "feature or variable" test — replaced by one closed `feature` vocabulary on the compiled field. These were framework block names, not domain names, so this row never counted them; they were the same *mechanism* (`PDR-0045`), and `test_no_observation_consumer_carries_a_field_name_literal` now fails on the next one. · prior **2026-08-17 (`PDR-0069`/`PDR-0070`) — the frontend pair is CLOSED, one of it was never live, and a site the row never counted was found and closed the same day: ≥4 → ≥2 LIVE (the curriculum pair), plus recording.** Executed-behaviour accounting (`PDR-0045`): (1) `formatting.js:24` (`name === 'money'` → `$`) **closed** — no function in the module takes a name; (2) `AffordanceGraph.vue:136` **struck from LIVE**: fed only by an `affordance_graph` message no server emits and keyed on legacy names no pack uses — it never executed (`hamlet-102db4c2e0` filed to delete-or-rebuild); (3) **newly found and closed in the same unit**: `live_inference.py` `AFFORDANCE_ICON_MAP` (name→emoji, in `src/townlet`, live on every state frame) plus seven further live name-keyed sites in `frontend/src` (tiers, relationship map, colours-by-name, mood/social semantics, per-name CSS tokens, `AFFORDANCE_ICONS`, InteractionProgressRing colours) — none of which the 2026-08-15 enumeration counted (it grepped for meter-name branching only; the icon tables key on affordance names). Presentation is now declared (`presentation.yaml`, observer-only) or honest; the grep gate for meter/affordance names in `frontend/src` returns nothing outside the dead component and tests. Remaining LIVE: `curriculum/static.py:44` and `curriculum/adversarial.py:31-59` (hardcoded `active_meters`); `recording/video_renderer.py` under `hamlet-16ae192d42`. Prior: **≥ 4 LIVE, corrected the same day it was first taken (2026-08-15, `1b25c99d`).** The first measurement below said **≥ 6** and counted two sites that do not execute. Corrected by running them rather than reading them: **(1) `universe/adapters/vfs_adapter.py:31-41` is struck — it was DEAD CODE.** Zero callers in `src/`, not loaded even when the compiler is imported, its `position`/`meter` vocabulary present in no compiled output; the observation spec is built by `compilers/observation.py` instead. The module and its two test files were deleted, and the deletion was proved inert (all five levels' `observation_schema_hash` / `action_schema_hash` / `vfs_hash` / `variable_schema_hash` / `transition_graph_hash` byte-identical before and after). **(2) `universe/compilers/metadata.py:83` is struck from the LIVE count and reclassified** — it executes (`EAT` → `cost=5.0`) but `AffordanceInfo.cost` has **zero consumers** (`grep -rn "\.cost\b"` over `src/` and `frontend/src/` → 0; `affordance_metadata` is read only for `.name`). It is the computed-but-unconsumed shape (`hamlet-2dde1015fe`), not a path an author's vocabulary is denied. **The four that survive are the frontend pair and the curriculum pair** (plus `recording/`, itself slated for deletion). **The methodological correction matters more than the number:** the original enumeration was made by reading code, and *a site that branches on a name is only a privileged-Python instance if it runs* — this row must count executed behaviour, not grep hits, or it inflates exactly like the doc claims it was created to police. What the recon found instead is filed as `hamlet-2fe1c34ebb`: `semantic_type` has three disagreeing vocabularies, no authority, and a `default="custom"` on a parameter that feeds a provenance hash. Prior reading, kept for the record: **≥ 6, first measurement (2026-08-15, `PDR-0045`, `hamlet-60dd3c4b53`).** No longer `UNMEASURED`. The owner's principle — *"the compiler doesn't know what money is, it sees a name, and a set of parameters and builds it"* — makes this row mechanically countable for the first time: **every site that branches on a variable's name is a privileged-Python instance by definition**, because Townlet Town's vocabulary gets behaviour no outside author's vocabulary can get. Counted: `universe/adapters/vfs_adapter.py:31-41` (semantic type substring-matched against `default_curriculum`'s meter names — **and hashed into the observation field UUID**, so a pack naming its currency `credits` gets a different observation schema hash); `universe/compilers/metadata.py:83` (`costs.get("money")` is *the* affordance cost); `frontend/src/utils/formatting.js:24-28` and `AffordanceGraph.vue:136` (`hamlet-0dd4ac24d9`); `curriculum/static.py:44` and `curriculum/adversarial.py:31-59` (hardcoded `active_meters`); `recording/video_renderer.py:35,216` (slated for removal, `hamlet-16ae192d42`). Floor not a total — the enumeration covered meter-name branching only. **The counter-example that shows the target shape exists**: `config/drive_as_code.py:500` declares `money_bar: str` **required with no default**, and `dac_engine.py:721` resolves it — engine holds the *role*, author binds the *referent*. | 2026-08-17 |
| **Model-export cost** — what stands between a trained agent and running in the user's own engine/pipeline (`PDR-0024`: the prototyping modeller leaves with a model + interface contract) | ≤ `<owner sets>` steps by `<owner sets date>` | `UNMEASURED` — **no export entry point exists** (`hamlet-0cdb8a6d1a`; `TASK-008` designed 2025-11, unbuilt, needs re-spec under `PDR-0012`). The contract half is real and hardening (WS-1's hashes); the standalone-model half is absent. Do not measure until an entry point exists. | 2026-08-13 |

## Guardrails (must NOT degrade while chasing authorability)

| Metric | Floor / ceiling | Current | Read on |
|--------|-----------------|---------|---------|
| **Gates green** — `ruff check`, `black --check`, `mypy src`, `pytest` | all 4 pass | **2026-09-02 (fifty-second checkpoint, `PDR-0145`/`PDR-0146`) — read by the `PDR-0127` method, every per-push row: `project-recovery-3@1eb347f7` Lint ✅ Config Validation ✅ Tests ✅ (the gate-2 stamp commit); PR #38 `lint` ✅ `unit` ✅ `validate-config-packs` ✅ on both PR and push triggers; `main@ea3648db` (the fifth merge) Lint ✅, Tests and Config Validation in progress at this checkpoint — the agent-owned merge landed no red gate on `main` (`PDR-0101` trigger 2 unfired). Every run on `project-recovery-3` in its life was green; the original `project-recovery` was too — only `-2` ever went red.** Prior: **2026-08-29 (forty-seventh checkpoint, `PDR-0130`) — read on `main` at `9efadd3c`: Lint ✅, Config Validation ✅, Tests in progress (`33245264455`); read on `project-recovery-3` (= `9efadd3c` + two docs-only commits): inherits the same. Scope note: `main` now carries the token cut, so its nightly is the first that runs the post-cut suite on the default branch. **2026-08-29 (forty-sixth checkpoint, `PDR-0127`) — GREEN at `b915139e` and `1065dbf0`, all three per-push workflows `completed/success` — after 47 CONSECUTIVE RED Lint runs (`7dc6f66c` 2026-08-22 → `8b733f3e` 2026-08-26; last green before them `0b659130` 2026-08-21) that this row did NOT record: the two checkpoints in that window stamped the branch green without reading it. Causes: Black (64 files) in front of 18 no-defaults hits, the workflow stopping at the first red. Fixed `237b0c38` + `b915139e` (three real defaults made required; fifteen structural hits whitelisted with reasons). RULE from here: this row is written only from `gh run list` at the tip SHA, every workflow, never inferred. Prior state, retained: **2026-08-18 (thirty-second checkpoint) — GREEN at the Trial O commit, and a four-push RED found and closed: branch Lint had been failing since the Trial F commit `fb56fbbd`** on two E501s in `configs/trial_f_durability/probe_trial_f.py` — the last two checkpoints recorded local suite green while nobody read the remote Lint gate, and the cause is structural: the local pre-commit habit lints `src/` while trial probes live under `configs/`. Fixed at `a3318624`; CI on the fix: Lint ✅ Config Validation ✅ Tests ✅ (21m45s). Trial O commit `2dcc2273`: local full default suite **3281 passed / 16 skipped / 0 failed** (910s, bare `uv run pytest`, nothing deselected — byte-identical counts to the pre-trial baseline), pack `validate` exit 0, ruff + black clean on the pack; CI on the push: Lint ✅ Config Validation ✅, Tests in progress at this checkpoint — read it before citing this row green at `2dcc2273`. `main`'s scheduled nightly GREEN again 08-18 (run `32107696959`) — now 3-for-3 since the merge. **Rule this row takes from the miss: a trial-pack commit's local gate list must include `ruff`/`black` on the pack directory, not just `src/`** (applied at the Trial O commit; the record's §10 evidence shows it). Prior: **2026-08-18 — the first SCHEDULED post-merge nightly on `main` is GREEN (run `32003077539`, 06:47Z 08-17, Full Test Suite, success): the owner-dispatched green on the same tree is now confirmed by the schedule itself, and `PDR-0072` trigger 2 is discharged without firing.** This session's four commits are docs/workspace-only; branch CI fires on the push and was not re-read locally — no code gate claim is made for them. Prior: **2026-08-17 (latest, twenty-sixth checkpoint, `PDR-0076`) — GREEN on the branch at unit 4 (`ebd16fce`), all six measured gates: `ruff` ✅ `black` ✅ `mypy` ✅ `no_defaults_lint` ✅ (caught one ternary-as-default in the new meter publisher before push — the lesson from `c69bd2ff` held) `validate_compiler_cli` ✅, full suite **3281 passed / 16 skipped / 0 failed** (bare `uv run pytest`, 21 new tests). Remote: Lint `31984884149` ✅ Config Validation `31984884176` ✅ Tests `31984884156` ✅ on `ebd16fce`. Harness matrix at the same tree, CPU + CUDA, run `20260817-111409` exit 0: 16 `AGREE` + 4 `DIVERGED_AS_REGISTERED` (DIV-006, unchanged). The scheduled 06:00 UTC Full Test Suite on `main` had not yet fired at this checkpoint (01:xx UTC); its reading is the next session's first look.** · prior (2026-08-17, post-twenty-fifth-checkpoint reading) — THE FULL TEST SUITE WORKFLOW IS GREEN ON `main` FOR THE FIRST TIME: the owner did not wait for the 06:00 UTC schedule and dispatched it by hand against `main` at `4222a917` — run `31981122221` (`workflow_dispatch`, 00:08–00:35 UTC): **3239 passed / 24 skipped / 0 failed**, bare `uv run pytest`, nothing deselected. That is the reading that retires the *"`main` still carries all 33 behind the marker"* caveat this row has carried since `PDR-0059`; the workflow's own history is now red-red-green (`31869712709`, `31931718941` scheduled and red with the 31 the deleted `slow` marker hid; `31981122221` green). `PDR-0072` trigger 2 **did not fire**. The scheduled run itself is still to come and should read the same tree — a red there would be a new fact, not the owed one. Branch: all three workflows green on `c69bd2ff` (Lint `31980842275`, Config Validation `31980842311`, Tests `31980842363`) — the `no_defaults_lint` red at `8c5fa2c8` is closed. Prior: **2026-08-17 (twenty-fifth checkpoint) — branch GREEN at `8c5fa2c8` locally over the whole suite (3260 passed / 16 skipped / 0 failed, ruff, black, mypy, config validation), CI Tests + Config Validation green on the push; CI **Lint RED once** on the push — the repo's `no_defaults_lint` gate (a fifth check I had not run locally) flagged one ternary-as-default in the new item-slot sync; fixed in the next commit and the gate now runs in the local pre-push list. `main` unchanged at `4222a917`; the first post-merge nightly (06:00 UTC 08-17) is still the reading owed. Prior: **2026-08-17 (same session, `PDR-0073`) — GREEN ON `main` FOR THE FIRST TIME OVER A SUITE THAT HIDES NOTHING: the second merge landed at `4222a917`.** Read at the tip before merging: `f023b9e7` Tests **3239 passed, 24 skipped, nothing deselected** (run `31968235680`, 26m57s), Lint and Config Validation green; the PR's own required `lint`/`unit`/`validate-config-packs` checks all passed (`unit` 26m55s). `main`'s `pyproject.toml` carries no `-m "not slow"`. **Not yet read: the first post-merge scheduled nightly** — it runs the same bare `uv run pytest`, so it is the reading that retires the *"`main` still carries all 33 behind the marker"* caveat this row has carried since `PDR-0059`; until it is read, that caveat is discharged by construction, not by observation (`PDR-0072` trigger 2). Prior: **2026-08-17 (twenty-fourth checkpoint) — READ, NOT FULLY RE-RUN, at `905acd96`, and that is correct: the commit changes `README.md`, a workflow comment, an `ORACLE.md` table row and the `harness.py` module docstring — nothing executable.** Re-read on the touched surface only: ruff / black / mypy clean on `harness.py`; `full-tests.yml` parses; the 117 oracle unit tests pass. The sweep itself re-executed the gate set as README claims (`PDR-0072`): ruff, black (520 files), mypy (171 files), `no_defaults_lint` (735), `validate_compiler_cli` all exit 0; `pytest --collect-only` 3263 collected, no `deselected`, no `-m` in `addopts`; `npm test` and `npm run build` pass; the harness cell `default_curriculum:L0_0_minimal` → `DIVERGED_AS_REGISTERED {"shape":"hash-only"}`, exit 0. **CI on the push: Lint ✅ Config Validation ✅ (`31968042982`, `31968042976`), Tests in progress (`31968042996`) at checkpoint — read it before citing this row as green at `905acd96`.** Scope, restated because the merge is imminent: `main` at `07b26ed5` still carries all 33 behind the marker and its nightly (`31931718941`) is red with 31; the first nightly after the merge is the reading that closes that thread. Prior: **2026-08-17 (twenty-third checkpoint) — GREEN at `fb791193`, and the FRONTEND HAS A GATE FOR THE FIRST TIME (`PDR-0070`).** pytest **3247 passed, 16 skipped, 0 failed** (643s; +27 = exactly the new tests: DTO 15, loader 9, server wiring 3); ruff pass; black clean; mypy clean on the touched modules; `no_defaults_lint` clean (735); harness matrix at the same tree, CPU+CUDA, run `20260817-002157`: 16/16 `DIVERGED_AS_REGISTERED`, exit 0, movers unchanged (nothing compiled moved — `PDR-0069`). NEW: `cd frontend && npm test` **37/37** (vitest, jsdom) and `vite build` clean — until `a5cca764` there was no `package.json`, so no frontend gate could exist. Branch CI for `a2f349d7` completed green (Lint, Config Validation, Tests) after the last checkpoint; the two new commits are not yet pushed at this reading. Scope unchanged: `main` still carries all 33 behind the marker until the merge, which now owes `PDR-0039`'s re-sweep on **26 commits** — `PDR-0068`'s ~30 trigger is one unit away. Prior: **2026-08-16 (latest, twenty-second checkpoint) — GREEN at `a2f349d7`, still over a suite that deselects NOTHING.** pytest **3220 passed, 16 skipped, 0 failed** (1138s) — +27 net (28 new tests, one dead fixture retired with the deleted module's test-free surface); ruff pass; black 516 unchanged; mypy 169 files clean; `no_defaults_lint` clean (735 whitelisted, down from 749); `validate_compiler_cli` exit 0. Harness matrix at the same tree, CPU+CUDA, run `20260816-225750`: 16/16 `DIVERGED_AS_REGISTERED`, exit 0, mover set exactly DIV-004's four. Branch CI on the push: Lint and Config Validation green, Tests in progress at checkpoint. Scope unchanged: `main` (23 behind) still carries all 33 behind the marker; nightly expected red until the merge (`PDR-0068` sequences the merge after the queue). Prior: **2026-08-16 (twenty-first checkpoint) — GREEN OVER A SUITE THAT DESELECTS NOTHING (`PDR-0065`).** Six gates at `a725bf66`: ruff pass; black 513 unchanged; mypy 168 files clean; `no_defaults_lint` clean (749 whitelisted); `validate_compiler_cli` exit 0; pytest **3193 passed, 16 skipped, 0 failed — and the word "deselected" no longer appears in the output at all** (889s). Arithmetic exact: 3189 + 2 harness self-tests + 2 new unit pins. `-m "not slow"` is out of `addopts` and the marker declaration is gone from both places it lived. **The harness's own reading, taken AFTER the fix through `main()`'s new plumbing:** matrix vs `oracle-2026-08-13`, CPU+CUDA, run `20260816-184228` — **16/16 `DIVERGED_AS_REGISTERED`** (10× DIV-004 hash-only with every stream byte-identical, 6× DIV-003), exit 0; same reading as `20260815-213851`. `PDR-0063`'s *"any harness green before this closes is void"* is discharged by the close, and the reading is stated rather than banked: 16/16 `DIVERGED_AS_REGISTERED` is the *correct* verdict while DIV-004 is open, and `AGREE` stays unreachable matrix-wide until it closes (`PDR-0056`, exit condition 2 untouched). **Scope, on purpose:** this is the BRANCH. `main` at `07b26ed5` still carries all 33 behind the marker, and the nightly on `main` this morning reported **31 failed** again (run `31931718941`, 06:33Z, identical to yesterday's) — expected, tracked, cleared by the next merge, which now owes `PDR-0039`'s re-sweep on 20+ commits. **Exit condition 3 is met on the branch.** Prior: **2026-08-16 — GREEN, AND FOR THE FIRST TIME IN THIS ROW'S HISTORY THE DEFAULT COMMAND RUNS WHAT IT CLAIMS TO (`PDR-0062`).** Full default suite at `e62a5e4a`: **3189 passed, 16 skipped, 2 deselected, 0 failed** (986s); ruff pass; black clean (513 files); mypy clean (168 source files). **The number that matters is the deselect count: 33 → 2.** The 31 failures `PDR-0059` found are repaired AND now run in the default suite, because the `slow` marker was measured against a clock and found false — the three files run 31 tests in **34.8s**. The arithmetic is exact and was checked rather than asserted: 3157 + 31 previously-hidden + 1 new contract test = 3189. **The remaining 2 deselected are stated with their disposition, per this row's own rule:** `test_differential_harness.py`, 2 failures, filed P1 as `hamlet-6f98e38a36`, and `-m "not slow"` stays in `addopts` only until it closes. **Which surfaces the sharper finding (`PDR-0063`): the hidden count was never 31, it was 33.** `hamlet-a0832f9004` enumerated failures from a CI log naming three files and never asked what ELSE the marker covered — the same incuriosity in miniature that let the marker hide 31 for weeks. And the fourth file is the worst one to have been blind to: **the differential harness's self-comparison DISAGREES**, so the instrument the entire strangler is measured with currently certifies nothing. Not banked, not fixed here — re-freeze-vs-register is a specification call for whoever holds the oracle. **CI CONFIRMS IT, AND THE DESELECT COUNT IS THE PROOF (2026-08-16, later).** The Tests workflow — the one carrying `-m "not slow"`, and the one that went green last session over a suite excluding the 31 — is **success on all three commits** (`e62a5e4a`, `e5e631ff`, `fda18b83`); run `31911162104` reports **3181 passed, 24 skipped, 2 deselected, 0 failed** (24m49s). *Success* alone would not have settled it, because that is exactly what a green-over-a-narrowed-suite looks like; **2 deselected instead of 33** is what shows the 31 actually executed in CI. Totals reconcile against the local run exactly — 3189+16 = 3181+24 = **3205**, the 8-test difference being CUDA tests skipped on CI runners. The specific risk taken here was that these 31 integration tests entered the per-push gate for the first time under always-on `--cov`, where this repo has two documented wall-clock-ratio flakes (`hamlet-f9090ec3e8`); **neither fired**. `PDR-0043`'s rule — *a gate restored is not a gate verified* — discharged by reading rather than assumed. Prior: **2026-08-15 (latest) — NOT GREEN, AND THE ROW HAS BEEN READING A COMMAND THAT EXCLUDES TESTS (`PDR-0059`, `hamlet-a0832f9004`).** The nightly Full Test Suite — restored on `main` at the merge, discharging `PDR-0043` trigger 2 — ran for the first time on 2026-08-15 06:32Z (run `31869712709`) and reported **31 failed, 3120 passed, 24 skipped**. All 31 sit in three files: `test_temporal_mechanics.py` (15), `test_training_loop.py` (8), `test_recurrent_networks.py` (8). **All three are `pytest.mark.slow`, and `pyproject.toml:113-121` carries `-m "not slow"` in the DEFAULT `addopts`** — so the local four/six *and* CI's `tests.yml` (bare `uv run pytest`) deselect them; only `full-tests.yml` (`-m "slow or not slow"`) runs them. **That deselection is the "35 deselected" this row has printed verbatim for weeks.** Nothing was hidden — the number was in every reading — and it was read as *tests that are slow*, never as *tests that are red*. Cause profile, **read from error text and NOT executed locally**: 11× `VectorizedPopulation.__init__()` missing 4–5 required positional args (plus one unexpected kwarg), 10× affordance-not-deployed assertions, 3× `FileNotFoundError` on a `configs/default_curriculum/…` path, 2× *"ObservationSpec-driven slicing is required in v2.1"*, 1× missing `next_observations`. Reads as tests left behind by this recovery's own changes — but `PDR-0049` applies with its polarity reversed: **the red is real (it executed, in CI, on the public default branch); the comfortable diagnosis "just stale tests" is the unverified half.** `test_training_loop.py` is the training loop, and this product's claim is trainable environments. **Second finding, separately adjudicated (`PDR-0060`): CI's Tests job also reddened on the branch** at `bf0f2fe4` (run `31870278368`) on `test_scripted_vtc_threshold_kernel_within_hardcoded_baseline_tolerance`, which carries **no** `slow` marker and passed at `07b26ed5` on unrelated code — a `time.perf_counter()` ratio assertion (tolerance 1.50) under always-on `--cov`, the same construction as `test_vfs_overhead_under_limit`. `PDR-0043` **reversal trigger 1 is adjudicated NOT FIRED** (the local set *does* cover the test, so the set-difference claim is not falsified; what failed is the assumption that a green local run of a nondeterministic covered test predicts a green CI run). `hamlet-f9090ec3e8` rescoped from one test to the class. **Reading at HEAD (`d5bfcc38`): all three CI workflows green — Lint ✅, Config Validation ✅, Tests ✅ (run `31887137993`) — AND THAT IS THE POINT.** The Tests job is the one carrying `-m "not slow"`, so it went green over a suite that excludes the 31 known failures. This is what the row looks like when it is wrong: **not a red anyone ignored, a green that was never entitled to the word.** Quoted here rather than banked. **The row's rule from here:** a `Gates green` reading that excludes tests must state what it excluded and its disposition — a deselect count with no disposition attached is exactly how this survived. Prior: **2026-08-15 — GREEN ON THE SECOND OF TWO RUNS; the first failed one flaky performance test, ruled out as deletion-caused on two independent grounds. Read the qualifier before the number.** 6 of 6 green locally at the dead-adapter deletion, AND A FLAKY GATE MEMBER FOUND. Re-read because code moved (the `vfs_adapter` deletion, `hamlet-60dd3c4b53`): ruff pass; black clean (512 files); mypy clean (168 source files — the −1 is the deleted module); `no_defaults_lint` clean; `validate_compiler_cli` exit 0. pytest: **3124 passed, 16 skipped, 35 deselected, 0 failed** (1005s) — and the −6 against the 3130 baseline is *exactly* the six tests in the two deleted adapter test files, which is the count the deletion predicted. **CI independently green on `1b25c99d`** (Lint, Config Validation, Tests all success), the second consecutive all-green push. **The finding, and it is the more important half:** the FIRST of these two pytest runs failed one test — `test_vfs_overhead_under_limit` — and the second, on an unchanged tree minutes later, passed it. Filed as `hamlet-f9090ec3e8`. The test asserts a **5% wall-clock ratio between two envs, measured under always-on `--cov=townlet --cov-branch` instrumentation**, with a jitter allowance four orders of magnitude below the noise; it is not measuring VFS overhead, it is measuring CPU contention. It is in the CI gate (`tests.yml` runs bare `uv run pytest`). Ruled out as caused by the deletion on two independent grounds before it was dismissed: the suite has no random-ordering plugin, and the performance test collects at position ~675 — **before** the `unit/universe/` tests that were deleted — so the change cannot have altered anything preceding it. **This is `PDR-0043`'s rule finding a new route: a gate restored is not a gate verified, and a gate that reddens at random is how a verified gate becomes a waved-through one.** Prior: **2026-08-15 — GREEN IN CI, NOT JUST LOCALLY. The channel this row could not see is now open.** The first CI runs ever to execute on `project-recovery` all passed on `dd94e122`: **Lint** 31852298597 (1m11s), **Config Validation** 31852298591 (1m14s), **Tests** 31852298586 (24m21s). `hamlet-2100105c9a` closed on that evidence (`PDR-0043`'s rule: a gate restored is not a gate verified). **`PDR-0043` trigger 1 did NOT fire** — nothing was red for a cause the local set covers, so the local-vs-CI gate correspondence held on first contact, which is the substantive result: the local six are now a *verified* proxy for CI rather than an assumed one. Still outside this row's reach, and stated so it is not read as covered: the **nightly full-tests cron** (scheduler reads the default branch's file — deferred to the merge, `PDR-0043` trigger 2) and the **full matrix** workflow (available, unrun). Prior: **2026-08-15 (CI restoration, `cb865af4`, `PDR-0043`) — THE MEASURED GATE SET WIDENED FROM 4 TO 6, AND ALL 6 ARE GREEN LOCALLY.** The two CI-only gates in the local/CI difference were BOTH red when measured this session: `validate_compiler_cli.py` (known, `hamlet-2100105c9a` — two schema-drifted packs, repaired rather than excluded) and `no_defaults_lint.py` (**undiscovered until now** — 93 violations, every one in code postdating the gate's last CI run on 2025-11-28: the "145 unvalidated commits" made concrete). Both now green: 93 violations adjudicated per the ledger's house style (12 stale entries for deleted files pruned; `vfs/vtc.py` entry **PROVISIONAL** pending `hamlet-c4ce5515cc` — the suppression names its register entry), and the local four re-read at the same commit — pytest **3130 passed, 16 skipped, 0 failed** (802s), byte-identical counts to the knockdown baseline. The CI CHANNEL itself: `lint`/`tests`/`config-validation` now fire on push to `project-recovery` (bare `pull_request` kept — that is the merge-gate mechanism); the nightly cron is DELETED and the Full Test Suite parked `disabled_manually` BY DESIGN — the scheduler reads the default branch's file, so an enabled cron resumes the recorded 15-failure red stream against stale `main` until the merge (`PDR-0010`'s corrosion, declined); on-demand full matrix = enable + `workflow_dispatch`. **The row is not done**: `hamlet-2100105c9a` sits in **verifying** and closes only on the first green CI run after the owner's push — a gate restored is not a gate verified. Prior: **2026-08-15 (later) — 4 OF 4 HELD, re-read at the FIRST KNOCKDOWN (`b7574132`)**: ruff pass; black clean; mypy clean (169 files); pytest **3130 passed, 16 skipped, 0 failed** in one clean full run (594s) — the +45 over 3085 is the substrate observation-shape contract tests (36), the seam integration tests (7), and the matrix pins. The harness gate ran the full **16-cell** matrix vs `oracle-2026-08-13` twice, exit 0 both: CPU (10 executed: 5 AGREE + 5 SKIPPED-cuda… run `20260815-055108`) and **CPU+CUDA** (all 16 executed: **10 AGREE, 6 `DIVERGED_AS_REGISTERED`**, run `20260815-055207`) — the first runs in which the gate's `DIVERGED_AS_REGISTERED` vocabulary carried real weight, and the six suppressed cells each name DIV-003 with the tag-verified signature matched in the old side's final exception. Acceptance additionally backed by a 5-mutant battery (detached worktree, loud-probe first, all killed; one probe input itself corrected by the battery — see `PDR-0041`). Two pinned-old-behaviour tests were deliberately rewritten with the cut (the 2-D-window-on-3-D-grid pin that never dared call `reset()`, and the window-clamp pin — both documented in `PDR-0041`). Prior: **2026-08-15 — 4 OF 4 HELD, re-read at the register-suppression close (`9a75b581`)**: ruff pass; black clean; mypy clean; pytest **3085 passed, 16 skipped, 0 failed** in one clean full run (489s) — the +19 over 3066 is exactly the new adjudication tests. The differential harness ran the full CPU matrix against `oracle-2026-08-13` **twice** this session (mid-state and final): AGREE×5, SKIPPED×5, exit 0 both times. **The harness gate's exit contract itself changed by design** (`hamlet-56ec575ae2`, `PDR-0040`): exit 0 now also admits `DIVERGED_AS_REGISTERED` naming its register entry, and refuses empty AND all-SKIPPED runs — so "harness green" now certifies *agreed-or-diverged-as-registered*, not bare agreement; the acceptance evidence includes a 10-mutant battery (all killed, injection probe-verified) because the gate is only as good as its red. **`PDR-0037` reversal trigger 1 FIRED during the pass and was answered by its own prescription** (narrow the match) — see `PDR-0040`. Prior: **2026-08-14 — THE LOCAL FOUR HOLD, AND CI IS DEAD (`hamlet-2100105c9a`, P1).** Found verifying README claims. This row's four gates are LOCAL, and its readings are sound — but a reader takes "gates green" to mean *this project's gates are green*, and CI is a channel that reading never covered. Measured at `a099663c`: **no workflow has ever run on `project-recovery`** (`gh run list --branch project-recovery` → empty; three of four workflows trigger on `push: branches: ["main"]` + `pull_request`, and this branch is neither), so **all 145 commits of the recovery — WS-1, the seeding door, the oracle tag, the whole harness — are CI-unvalidated.** The last successful run of anything was **2025-11-28** (PR #31 into `main`). The only workflow still firing, the nightly Full Test Suite, **failed on every run on record** thereafter, last on 2026-01-30, and has not fired since — GitHub's dormancy policy on a quiet default branch. And were CI pointed here today, three of four would stop early: `scripts/validate_compiler_cli.py` **exits 1** at `configs/reference/model_pack`, runs before the other steps in all three, and nothing sets `continue-on-error` — so pytest is never reached. Note the local four are a strict SUBSET of CI's gate set (CI also runs `no_defaults_lint.py` and `validate_compiler_cli.py`), and the gate in the difference is the failing one. **Fourth distinct instance of the `PDR-0010` pattern** after the original four, wardline (`hamlet-f894ade20a`), and the harness's unreadable red (`PDR-0037`). Stated publicly in `README.md` → *Continuous integration* rather than left implied. Prior: **2026-08-14 — the local four NOT re-read, and that is correct, not a lapse.** This checkpoint committed no code: `HEAD` was `58ace08f` (docs-only) throughout, the tree was clean, and the session's entire delta is decisions. The reading below stands unchanged because nothing it measures moved. The rule in *Reading notes* — re-read at every checkpoint **that touches code** — is being followed, not skipped; stated explicitly so a future reader does not read the gap as the 2026-05-16 failure repeating. Prior: **4 of 4 — HELD, re-read 2026-08-13 at the differential harness (`d54ad7df`)**: ruff pass; black clean; mypy clean; pytest **3035 passed, 16 skipped, 35 deselected, 0 failed** in one clean full run (781s). The +43 over 2992 at the oracle tag is exactly the harness's own tests. **A FIFTH GATE IS NOW KNOWN TO BE DECORATIVE, and it is the Gates-green lesson repeating (`hamlet-f894ade20a`, P1).** `CLAUDE.md` and `docs/agents.md` instruct every agent to run `wardline scan . --fail-on ERROR` before returning code that touches external input, and to read exit 0 as the gate passing. Measured 2026-08-13: it passes — and `--fail-on-inert` **fails**, reporting *"taint gate INERT: 0 trust boundaries recognized across 1555 analyzed functions."* Cause: wardline fires only when data crosses a **declared** boundary; `grep` finds zero `@external_boundary`/`@trusted`/`trust_boundary` anywhere in `src/townlet/`, no `trust_packs` in `weft.toml`, and wardline is not even a project dependency. **The instruction is currently unfalsifiable — no code change can make that command fail.** Repo-wide and pre-existing; not caused by the harness branch. The fork is the owner's: wire real boundaries, or delete the instruction — leaving an instructed gate that checks nothing is the antipattern this row exists to catch. Prior: **4 of 4 — HELD, re-read 2026-08-13 at the oracle tag (`0e875d7a`)**: ruff pass; black clean (501 files); mypy `Success: no issues found in 164 source files` (the +1 is `determinism.py`); pytest **2992 passed, 16 skipped, 0 failed** in one clean full run at the exact tagged commit (the +11 over 2981 is exactly the eleven new seeding/determinism tests). This run doubled as the `PDR-0029` trigger-1 test: every WS-1 pinning test green at the tag commit. Seeding landing additionally mutation-verified in a detached worktree (three named mutations, each caught by its intended test). Prior: **4 of 4 — HELD, re-read 2026-08-13 at the WS-1 batch close (`e8ad4985`)**: ruff pass; black clean (497 files); mypy `Success: no issues found in 163 source files`; pytest **2981 passed, 16 skipped, 0 failed** (the +4 over 2977 is exactly task 3b's four dead-agent pinning tests). Task 3b's landing additionally mutation-verified (two named mutations each caught by its intended test). The first batch-gate run failed 2 tests — a *latent test defect* (CPU action tensors into a CUDA env, shielded until 3b's dones filter ran ahead of the `.any()` gate), root-caused and repaired to the file's own pattern, not a product regression. Prior: **4 of 4 — HELD, re-read 2026-08-13 at the tasks-6+7 gate (`97e4b16b`)**: ruff pass; black clean (496 files); mypy `Success: no issues found in 163 source files`; pytest **2977 passed, 16 skipped, 0 failed** (the +8 over 2969 is exactly the nine new recurrent pinning tests minus the one deleted obsolete-API test). Landing verified beyond the gates: red-before/green-after reproduced in a detached worktree at `53d99d5f`, and four named mutations (clobber reintroduced, boundary hidden states crossed, carry dropped, boundary hidden zeroed) each caught by its intended test. Prior: **4 of 4 — HELD, re-read 2026-08-13 at the task-5 gate**: ruff pass; black clean; mypy `Success: no issues found in 163 source files`; pytest **2969 passed, 16 skipped, 0 failed** (the +7 over 2962 is exactly task 5's 8 new pinning tests minus the deleted `config_hash_warning` test). Prior: **4 of 4 — RESTORED 2026-08-11 (`c2f61beb`).** ruff pass; black 0 files; mypy `Success: no issues found in 163 source files`; pytest **2935 passed, 16 skipped, 0 failed**. The gate now also *stays* green by construction: pre-commit pinned black 25.11.0 / ruff v0.14.5 against a lock resolving 26.3.1 / 0.15.12, so every commit was formatted by a different binary than CI checked with — replaced with `repo: local` hooks running `uv run`. Prior reading, for the record: **1 of 4 — MEASURED, `PDR-0010`.** ruff **pass**; black **fail** (`environment/dac_engine.py`, +3 under `tests/`); mypy **fail** (3 errors in 2 files); pytest **fail** (1 of 2942 — a stale benchmark fixture, not a product defect). Recorded green since 2026-05-16 while three of four were red. Nothing here is deep; it went unnoticed *because the workspace said it was green*. | 2026-08-17 |
| **Test coverage** | ≥ `<owner sets>`% by `<owner sets date>` | **81% — RESOLVED, `PDR-0010`.** One clean full-suite run (`--cov=townlet`, 2942 tests, 7m53s). Both disputed figures retired. The **19%** is now *explained*, not merely superseded: re-running a single test overwrote `.coverage` and reported `TOTAL 16%` — the same artefact the 2026-05-16 report suspected of its own number. Needs one replication before it is cited publicly. | 2026-08-11 |
| **Documentation truth** — count of confirmed false architecture claims in canonical docs | 0 | **2026-09-02 (fifty-second checkpoint, `PDR-0145`) — THE SIXTH README SWEEP, for the fifth merge: 103 claims checked across 39 commits, 29 defective (22 stale / 2 wrong / 5 misleading) and 22 omissions; the adversarial pass found NINE defects in the draft's own corrections (a P1 count of two that is fifteen, three UTC/local dates, the hash-field history, an adjudication run framed as current, a "first branch with no red" that wasn't first, a claim falsified by a fix in the same commit, a CLAUDE.md hunk omitting mypy). CLAUDE.md's 18-day-old false CI caveat and the dead `CuesCompiler` claim corrected in the same commit (`4e23b3ea`); README stamped at `1eb347f7`. `PDR-0101` trigger 1 (a clean sweep = an unrun sweep) did not fire.** Prior: **2026-08-29 (forty-sixth checkpoint, `PDR-0128`) — THE FIFTH README SWEEP, for the fourth merge: 33 stale, wrong or misleading claims and 16 material omissions in 143 commits (the largest range yet), SIX defects in the draft's own corrections caught by the adversarial pass, two sweep findings overturned by the drafter. All corrected, not re-described; stamped at `1065dbf0` (`6fb148fd`). Reading on the README at the stamp: 0 known-false claims. The trend holds — 10 / 5 / 21 / 18 / 33 across five sweeps, roughly linear in commits — and remains the argument that the file is a stamped status report, not a standing description. Prior state, retained: **2026-08-17 (later, `PDR-0072`) — THE THIRD README SWEEP: TWENTY-ONE claims stale or misleading in 27 commits, TEN more caught in the revised draft, all corrected in `905acd96` — and the decay rate is now measured three times (10 in one day, 5 in four commits, 21 in 27 commits): roughly one stale claim per commit-and-a-bit, every sweep.** The twenty-one are itemised in `PDR-0072`; the headline ones: the frontend *"cannot be built as shipped"* (false since `a5cca764`, and it now has a test gate); the first merge described as a 167-commit fast-forward (168-commit PR merge `07b26ed5`); *"every CI run passed, six pushes"* (19 pushes, 59 of 60, one flake); the nightly *"has not yet completed a run against the merged tree"* / *"may still need `gh workflow enable`"* (`active`, fired twice, red both times with the 31 the deleted `slow` marker hid); *"15-run failure streak"* (64; the workflow has never passed in 91 scheduled runs); the harness described with ONE `DIVERGED_AS_REGISTERED` shape a full session after `PDR-0056` taught it two; the pack layout ignorant of `presentation.yaml`, `range_type`, `semantic_type`, `interaction_type`; *"the README still on `main` badges a test count"* — `main`'s README **is** this file. **The adversarial pass's ten include a NEW CLASS this row must watch from here: sentences true on the branch that read as false the moment the file becomes `main`'s README** (*"They go green when this branch merges"*, written into the file that will sit on `main` after they went green). Rule recorded in `PDR-0072`; the adversarial brief now asks the tense question explicitly. Also fixed by the same rule, outside the README: `harness.py`'s module docstring (*"currently one shape"*), the `full-tests.yml` comment (*"15-run"*), `ORACLE.md`'s harness row (*"next, reshapes WS-3"* — contradicting `PDR-0034` for four days in a `docs/oracle/` file this project calls trustworthy). Not counted here, filed: `hamlet-df91baa2bb` — `CLAUDE.md`'s *"No file named `drive_as_code.yaml` exists in any shipped pack"* is contradicted by a stray, unread fixture at `configs/test/items_smoke/`; the sentence is true of *shipped* packs and the fixture is the defect. `CLAUDE.md:65` still cites the deleted `REVIEW-2026-08-15…` file — third flag, owner's file, unchanged. Prior: **2026-08-17 (`PDR-0070`) — one doc rewritten because the thing it described was replaced, one new schema doc, the dangling citation unchanged.** `frontend/CLAUDE.md` "Customization" said icons live in `constants.js` and meter colours in `tokens.js` — true until `fb791193`, now false, rewritten in the same commit (declared in `presentation.yaml`; rendering rules in `formatting.js`). New: `docs/config-schemas/presentation.md` (schema, honest default, payload, failure modes, what it is not). Root `CLAUDE.md:65` still cites the deleted `REVIEW-2026-08-15…` file — flagged again, not fixed (owner's file); its DTO list also lacks `presentation_config.py` (incomplete, not false). Prior: **2026-08-16 (latest, `PDR-0066`) — TWO CORRECTED, ONE NEW DANGLING CITATION, and a root cause named by the owner.** Corrected in `docs/architecture/archive/UNIVERSE_AS_CODE.md`: it listed FOUR interaction types including `continuous` (never real — VTC rejected it, the only module naming it had zero importers, now deleted) → three, with `interaction_type` required; and it named the duration field `required_ticks` in 13 places where every shipped pack and the DTO say `duration_ticks` (verified against L1's `affordances.yaml`). `vfs.md` (§4.1/§4.3/§8.4) and `vfs-current-implementation.md` now describe the semantic-type vocabulary at all — they never did; the owner's diagnosis: *"vfs was extended at the last minute to include a bunch of extra stuff and the docs never backflowed"* — QUICK-05 added `semantic_type` to five classes after the design doc was written, which is exactly how it drifted three ways with no authority. NEW: the owner deleted `docs/architecture/REVIEW-2026-08-15-architecture-docs-and-hld.md` (`0da08142`) and **`CLAUDE.md:65` still cites it** — flagged, not fixed (owner's instructions file). Two doc gaps routed to WS-5 (comment 157): no schema doc for `variables[].semantic_type` or `interaction_type`. Prior: **2026-08-16 (earlier) — THREE MOVEMENTS, and again the sharpest is this workspace's own (`PDR-0065`).** (1) **`current-state.md` predicted the nightly would report "RED (2 failures)"; it reported 31** (run `31931718941`). The nightly reads the DEFAULT branch, which still carries all 33 behind the marker; the 2 harness failures were branch-only. That is the `PDR-0058` shape exactly — a claim about `main` written from the branch's vantage — recurring one checkpoint after `PDR-0058` named it. Corrected in this checkpoint's brief; the durable rule is now in the reading notes below. (2) **`PDR-0063` itself overstated**: *"the harness cannot currently certify anything"* — the matrix had certified the meter cut at `2535a306` and did so again today; the self-tests were the defect. Corrected by supersession-in-part, not by editing the body. (3) **`tests/README.md` and `tests/test_townlet/README.md`**: every sentence describing the `slow` marker corrected in the same commit that removed it (`a725bf66`); their OTHER staleness — a `tests/test_townlet/slow/` directory that does not exist, `configs/test/training_level_*_smoke.yaml` that do not exist, 2026-05-16 test counts and the 19% coverage artefact `PDR-0010` already diagnosed — is **named and routed to WS-5 (`hamlet-7a52a63e0b`), not fixed here.** Fourth consecutive session in which this row's movement includes a product-workspace document. Prior: **2026-08-16 — a false status block corrected for the THIRD time in the same file, and this time the mechanism that made it false was removed too.** `test_temporal_mechanics.py`'s docstring twice read *"ALL TESTS PASSING (17/17)"* while 15 of 17 failed. Last session replaced the claim; this session removed what made it undetectable (the `slow` marker, `PDR-0062`) so the docstring's successor is checked by the default suite on every run. **The general lesson, and it is this row's sharpest:** correcting a false claim does not stop it recurring — the claim was not careless, it was *unfalsifiable*, and only the second fix changes that. Two further stale-comment corrections in the same sweep: `# $100 money` on a line setting money to 1.0 (money's bound is 999999, so it meant $1 and the affordance cost $5), and a docstring citing affordances that no shipped pack contains. Prior: **2026-08-15 (latest) — TWO MOVEMENTS, AND THE WORSE ONE IS THIS WORKSPACE'S OWN (`PDR-0058`).** (1) **The merge sweep found FIVE more stale README claims in four commits** (`33bfff51`, executing `PDR-0039`'s unconditional re-fire at the merge commit — the `1b25c99d` sweep did not count for `1576e280`). That is the second consecutive measurement of this file's decay rate, and it is consistent with the first: ten claims in one day, then five in four commits, every one decaying because the recovery *fixed* the thing being described. The re-fire-at-merge rule is now measured, not assumed. (2) **This workspace asserted, in plain text, something that had been false for seven hours.** The merge to `main` landed at `07b26ed5` (2026-08-15 14:09 AEST); `current-state.md:61` said *"The merge remains available and untaken"* and `roadmap.md` said *"SATISFIED but NOT BANKED"* — carried forward across **four checkpoints written after the merge**. Corrected this checkpoint. **The structural finding is why it survived four re-readings:** `roadmap.md` defined the Now bet's exit as *the owner's merge* — an event the workspace neither controls nor performs — so no reading was attached to it and nothing was watching for it. An exit stated as someone else's action cannot be observed to have happened. Restated as an outcome condition in `PDR-0058`. **Third consecutive session in which this row's movement is in product-workspace documents rather than engineering ones**; the 2026-08-14 note that its scope was never restricted to `docs/` is now simply how the row behaves. Prior: **2026-08-15 — MERGE GATE 2: the README re-verified by method, and TEN claims had gone stale in ONE DAY (`PDR-0048`, `1b25c99d`).** Every one decayed because the recovery *fixed* the thing being described — CI green where it said nothing had ever run, `validate_compiler_cli.py` exiting 0 where it said 1, both named packs compiling, the harness matrix 16 cells not 10, and transition families carrying zero rules three → two. **The decay rate is the finding**: a file stamped and verified on 2026-08-14 was ten claims wrong on 2026-08-15, which is why `PDR-0039` scopes README claims to a commit and re-fires the sweep at the merge rather than banking it. **The adversarial pass again caught a defect in the new draft** ("three pushes deep" against a two-push branch) — fourth consecutive rewrite in which it earned its cost. Also corrected this session, and they are the sharper class because they were *this workspace's own*: `docs/architecture/archive/UNIVERSE-COMPILER.md:36` claimed compiler stage 5 uses `vfs_to_observation_spec` (deleted as dead code the same day); `PDR-0045`'s two cited violations were struck after execution falsified them (`PDR-0049`); and a claim written into `PDR-0047` **during this session** — "log scaling is implemented and unwired" — was false in both halves, carried forward from an older cell of *this file* rather than read from source, and was caught only because the owner pointed at `docs/architecture/VFS.md`. **Three of this session's corrections were to product-workspace documents, not engineering docs.** The row's 2026-08-14 note that its scope was never restricted to `docs/` is now the common case rather than the exception. Prior: **2026-08-15 — the `docs/` triage opened, and the biggest single source of false claims turned out to be an AMBIGUITY, not staleness (`PDR-0044`/`PDR-0045`).** Three irreconcilable observation-dimension tables were never reconcilable: **"observation dim" is two quantities** — allocated (**124**, constant at every level, and the actual mechanism behind the transfer-learning property) and active (**95 / 56 / 99**). Note this row's own prior entry said "measured 124 for *both* L0 and L2" — true of allocated width, and it repeats the conflation. Corrected in place. Root cause found: `docs/zzz. archive/vfs/observation-dimension-manual-validation.md` ticks ✓ against 3×3 / 7×7 grids **that no level can express** (grid size is pack-level and unoverridable) — it validated the *intended* curriculum against *shipped* code, and `docs/architecture/VFS.md` §2.3 imported the result marked "Validated". **A doc that validates intent against code is more convincing than one with no ticks, and every tick in it is false.** Also removed: `docs/README.md` claimed "85+ files across 9 categories" (actual **573 across 27**, of which **320 live**) and routed developers to three of the worst files, one of which (`architecture/TOWNLET_HLD.md`) **does not exist** — rewritten with trust tiers and all links verified. `docs/zzz. archive/vfs-integration-guide.md` (orphaned; flat pack paths, `variables_reference.yaml` implied required, false dims) and both `docs/vfs/observation-dimension-*.md` bannered **superseded rather than corrected** — under `PDR-0044` the method is obsolete, not the arithmetic. Two errors of this project's own AI authorship also corrected: the artifact accessor added to `CLAUDE.md` the prior session was wrong twice (`.levels` does not exist; the field is `total_dims`), and the first framing of this measurement ("all five levels identical") was fixed in all six places before it was reported. Prior: **2026-08-15 — the first knockdown moved this row in BOTH directions at once (`b7574132`).** Two claims were *corrected in the same commit that falsified them*: `CLAUDE.md`'s substrate-type list and `README.md`'s rough-edges bullet both described `type: grid3d` as present-but-branchless, and the commit that deleted the literal rewrote both lines (the `dcc5f803` lesson — when you change the thing, fix every sentence that described it — applied prospectively for once). And one long-false claim **became true instead of being deleted**: `CLAUDE.md`'s "POMDP Support — Grid2D, Grid3D (vision_range ≤ 2)" was measured false on 2026-08-12 (cubic+partial crashed at reset); the cut makes the claim accurate, which is the *code-ignores-the-docs* class resolving in the docs' favour without an owner ruling — possible here only because the crash, not the sentence, was the defect. Prior: **≥ 14 confirmed** and growing: `configs/L0_*`–`L3_*` packs documented in `README.md`/`CLAUDE.md` **do not exist**; `frontend/package.json` documented as runnable but **missing**; README's **70% coverage badge is false** (real: 81% — errs low, still wrong on a public repo); `CLAUDE.md`'s **29/54 observation dims are false** (measured 124 for *both* L0 and L2); `CLAUDE.md` documents **L2/L3 as LSTM/`RecurrentSpatialQNetwork`** while every shipped pack is feedforward and per-level architecture is unauthorable (`PDR-0009`). Note `caf404b8` rewrote `CLAUDE.md`/`AGENTS.md` on 2026-08-11 *without* fixing any of these — shorter, not truer, and the mtime signal is reset again. **2026-08-11 (later) — three MORE confirmed false, all in CLAUDE.md, all measured:** (1) the architecture claims — no pack constructs `SimpleQNetwork` (zero production sites; it is dead code) or `RecurrentSpatialQNetwork`; the shipped brain is `feedforward` with `layer_norm: false`, so the documented "MLP with LayerNorm" does not exist; (2) `drive_as_code.yaml` is stated REQUIRED for all packs **in two places** — every shipped pack uses `drive.yaml`, and a grep written against the documented filename returns zero hits and falsely "confirms" that no level references `money` (two packs' `drive.yaml` do); (3) "L0_0_minimal: 1 affordance / 29 dims" — measured 14 affordances and 124 dims. The second is the dangerous class: a doc error that makes a *verification* silently vacuous. **2026-08-12 — three MORE confirmed false, all measured by `diff`, all in CLAUDE.md (`PDR-0018`):** (1) the **"Low Energy Delirium" curriculum is not implemented** — `L0_0_minimal/drive.yaml` and `L0_5_dual_resource/drive.yaml` are byte-identical, both `constant_base_with_shaped_bonus`, and **no shipped level declares a `multiplicative` extrinsic**, so the documented contrast cannot be demonstrated; this is the claim `vision.md:94` calls *"the flagship demonstrator of the substrate"*; (2) the **per-level grids (3×3, 7×7) do not exist** — 8×8 is set once in pack-level `stratum.yaml` with no override path; (3) **the five levels are three universes** — `bars`/`affordances`/`drive` byte-identical across all five, `L0_5` and `L1` `training.yaml` identical but for `output_subdir`, `L0_0` vs `L0_5` `curriculum.yaml` differing only in comments. **All four of the sites this row has been tracking are now CORRECTED in CLAUDE.md** — the first repair to this guardrail, after `PDR-0016` committed to it and `30c433e3`/`9a6de69e` did not deliver it. Plus `CLAUDE.md`'s "Job payment = $22.5, sustainable with proper cycles" (false at runtime until `7065729a`) and `docs/config-schemas/brain.md`'s `brain_hash` description and bogus import path (`31c17111`); `docs/config-schemas/bars.md` did not exist and was written. **2026-08-12 (later) — a DIFFERENT class, and it cuts the other way.** `docs/architecture/VFS.md` ch.9 turns out to be *correct and unimplemented*: it prescribes `clipped_log_scaled` for money (implemented, never wired) and states money is normalised such that `1.0 ≈ $100`, which the shipped dollar-denominated configs contradict. **Two confident diagnoses in two days were corrected by a design doc that existed the whole time** (`PDR-0018`, `PDR-0020`). The row has been counting docs that lie about the code; it must also count code that ignores the docs. **2026-08-13 — the vision's own false-tense claim is corrected at source (`PDR-0026`).** `vision.md`'s "flagship demonstrator … proof that the thing works" was measured false (`PDR-0018`) and escalated; the owner resolved it as **mis-tensed intent, not a false claim** — *"the idea outran the codebase"* — and the paragraph is re-tagged as delivery intent with an amendment-log entry. This sets the resolution pattern for the *code-ignores-the-docs* class: only the owner can say whether an ENDORSED document describes a lie or an unbuilt intention. **2026-08-14 — one MORE confirmed false, and it is in `vision.md` itself.** The note under the authority grant states the public repo is at `github.com/tachyon-beep/hamlet`; `origin` is `github.com/foundryside-dev/hamlet`. Measured: `gh repo view` on the old path resolves to the new one, and **both report `PUBLIC`** — so the grant's blast radius is unchanged and this is a stale pointer, not a wrong claim about reachability. **CORRECTED the same day** (`PDR-0038`, `a099663c`): offered at the grant re-confirmation, initially declined-by-omission and carried here as blocked-on-owner, then owner-approved later in the session and fixed with an amendment-log entry. Class note: this is the first entry in this row sitting inside a *product-workspace* document rather than `CLAUDE.md`/`README.md`/`docs/` — the guardrail's scope was never restricted to engineering docs, and this is the reminder. **2026-08-14 — THE LARGEST SINGLE MOVEMENT THIS ROW HAS EVER RECORDED, and it is downward.** `README.md` was rewritten from verified source (`hamlet-6730ba7915`, `PDR-0038`, `be3ebc2e`). A ten-agent sweep catalogued **102 confirmed-false claims** across the old README and adjacent docs, drafted the replacement from 147 evidence-backed facts, and three adversarial lenses then found **24 more in the draft itself** (8 FALSE, 15 MISLEADING, 1 UNVERIFIABLE) before it was finalized — the draft-stage catch rate is the number to remember: a rewrite briefed entirely on not lying still produced 24 defects, which is why the adversarial pass is not optional (`PDR-0033`). Highest-risk claims independently re-verified before publishing. The new file deliberately carries **no test count, no coverage percentage, no observation width and no performance figures** — those literals are exactly what decayed into the old file's falsehoods — and says so in a `Numbers` section that tells the reader how to read each off the tree. **Two structural notes:** (1) the three false claims deleted with the wardline block (`hamlet-f894ade20a`) count here too; (2) `docs/architecture/` and `docs/config-schemas/` are **untouched and still unreconciled** — the README now warns readers explicitly rather than the row silently implying they are clean. `hamlet-7a52a63e0b` (WS-5) owns them. | 2026-08-17 |
| **Runtime throughput** — env steps/sec at the benchmarked scale axes | ≥ baseline, no regression | **Runnable, but no recorded baseline artifact is on disk** (`PDR-0011`). The benchmarks execute (the 2026-08-11 suite ran `env-step`, `env-step-scale`, `action-mask`, `reward`, `vtc-runner`) and the commits exist (`7868dba7`/`3311bc00`, `hamlet-2b92152ac9`), but `runs/` is empty, so the comparison baseline must be **re-run to be cited**. Do not quote the hot-path report's numbers as current. **2026-08-11 (later) — GPU is no longer blocked.** `torch.cuda.is_available()` was `False` with `nvrtc: failed to open libnvrtc-builtins.so.13.0`; cause was the duplicate CUDA stack above, now removed. Full suite wall-clock roughly halved (10m42s → ~5m15s). Still **no recorded env-steps/sec baseline artifact** — that gap is unchanged. | 2026-08-11 |
| **Provenance integrity** — a change to variables / observation / actions / transitions changes the corresponding hash, and a mismatched checkpoint is rejected | no silent acceptance | **2026-08-16 (`PDR-0066`, DIV-005) — the register learned its FIFTH entry and the harness adjudicated a second hash-only cut under the existing binding.** Predicted-then-measured (DIV-004's method, pre-cut worktree `fb60d581` vs live, five levels + `effects_smoke`): `default_curriculum` moves only the RAW `environment_hash`; `effects_smoke` moves `observation_schema_hash` + `vfs_hash` (its mirror stops lying about `obs_effects`); the prediction matched every row. Cost inherited from `PDR-0056`, stated in the entry: because DIV-005's movers ⊆ DIV-004's declared four, the standing cells cannot distinguish the two cuts' provenance movement — behaviour is adjudicated at full strength (every stream byte-exact), provenance only as "still exactly the four". `hash_fields` was NOT widened, so DIV-004's own "third widening → re-tag" trigger did not fire. Prior: **2026-08-13 (harness session) — GREEN, and now instrumented.** The differential harness (`PDR-0032`, `d54ad7df`) compares the two sides' compiled provenance hashes as **stage 1** of every cell and short-circuits to `HASH_MISMATCH` before traces are consulted — so a hash surface that moves between oracle and rebuild is now caught mechanically rather than by inspection. Scope honesty: this instruments the **compile-time** hash surface only; DIV-001/DIV-002 are checkpoint-boundary and remain **un-machine-checked** under the trace-only v1 scope (`PDR-0032`) — probes arrive when a knockdown touches that surface. Two harness defects found in review would have weakened this row had they shipped unnoticed: absent-vs-`None` hash keys compared equal (11 `*_hash` fields are `str \| None`, so a hash field the rebuild *removed* read as unchanged), and nothing asserted the `PYTHONPATH` injection took effect (both sides on one code root → every cell trivially AGREE). Both closed; the injection guard is now a `HARNESS_ERROR` verdict. **`PDR-0030`'s third reversal trigger was tested and did NOT fire**: the harness reproduces the oracle's traces from the recorded seeds on **both** CPU and CUDA. Residual, tracked: hash-boundary tests unwritten (`hamlet-c8c316ba03`); `load_trace` does not assert array leading dims match its own declared `steps` (`hamlet-1ec950ee60`, contained as `HARNESS_ERROR`, no false AGREE). Prior state: **2026-08-13 (oracle session) — GREEN.** The `PDR-0028` condition is met: both filed gaps entered WS-7's known-divergences register (`hamlet-2dde1015fe` → DIV-001, `hamlet-df2b972c49` → DIV-002; `docs/oracle/known-divergences.md`, `ce9288c0`), each re-verified against source at the oracle tag and stamped `tag-stamped`. The register carries them as **intended** divergences the rebuild closes — old-accepts/new-rejects on those surfaces is the divergence working. Additionally banked this session: **run reproducibility is real** — `seed_all` single door, required `training.seed` in all 25 packs riding `training_hash` (`PDR-0031`), same seed → bit-identical env trace verified **CPU and CUDA** through the JIT vtc kernels (`6f60060e`, `0e875d7a`). Residual, tracked and named, not silent: hash-boundary tests unwritten (`hamlet-c8c316ba03`); the DIV-001/DIV-002 fixes themselves land in the rebuild, where the differential harness adjudicates them. GPU **training-loop** determinism (cuDNN backward) is explicitly unclaimed (`ORACLE.md`). Prior state: **2026-08-13 — BREACH 3 OF 3 CLOSED (`ebb8fa85`). All three original breaches are now closed.** The serving path routes through the new shared `assert_checkpoint_identity` gate (format version → `vfs_hash` → dimensions + drive/brain + four content hashes → `primary_level`), inserted before weight-apply so a mismatch serves nothing; `config_hash_warning` DELETED per `PDR-0022` (a warning is silent acceptance); both of `runner.py`'s `if universe is not None:` silent skips deleted — the write-path one could stamp a checkpoint with **zero** provenance keys; `unified_server` now raises when uvicorn's `serve()` returns without starting (it returns *normally* on lifespan failure). **D5 is done end-to-end**: the L0_5→L1 cross-level resume is rejected, measured with `output_subdir` normalised so `primary_level` is provably the only separator. Verified red-before/green-after on the pre-fix tree and by four mutations, each caught by name. **The row is NOT yet green**: `hamlet-2dde1015fe` (nine dead hashes) and `hamlet-df2b972c49` (two uncovered stamp/compare paths) must enter WS-7's known-divergences register — the first is a hard precondition of the `config_hash_warning` deletion (`PDR-0022`) — and hash-boundary tests remain unwritten (`hamlet-c8c316ba03`). Prior state: **BREACHED — measured end-to-end, `PDR-0008`.** Three independent silent-acceptance paths: (1) ~~the compile cache is not keyed on `primary_level`~~ **CLOSED 2026-08-11 (`22b7616d`)** — cache keyed per level, `from_dict` resolves by name rather than by a colliding hash triple, and `primary_level` is stamped into every checkpoint (D5); previously `assert_checkpoint_vfs_hash` was *satisfied by a corrupted comparand* and an L0-trained network resumed into a live L2 run with no error — then wrote a checkpoint stamped with L0's identity, so the mislabelling is **inverted and permanent**; (2) four per-level content hashes (`bars`, `affordances`, `curriculum`, `training`) are computed and read by **nobody** — `hamlet-ae6601e463`; (3) the live-inference serving path invokes **zero** identity guards — `hamlet-1029f99f4b`. `hamlet-d5cb2dd4e7`'s enforcement is real but only on the honest path. Hash-boundary tests still unwritten (`hamlet-c8c316ba03`). **2026-08-12 — BREACH 2 OF 3 CLOSED (`31c17111`).** The four per-level content hashes (`curriculum`, `bars`, `affordances`, `training`) are now stamped by `attach_universe_metadata` and **hard-compared** by `assert_checkpoint_dimensions`, copying the `drive_hash` pattern (missing on either side raises). `brain_hash` was also wrong in both directions — it hashed `brain.yaml` RAW, so a `training.yaml` override that changes the network that actually trains left it byte-identical (false negative), while editing an already-overridden `brain.yaml` value rejected a bit-identical run (false positive); it now covers the **effective** config at the primary level. Recon before implementing found the spec's change 1 was a `TypeError` as written. **Two adjacent gaps found and filed, NOT closed:** the dead-hash set is **nine, not four** (`hamlet-2dde1015fe` — `experiment`/`stratum`/`environment`/`actions`/`items` are computed, serialized and compared by nobody), and **two further checkpoint stamp/compare paths** exist that neither task 4 nor task 5 covers (`hamlet-df2b972c49`). So breach 3 of 3 closing does **not** by itself make this row green. | 2026-08-12 |
| **Pre-release hygiene** — dead-code / orphan items outstanding against the zero-backwards-compat rule | 0 | **2026-08-17 (later, `PDR-0076`) — two more dead things gone with unit 4:** the `obs_affordances` field-name alias the recurrent network and the encoder still matched (no compiler has emitted it since the v2.1 spec), and `meter_name_from_observation_field` (a name-prefix parse whose only reason to exist was the branch it served); plus the duplicated vision-window math at two demo sites, now one helper. · prior **2026-08-17 (`PDR-0070`) — the frontend's un-runnable-gate debt is gone, and three dead exports plus two dead components with it.** `frontend/package.json` had never existed (`hamlet-d892e161c0`, open since 2026-05-16): no build, no test could run — `PDR-0012`'s failing-gates category in a form no gate could even report. Closed at `a5cca764`. Deleted as dead: `AffordanceLegend.vue`, `ReferencePanel.vue` (unreferenced, hardcoding legacy affordance names), `AFFORDANCE_ICONS`, `METER_THRESHOLDS`, `MOOD_THRESHOLDS` (zero importers), per-name CSS tokens (`--color-meter-*`, `--color-affordance-*-stroke`) in two files. Left, filed: `AffordanceGraph.vue` (`hamlet-102db4c2e0`). Prior: **2026-08-16 (latest, `PDR-0066`) — one dead module and fourteen whitelisted defaults gone.** `src/townlet/environment/affordance_config.py` (zero importers in `src/` and `tests/`, admitted an interaction type nothing implemented) deleted; five `default="custom"` and one `default=None` on behavioural parameters removed; `no_defaults_lint` whitelist consumption 749 → 735. Also removed as debt-by-inertness: `CompiledVariable.semantic_type` and its serialization in `compiled.py` (written to every artifact, read by nobody). Prior: **2026-08-16 (earlier) — THE FAILING-GATES CATEGORY IS EMPTY ON THE BRANCH, and two more dead declarations are gone (`PDR-0065`).** The 33 this row picked up on 2026-08-15 (31 + the harness's 2) all run and pass in the default suite; the category `PDR-0012` widened this row to cover — failing gates — reads **0 on `project-recovery-2`** (still 33 on `main` until the merge). Also removed: the `slow` marker declaration in `pyproject.toml` (zero users) and a **second** registration of the same marker in `tests/test_townlet/conftest.py` citing a `--runslow` option that exists nowhere — a declared-but-inert surface registered twice. **A dead fixture, a dead flag, a marker with no users, an option that never existed: four consecutive removals in two sessions of things that read as coverage or configuration and were neither.** Still counted: the two wall-clock ratio assertions in the gate (`hamlet-f9090ec3e8`), untouched. Prior: **2026-08-16 — TWO DEAD ARTEFACTS REMOVED, one of which had been asserting a signature that never existed.** `tests/test_townlet/_fixtures/networks.py::recurrent_qnetwork` had **zero users** and passed `num_meters=`, which is not a parameter of `RecurrentSpatialQNetwork` and never has been — any use would have raised `TypeError` at binding, so its wrongness was preserved precisely BY being dead (`PDR-0064`). Also deleted: the `_use_observation_spec` flag and the `forward()` raise it guarded, unreachable once the spec became required. A fixture with no users and a wrong signature is not coverage; it is an artefact that reads as coverage, which is this row's whole subject. Prior: **2026-08-15 (latest) — THE COUNT GOES UP BY 31, and they are the row's own first category.** `PDR-0012` widened this row to cover **failing gates**; 31 tests are failing on `main` behind a marker deselection (`Gates green` above, `PDR-0059`, `hamlet-a0832f9004`). `vision.md`'s tech-debt anti-goal lists failing gates first and forecloses the deferral — *broken-but-unreached is still broken* — so these count here whether they turn out to be stale tests or live defects. Disposition is deliberately unset: step 1 of the unit is to run them and decide repair-vs-delete by execution, per `PDR-0049`. Also newly counted: **two wall-clock ratio assertions inside the gating suite** (`PDR-0060`, `hamlet-f9090ec3e8`) — a test that measures CPU contention rather than the thing it names is the decorative-gate shape applied to a gate that does run. Prior: **2026-08-15 (later) — four removals (`cb865af4`, `d77e0610`, `PDR-0043`):** two schema-drifted packs repaired (empty `costs:{}`/`effects:{}` blocks on WAIT, left behind by `f63e0f00`); **12 stale no-defaults whitelist entries** referencing deleted files pruned (a verification ledger declaring dead surface is the decorative-gate shape applied to the gate's own config); **ten expended mutation/probe worktrees** deleted from disk (221MB — unique diffs salvaged first: one in-flight draft of the already-shipped `PDR-0040` change, nine identical copies of one network probe); ten accidentally-tracked worktree gitlinks removed from the index and `.claude/worktrees/` gitignored so it cannot recur. Prior: **2026-08-15 — two removals (`b7574132`):** the dead `type: grid3d` literal (declared in the schema, branched on in two compilers, buildable by nothing — the assessment's "delete the literal and the four branch sites" disposition, executed; the branch sites died with the type-switches themselves) and the window min-clamp (a latent declared-and-crashing surface: any config where it engaged declared one width while the encoder produced another). Prior: ~15 at 2026-05-16; several removed since (`4124ab5f`, `4360148d`, `fdc08611`) — **uncounted since**. Scope widened by `PDR-0012` (strict no-tech-debt until 1.0, no research-code exemption): this row now also covers failing gates, inert surfaces, computed-but-unconsumed outputs, and broken-but-unreachable code. Needs a recount. **2026-08-11 — first recount since May, and a large one.** Removed: 13 runtime dependencies with **zero references anywhere** in the repo (`tensorflow` declared *twice*, plus `mlflow`, `pettingzoo`, `gymnasium`, `pandas`, `scikit-learn`, `flask`, `flask-cors`, `cloudpickle`, `gitpython`, `requests`, `python-dotenv`, `rich`), `apply_interaction`, `get_affordance_cost`, `check_affordability`, `money_idx`. Dependency floors corrected from fiction (`ruff>=0.0.280` against 0.15.12 running) to what is exercised. Side effect worth recording: removing `tensorflow` **restored CUDA on this machine** — it dragged a duplicate `nvidia-cudnn-cu12` stack alongside torch's `cu13`. | 2026-08-17 |

## Reading notes

- **An instrument's fix message describes its author's scenario, not necessarily yours (2026-08-16,
  `PDR-0065`).** The harness said *"either re-freeze the fixture or register the divergence"*, and
  `PDR-0063` took that as the decision space — without reading the register, which had already
  chosen *register* the day before. The message was correct for an oracle-vs-live run; the failing
  test was not one. Same shape as reading "35 deselected" as *tests that are slow*: the text was
  accurate, the reading of it was wrong. On any instrument's message, ask what situation it was
  written for and whether this is that situation, and read the register/spec that message points
  at before treating its options as exhaustive.
- **A prediction about `main` written from the branch is the `PDR-0058` shape, and it recurred one
  checkpoint later (2026-08-16).** "The nightly will report RED (2 failures)" was a branch reading
  attached to a default-branch event. Any sentence in this workspace that predicts what CI on
  `main` will do must say which commit `main` is at when it fires.
- **The coverage row is resolved (`PDR-0010`) and the finding it carried still stands.** The real
  figure is **81%**; the README advertises **70%**. Being wrong in the *safe* direction does not
  make an unverified quality claim on a public repo acceptable — that is what the guardrail is for.
  The 19% is diagnosed as a partial-run `.coverage` artefact, reproduced deliberately. **Do not
  cite 81% publicly until a second full-suite run replicates it** (`PDR-0010` reversal trigger).
- **A guardrail can be re-read faithfully and still be blind: check what the reading COMMAND
  excludes, not just what it reports (2026-08-16, `PDR-0062`/`PDR-0063`).** `Gates green` was
  re-read at every code-touching checkpoint exactly as the note below prescribes, and it printed
  "35 deselected" every time — an accurate number, read as *tests that are slow* and never once as
  *tests that are red*. Two distinct failures compounded: the marker was never checked against a
  clock (the three files run 31 tests in **34.8s** — "slow" was simply false), and when the
  failures were finally enumerated, they were enumerated from a CI log listing three files rather
  than by asking **what else the marker covers**. It covered a fourth file, so the hidden count was
  **33, not 31**, and the extra two are the differential harness — whose self-comparison
  **disagrees**, meaning the instrument the strangler is measured with certifies nothing. The
  durable rule: **a deselect/skip/xfail count with no disposition attached is an unread part of the
  gate**, and enumerating a suppression's victims from a downstream report is not the same as
  enumerating its scope.
- **An unfalsifiable claim is not fixed by correcting it (2026-08-16).** The temporal test file's
  docstring said "ALL TESTS PASSING (17/17)" while 15 failed; last session corrected the words,
  and it would have rotted again, because nothing could check them. This session removed the
  marker instead, so the successor claim is verified by the default suite on every run. Prefer
  removing the reason a claim cannot be checked over improving its wording.
- **A guardrail that is not re-read is not a guardrail.** `Gates green` sat recorded as green from
  2026-05-16 to 2026-08-11 while three of its four gates were red, and every failure is trivial.
  The cost was not the failures; it was three months of false confidence. Re-read the guardrails at
  every checkpoint that touches code, not at milestones.
- **No metric has fired a PDR reversal trigger yet — and that is now an ADJUDICATED "no", not an
  unexamined one (`PDR-0060`, 2026-08-15).** `PDR-0043` reversal trigger 1 was tested against a
  real CI red this session and ruled **not fired**: the trigger protects the claim *the local gate
  set is a faithful subset of CI's*, and the local set genuinely covers the test that failed, so
  that claim stands. What failed is a weaker assumption nobody had written down — that a green
  local run of a **nondeterministic** covered test predicts a green CI run of it. Recording the
  distinction matters both ways: calling it fired would reopen a correctly-closed issue, and
  calling it nothing would let a gate that reddens at random keep teaching everyone to wave it
  through. `PDR-0043` trigger 2 (*merge without the nightly restored*) also did **not** fire — it
  was discharged at `33bfff51`, and the restored nightly immediately earned its keep by surfacing
  the 31 hidden failures.
- **A guardrail can be run, read, honest, and still wrong — if nobody audits the SCOPE of the
  command (`PDR-0059`).** `Gates green` was not neglected this time; it was re-read at every
  code-touching checkpoint exactly as the rule below demands. It still missed 31 failing tests,
  because every reading was taken from `uv run pytest` and `pyproject.toml`'s default `addopts`
  silently deselects `-m "not slow"`. The prior instances of the `PDR-0010` pattern were gates not
  run, not read, or unfalsifiable. This one was all three of run, read, and honest. **Re-reading a
  guardrail is necessary and not sufficient; the command it is read with is part of the reading.**
- **No metric has fired a PDR reversal trigger yet.** `PDR-0007`'s trigger (inert-surface count
  *rises*) is the one to watch and cannot fire until an option is enabled under it — `PDR-0009`'s
  work is the first, so the next reading of the inert count is the real test of that principle.
  `PDR-0014`'s trigger 2 (*"bounds wiring materially delays the freeze"*) was recorded as
  approaching and is **retired without firing**: `PDR-0028` froze WS-1's scope at ten units,
  answering the question the trigger existed to force.
- **A differential instrument cannot see inertness** (`PDR-0034`). The harness answers *did old
  and new behave the same?*; a surface that is inert in the oracle **and** inert in the rebuild
  produces identical traces and correctly reports AGREE. It would have certified every one of
  the ~40 inert surfaces below. The `Declared-but-inert` row is therefore **not** instrumented
  by the harness and still moves only by hand — WS-3's wiring tests are the instrument that
  would move it, and they remain unbuilt.
- **This row's biggest win is branch-scoped and expires** (`PDR-0039`, 2026-08-14). The README
  rewrite retired ~102 confirmed-false claims, but it describes `project-recovery` and is stamped
  with the commit and date every claim was checked at. Its *Known rough edges* and *Continuous
  integration* sections describe conditions the recovery intends to **fix** — so merging the file
  unchanged to `main` would convert honest status into stale claims, re-creating this row's
  failure through the passage of time rather than through carelessness. Re-verification before
  the merge is a named gate, and it must use the **same method**, not a re-read: the adversarial
  pass caught **24 defects in a draft written expressly not to lie**.
- **A correction is not self-verifying either — and this row proved it on itself.** 2026-08-14:
  the commit that retired 102 false claims (`be3ebc2e`) shipped **two new ones**, and one of them
  was *in this cell* — it still read "NOT corrected" about the `vision.md` URL after the owner had
  approved the fix and the fix had landed. A guardrail counting false claims in canonical docs
  contained a false claim about itself for one commit. Fixed at `dcc5f803`. The operational rule
  this yields: **when you correct a document, re-read every other cell that referenced the thing
  you corrected** — the stale reference is never where you were looking.
- **And it cannot yet PASS a divergence it predicted** (`PDR-0037`, 2026-08-14) — **RESOLVED
  2026-08-15** (`hamlet-56ec575ae2` closed at `9a75b581`, accepted in `PDR-0040`). The original
  note, kept for the lesson: `exit_code` returned 0 **iff** every verdict was `AGREE` or
  `SKIPPED`; the vocabulary had no verdict meaning *diverged exactly as the register said it
  would*, so a **correctly rebuilt** substrate would have exited 1 on every cell at the first
  knockdown. `PDR-0033` taught *ask what a green tool cannot see*; this was the inverse —
  **ask what its red cannot distinguish**, because a gate whose red is unreadable gets waved
  through by hand, and that is how `Gates green` was lost for three months. The resolution kept
  the question alive: the match that now reads that red is conjunctive and anchored precisely
  because the adversarial pass showed every looser form manufactures false greens.
- **A verifier is not self-verifying — the `PDR-0033` pass proved it on itself** (2026-08-15,
  `PDR-0040`). Two instrument failures inside the adversarial pass, both caught: (1) the verify
  phase died on a session limit and the fail-closed partition labeled every unverified finding
  "confirmed" — **a fail-closed KEEP is not a confirmation**; 15 of 21 had never been examined
  when the label was first read. (2) The mutation adversary's injection was silently defeated by
  pytest's `pythonpath = ["src"]` ini setting (it outranks the `PYTHONPATH` environment
  variable), so its "surviving mutants" were measured against unmutated code — caught only by
  probing first (inject a loud raise, demand the suite scream). Standing rules: mutation results
  are believed only after a probe; a multi-agent verdict partition is checked for verifier
  failures before its labels are read as verdicts. Joins the family: a recorded green is not a
  green (`PDR-0010`); a green tool is not evidence (`PDR-0033`); a red must be distinguishable
  (`PDR-0037`); a correction is not self-verifying (`dcc5f803`).
- **Knockdown 1 registered a trace-visible divergence, so `PDR-0037` trigger 3's counter
  is at zero** (2026-08-15). That trigger folds the suppression path back out if two
  *consecutive* knockdowns register nothing trace-visible; the first knockdown used the
  machinery for exactly its intended purpose, six cells' worth. No reversal trigger fired
  this session — `PDR-0041`'s own triggers (a standing cell diverging at the pinned
  oracle, window-overhang breakage, grid3d resurrection-by-literal) are now armed and
  metric-bound to the harness gate above.
- **`Provenance integrity` went green 2026-08-13** (register condition met, `PDR-0028`), three
  days after being the most degraded guardrail on the board. Keep reading it: green here means
  *no silent acceptance* — the two registered divergences are still open defects the rebuild
  closes, and the row regresses if a new stamp/compare path appears outside the register, or if
  DIV-002's enumeration (when produced) finds sites the entry's rule does not cover.
