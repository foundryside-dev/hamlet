# Trial F — Tool durability            2026-08-18 · Claude (standing agent) · blind: no

## Preflight (protocol §3 — paste outputs)

- P1 corpus hash: `48840cc3ae62e381e0a96a6e850e3cc2fd309081b00bcbd8974cd9d58de935d9` — MATCH
- P2 drawn: F ∈ {B,D,E,F,J,K,L,M,O} — yes
- P3 prediction present: yes — quoted in §Facets below
- P4 `git status --porcelain src/townlet/`: `` (empty)
- P5 commit pin: `e5f7dd7a50d0dcb2ac26b5dbe9035a2ce98412da` on `project-recovery-2`
- P6 this record created before authoring: yes

Selection note (not part of the protocol): F chosen from the eight remaining by the executor for
axis diversity (items — untouched by any trial so far) and because it is the corpus's only
predicted-PASS in the remaining set: the aggregate pre-registration ("1, possibly 2, pass") is at
1 after Trial L, so F stresses that ceiling in whichever direction it lands.

## Facets (pre-committed BEFORE authoring; append-only after)

Corpus prediction, verbatim: **Predict: PASS.**

Spec: "An item degrades per use and eventually breaks." Stresses: "Item-scoped variables that
decay on use; item destruction." Facet shape given by the protocol's own §4 example for F.

| # | facet | leg-(b) evidence accepted | result | classification |
|---|-------|---------------------------|--------|----------------|
| 1 | An item carries a declared wear state — an item-scoped variable authored in the pack's config, no Python | `inspect --format json` (or the compiled artifact) shows the wear variable declared for the item, and after `env.reset()` a registry read returns the declared initial value | **PASS** | — |
| 2 | Use decrements wear — per-use decay tied to the interaction, not passive per-tick drain | runtime probe: agent acquires and uses the item; wear read back (registry or observation) decreases by the declared per-use amount on use, and does NOT change across ticks with no use | **PASS** | — |
| 3 | At zero wear the item breaks or stops working | runtime probe: drive wear to zero by repeated use; the declared consequence is observed — the item is destroyed/removed (slot empties or item disappears from the world state) OR its use is refused/has no further effect, whichever the pack declares | **PASS** (declared as stops-working) | — |
| 4 | Wear is observable to the agent | the encoded observation tensor carries the wear value at a compiled offset (item-slot block or a declared observation field named by the compiled spec); after a use the observation at that offset shows the decremented value | **PASS** | — |

## Authoring log (brief — what was tried, in order; pack path)

Pack: `configs/trial_f_durability/` (started from: copy of `configs/trial_l_cooldown/`)

1. Copied the Trial L pack; removed its probe; renamed the level `L0_effects` → `L0_tools`;
   deleted the copied `.compiled` cache immediately (the exact stale-cache mistake Trial L hit).
2. Declared the wear state: `vfs_profiles.yaml` `item_profiles` → profile `tool_stats` with one
   variable `durability` (float, `initial_value: 3.0`, `exposed_to: [agent]`). Item variables
   deliberately carry no `semantic_type` (PDR-0075) — none was needed.
3. Declared the tool: `items.yaml` catalog item `hammer` (`vfs_profile: tool_stats`) with
   `on_use: [if self.vfs.durability > 0.0 → modify target.bar.energy +0.1; modify
   self.vfs.durability −1.0; else []]`. "Breaks" is declared as stops-working: the guarded
   branch is the tool's entire effect, so at 0 the tool does nothing. Note: no despawn/destroy
   effects command exists, so physical destruction-at-zero was not attempted; the facet as
   pre-committed accepts either consequence, and stops-working is the one this pack declares.
4. Appearance: `levels/L0_tools/items.yaml`, one hammer, `placement: {mode: fixed,
   fixed_positions: [[2, 2]]}`, no respawn. Meters cut to energy + health; Trial L's cooldown
   timers, affordances, drive shaping reference and `enabled_affordances` updated to match.
5. Syntax note: `docs/config-schemas/items.md` shows a nested `if: {condition: …}` form the DTO
   refuses — the real schema is `- if: <expr>` / `then:` / `else:` (as Trial L's affordances
   used). Filed as by-catch (below); the pack used the real schema first try.
6. First `env.reset()` crashed: the pack initially declared `affordances: []` (idea F needs
   none) — a zero-affordance pack **validates and compiles, then crashes at the first
   observation** (`obs_affordance_at_position` publishes `(N, num_types+1)` against a
   scalar-shaped registry slot when `num_affordance_types == 0`). Not a facet of idea F; a
   pack-shape edge the engine mishandles. Worked around with one inert `BENCH` affordance
   (any real author's world would carry ≥1 affordance); filed as by-catch, not fixed.
7. Probe fix (probe mistake, not a gap): item variables are read from
   `vfs_registry.item_vfs[vfs_index, item_profile_map[profile][var]]`, not `registry.get(name)`.
8. Full probe run: every facet demonstrated in one pass (output below).

## Verdict

**Leg (a):**

```
$ git diff --stat -- src/townlet/
(empty)
$ git status --porcelain src/townlet/
(empty)
```

**Leg (b), per facet** (probe: `configs/trial_f_durability/probe_trial_f.py`, run at the pin
with `UV_CACHE_DIR=.uv-cache PYTHONPATH=$(pwd)/src uv run python configs/trial_f_durability/probe_trial_f.py`):

```
== Facet 1: compiled wear state ==
  item_profile_vars = {'tool_stats': ('durability',)}
  item_vars_per_slot = 1, item_vfs_dim = 1
  field 'obs_item_slots': offset=58 dims=1 feature='item_slots'
  total_dims=59
  item actions available: ['DROP_SLOT_0', 'GET', 'USE_SLOT_0']

  spawned hammer at (2, 2), vfs_index=0, durability=3.0   <- expect 3.0 (facet 1)

== Facet 2/4: pick up, use, idle, use again ==
  after GET       durability=3.0 energy=0.5 obs_item_slots=[3.0]
  after USE #1    durability=2.0 energy=0.6 obs_item_slots=[2.0]   <- expect 2.0, +0.1 energy
  after 5 WAITs   durability=2.0 energy=0.6 obs_item_slots=[2.0]   <- expect durability UNCHANGED (per-use, not per-tick)
  after USE #2    durability=1.0 energy=0.7 obs_item_slots=[1.0]   <- expect 1.0, +0.1 energy
  after USE #3    durability=0.0 energy=0.8 obs_item_slots=[0.0]   <- expect 0.0, +0.1 energy

== Facet 3: at zero wear the tool stops working ==
  after USE #4    durability=0.0 energy=0.8 obs_item_slots=[0.0]   <- expect NO energy change, durability stays 0.0
  after USE #5    durability=0.0 energy=0.8 obs_item_slots=[0.0]   <- same
```

- Facet 1: the wear state compiles from declaration alone and initializes at the declared 3.0.
- Facet 2: USE decrements by exactly the declared 1.0 (3→2→1→0); five idle ticks change nothing.
- Facet 3: at 0.0 the tool's declared effect stops firing — two further USEs move nothing.
- Facet 4: `obs_item_slots` (compiled offset 58) tracks the wear value in the encoded
  observation at every step.

Compile gate: `python -m townlet.universe validate configs/trial_f_durability --primary-level
L0_tools` → `Validation succeeded` (final pack).

**Headline: PASS** (binary; every facet passed both legs)
Budget-limited: no

**Prediction vs. actual:** predicted PASS, actual PASS — the first confirmed prediction (1 of 2
so far). The aggregate pre-registration ("1, possibly 2, pass; INERT count 1–2") now stands at
its ceiling with seven trials remaining and the INERT count still at 0: any further pass
falsifies the aggregate.

## Gaps filed

| facet | classification | tracker ID |
|---|---|---|
| — (by-catch: zero-affordance pack crashes at first observation; declared-and-crashing) | filed as bug, routed WS-4 | `hamlet-fba3d5aa3c` |
| — (by-catch: `docs/config-schemas/items.md` stale in three load-bearing places — if-syntax, item_profiles schema, "no item observations" claim) | filed as docs bug, routed to the docs-truth task | `hamlet-6f27878731` |

No facet-attached gaps: all four facets passed.

## Pack disposition (protocol §9; deadline 2026-10-06)

OUTSTANDING
