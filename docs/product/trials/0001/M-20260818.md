# Trial M — Combo actions (A enables B enables C)            2026-08-18 · Claude (standing agent) · blind: no

## Preflight (protocol §3 — paste outputs)

- P1 corpus hash: `48840cc3ae62e381e0a96a6e850e3cc2fd309081b00bcbd8974cd9d58de935d9  docs/product/prds/0001-corpus-FROZEN.md` — MATCH
- P2 drawn: M ∈ {B,D,E,F,J,K,L,M,O} — yes
- P3 prediction present: yes — quoted in §Facets below
- P4 `git status --porcelain src/townlet/`: `` (empty)
- P5 commit pin: `a519f312d7ce6755a7ffbf12fc675d7fcae9b15c` on `project-recovery-2`
- P6 this record created before authoring: yes

Selection note (not part of the protocol): M chosen from the seven remaining by the executor for
(1) axis diversity — the action-structure bucket is untouched by any trial; (2) it is the corpus's
second live test of the ABSENT/INERT distinction — the authorability ledger's claims that "VTC
action-writes have no YAML path" and "custom actions are structural no-ops" meet the metric here,
and the escalation clause (`PDR-0079`) watches the INERT count; (3) it is single-agent and
compact, fitting the one-session budget where the remaining multi-agent ideas (D, E, J, O) risk a
budget-limited record.

## Facets (pre-committed BEFORE authoring; append-only after)

Corpus prediction, verbatim: **Predict: PARTIAL, possibly INERT** — a declared dependency surface
that nothing enforces would be the same shape as L.

Facet reading of the Spec/Stresses: the Spec's "action" is read as any agent-performable
operation. The substrate declares two candidate surfaces (custom actions in the pack's
`actions.yaml`; affordance interactions). The trial accepts whichever declared surface expresses
sequential unlocking with zero Python, and the authoring log records which surface an author
reaches first (`PDR-0082` calibration note). "Available" is read on the L facet-3 precedent:
before the precondition holds, performing the operation is refused or produces none of its
declared effect; after, it produces it.

Anti-conflation, pre-committed: the gate must test an **event trace** ("A was performed" —
per-agent state written by A itself), not affordability of a cost. A pattern where A credits a
stock that B *spends* reproduces the availability pattern but is resource management — the
mechanic class the action-structure bucket exists to contrast with — and does not pass facet 3.
A flag written by A and *required but not consumed* by B is the accepted shape. The trace must
also not drift with time (a decaying trace is L's cooldown, not an unlock): idle ticks leave it
unchanged.

| # | facet | leg-(b) evidence accepted | result | classification |
|---|-------|---------------------------|--------|----------------|
| 1 | Three sequential operations and their progression state are declarable — the pack declares operations A, B, C an agent can perform, plus per-agent progression state (the A-trace and B-trace), config only | `UV_CACHE_DIR=.uv-cache uv run python -m townlet.universe inspect configs/<pack> --primary-level <level> --format json` (or the compiled artifact) shows the three operations and both trace variables in the compiled universe | **PASS** | — |
| 2 | Performing A writes its trace — on the tick A completes, the per-agent trace changes from its declared initial value to the declared post-A value, written by A's own declaration; idle ticks change nothing | runtime probe (script in the trial pack dir): read the trace via registry/compiled offset — initial value at reset; unchanged across N idle ticks; declared post-A value on the tick A completes; unchanged across N further idle ticks | **PASS** | — |
| 3 | B is gated on A's trace (the unlock) — before A, performing B produces none of B's declared effect and does not set the B-trace; after A, the same attempt produces B's declared effect. The declared gate is a condition on the A-trace, not a cost on a stock | runtime probe: attempt B at reset → B's target meter/variable unchanged AND B-trace unchanged; perform A; attempt B → B's declared effect applies. Plus the declaration itself: the gate names the A-trace in a condition | **PASS** | — |
| 4 | The dependency chains to depth 3 — C is gated on B's trace: after A alone, C still produces no effect; after A then B, C produces its declared effect | runtime probe: perform A; attempt C → C's target unchanged; perform B; attempt C → C's declared effect applies | **PASS** | — |
| 5 | Progression is observable to the agent — both trace variables are observation fields; the encoded observation at their compiled offsets changes when A and B are performed | `inspect --format json` shows observation fields bound to both traces, AND the probe reads the encoded values at the compiled offsets flipping when A (then B) completes | **PASS** | — |

## Authoring log (brief — what was tried, in order; pack path)

Pack: `configs/trial_m_combo/` (started from: copy of `configs/trial_l_cooldown/`)

1. Surveyed the custom-action surface FIRST — the surface a fighting-game author would reach for
   a combo. Read `configs/default_curriculum/actions.yaml`,
   `src/townlet/config/actions_config.py` (parse-time) and
   `src/townlet/environment/action_config.py` (runtime). Parse-time `CustomActionConfig` admits
   exactly `name` / `description` / `enabled_by_default` with `extra="forbid"` — no effects, no
   costs, no precondition — and the runtime `ActionConfig.reads`/`writes` (the door into the
   composition engine) is `default_factory=list`, reachable from no YAML. Sequential unlocking
   is **inexpressible on the actions surface**; the authorability ledger's "custom actions are
   structural no-ops" claim held at the schema level. Filed as by-catch (§8), not fixed; the
   affordance surface (below) is the second reach and carries the trial.
2. Design on the affordance surface (the Trial L pattern): copied `trial_l_cooldown`; deleted
   the copied `.compiled/` cache preemptively (the exact Trial L mistake) and its probe; renamed
   the level `L0_effects` → `L0_combo`. Traces as two meters (`did_a`, `did_b`: initial 0.0,
   every passive dynamic 0.0 so nothing drifts — a decaying trace would be L's cooldown, not an
   unlock). `COMBO_A` unguarded: energy +0.05 and `did_a := 1.0`. `COMBO_B` entirely inside
   `if: target.bar.did_a >= 1.0`: energy +0.07 and `did_b := 1.0` (the whole effect gated, so
   nothing leaks pre-A). `COMBO_C` inside `if: target.bar.did_b >= 1.0`: energy +0.11. Distinct
   payoff deltas make attribution unambiguous in the probe. `environment.yaml` meters and
   affordances, `training.yaml` `enabled_affordances`, and `drive.yaml`'s approach target
   updated to match.
3. `validate` succeeded first try. First probe run: **every facet demonstrated in one pass**.
   The probe was then extended with one mid-chain observation read (facet 5, traces at [1, 0]
   between A and B) and rerun — identical results, no pack change.

## Verdict

**Leg (a):**

```
$ git diff --stat -- src/townlet/
(empty)
$ git status --porcelain src/townlet/
(empty)
```

**Leg (b), per facet** (probe: `configs/trial_m_combo/probe_trial_m.py`, run at the pin with
`UV_CACHE_DIR=.uv-cache PYTHONPATH=$(pwd)/src uv run python configs/trial_m_combo/probe_trial_m.py`):

```
== Facet 1: compiled operations and traces ==
  field 'obs_meter_did_a': offset=31 dims=1
  field 'obs_meter_did_b': offset=32 dims=1
  total_dims=62
  affordances in runtime: ['COMBO_A', 'COMBO_B', 'COMBO_C']

== Facet 3 (first half): B before A does nothing, leaks nothing ==
  t0 (post-reset)          did_a=0.0 did_b=0.0 energy=0.5
  after INTERACT@COMBO_B   did_a=0.0 did_b=0.0 energy=0.5   <- gate closed, no did_b leak

== Facet 4 (zeroth half): C at reset does nothing ==
  after INTERACT@COMBO_C   did_a=0.0 did_b=0.0 energy=0.5

== Facet 2: idle ticks change nothing; A sets its trace on completion ==
  after 5 WAIT ticks       did_a=0.0 did_b=0.0 energy=0.5   <- no drift
  after INTERACT@COMBO_A   did_a=1.0 did_b=0.0 energy=0.55  <- did_a set, +0.05
    mid-chain obs[31] (obs_meter_did_a) = 1.0
    mid-chain obs[32] (obs_meter_did_b) = 0.0               <- facet 5, mid-chain
  after 5 more WAIT ticks  did_a=1.0 did_b=0.0 energy=0.55  <- event flag, no decay

== Facet 4 (first half): C after A alone is still refused ==
  after INTERACT@COMBO_C   did_a=1.0 did_b=0.0 energy=0.55  <- no change

== Facet 3 (second half): B after A applies its effect and sets did_b ==
  after INTERACT@COMBO_B   did_a=1.0 did_b=1.0 energy=0.62  <- did_b set, +0.07

== Facet 4 (second half): C after A then B applies its effect ==
  after INTERACT@COMBO_C   did_a=1.0 did_b=1.0 energy=0.73  <- +0.11

== Facet 5: observation carries both traces ==
  obs[31] (obs_meter_did_a) = 1.0
  obs[32] (obs_meter_did_b) = 1.0

== Facet 5 cross-check: fresh env, traces read 0 in the encoding ==
  fresh obs[31] (obs_meter_did_a) = 0.0
  fresh obs[32] (obs_meter_did_b) = 0.0
```

- Facet 1: the three operations and both traces compile from declaration alone
  (`obs_meter_did_a` offset 31, `obs_meter_did_b` offset 32, `total_dims=62`).
- Facet 2: A sets `did_a` 0→1 on its tick; ten idle ticks (five before, five after) move
  nothing — an event flag, not a drifting stock.
- Facet 3: B before A applies nothing and leaks nothing (whole effect inside the gate); B after
  A applies exactly +0.07 and sets `did_b`. The declared gate is `if: target.bar.did_a >= 1.0`
  — a condition on the event trace; nothing spends it.
- Facet 4: C refused at reset AND after A alone; applies exactly +0.11 only after A then B —
  the dependency composes to depth 3.
- Facet 5: the encoded observation tracks both traces, including the mid-chain [1, 0] state.

Compile gate: `python -m townlet.universe validate configs/trial_m_combo --primary-level
L0_combo` → `Validation succeeded`.

**Headline: PASS** (binary; every facet passed both legs)
Budget-limited: no

**Prediction vs. actual:** **The pre-registered prediction is FALSIFIED** — it said PARTIAL,
possibly INERT; the actual verdict is PASS: sequential unlocking is fully authorable with zero
`src/townlet/` changes. Stated, not smoothed over. The falsification has the exact `PDR-0082`
shape, for the third time: the prediction scored the first surface an author would reach (custom
actions — which is indeed incapable, see by-catch), and a second declared surface (affordance
event-trace gates) expresses the whole idea. The "possibly INERT" limb never materialized: the
incapable surface refuses extra fields at parse (`extra="forbid"`) rather than accepting them
silently, so the miss-class it would have produced is ABSENT, not INERT. **This trial also
falsifies the AGGREGATE pre-registration**: "1, possibly 2, pass; INERT count 1–2" is now
exceeded at 3 passes of 3 run, INERT count 0, with six trials remaining.

## Gaps filed

| facet | classification | tracker ID |
|---|---|---|
| — (by-catch: the custom-action surface cannot express effects or preconditions; `CustomActionConfig` is name/description/enabled_by_default only, `reads`/`writes` reachable from no YAML; the idea passed via the affordance surface) | ABSENT on the actions surface — filed as feature, routed WS-4 | `hamlet-f1dec55b9d` |

No facet-attached gaps: all five facets passed.

## Pack disposition (protocol §9; deadline 2026-10-06)

OUTSTANDING — intent: promote `configs/trial_m_combo/` to a regression fixture referenced by a
wiring test (the sequential-unlock pattern: event-trace meters + whole-effect `if` gates +
require-but-not-consume), on the `configs/trial002_money_*` precedent. Deadline 2026-10-06.
