# Trial O — Adversarial bidding            2026-08-18 · standing agent (not blind) · blind: no

## Preflight (protocol §3 — paste outputs)

- P1 corpus hash: `48840cc3ae62e381e0a96a6e850e3cc2fd309081b00bcbd8974cd9d58de935d9` — MATCH
- P2 drawn: O ∈ {B,D,E,F,J,K,L,M,O} — yes
- P3 prediction present: yes — quoted in §Facets below
- P4 `git status --porcelain src/townlet/`: `` (empty; command exited 0 with no output)
- P5 commit pin: `a33186246da17d7ceb3fecdc15ebdfcb100a5f96` on `project-recovery-2`
- P6 this record created before authoring: yes

Idea selection note (executor's one degree of freedom): O chosen from the remaining
{B, D, E, J, K, O} under the owner's session directive "trial four: multi-agent". Grounds:
(1) the clearing-phase axis is untouched by L/F/M; (2) O's predicted FAIL is structural —
"nothing else in the corpus needs the engine to run a clearing step" — not surface-choice,
so it tests exactly the prediction class the three falsifications so far have not touched;
(3) it is the most compact of the four multi-agent ideas, fitting the one-session budget
(`PDR-0081` trigger 1 watches budget-limited records).

## Facets (pre-committed BEFORE authoring; append-only after)

Corpus prediction, verbatim: **Predict: FAIL, ABSENT.**

Spec: "Agents bid against each other for a contested item; highest bid wins and pays."
Stresses: "A **global resolution phase over simultaneous agent submissions** — collect bids,
compare, award, charge."

| # | facet | leg-(b) evidence accepted | result | classification |
|---|-------|---------------------------|--------|----------------|
| 1 | Bid declaration — an agent action records a per-agent bid amount, from config alone | pack `validate` exit 0; probe: after agent 0 performs BID, agent 0's bid variable reads the declared amount while agent 1's reads its pre-bid value (registry read and/or compiled observation offset) | **PASS** | — |
| 2 | Simultaneous collection — two agents' bids are both live in the same resolution window; no first-come ordering decides | probe: both agents perform BID on the same tick; both bid values read back nonzero afterwards | **PASS** | — |
| 3 | Clearing: highest bid wins — a declared surface compares bids across agents and resolves a winner | `inspect --format json` shows the declared resolution surface (whatever form it takes); probe: agent 0 bids high / agent 1 low → winner state identifies agent 0; amounts swapped → identifies agent 1 (outcome tracks the comparison, not agent index or order) | **PASS** | — |
| 4 | Award — the contested thing goes to the winner only | probe: after clearing, the winner's win-state/item is set and the loser's is not | **PASS** | — |
| 5 | Charge — the winner pays their bid; losers pay nothing | probe: winner's money/resource decremented by exactly the bid amount; loser's unchanged | **PASS** | — |
| 6 | Observability — bid state and/or outcome appears in the encoded observation | `inspect --format json` shows observation field(s) for the bid/outcome variables; probe: encoded observation at the compiled offset tracks the value as it changes | **PASS** | — |

## Authoring log (brief — what was tried, in order; pack path)

Pack: `configs/trial_o_bidding/` (started from: copy of `configs/trial_m_combo`)

1. **Recon before authoring.** Read the effects command vocabulary
   (`src/townlet/config/effects_config.py`): `modify`, `if/then/else`, `for_each`, `reduce`,
   `switch`, `parallel`, `delay`, `sample`, `spawn_effect`, `spawn_item`. Found
   `for_each: all_agents` is a built-in collection resolver
   (`src/townlet/effects/collections.py` — `all_agents`, `nearby_agents`, `inventory_items`,
   `active_effects`), iterating agent indices with `target.*` rebound per iteration. Found the
   effect expression schema (`src/townlet/universe/compilers/effects.py`) addresses global
   profile variables as `vfs.<name>`, not `global.<name>`.
2. **The first-reached surface for "the engine runs a clearing step" — a `scope: global`
   effect — is declared but unreachable.** `_execute_spawn_effect`
   (`src/townlet/effects/executor.py:228-231`) hardcodes `scope=EffectScope.AGENT` ("scope
   hardcoded to AGENT for now"); the manager's `global_effects` tick machinery exists and is
   reachable from no YAML. And effects only enter the world downstream of an agent action —
   nothing spawns an effect at reset. Both filed (§Gaps), neither fixed.
3. **Second surface expresses the whole idea**: the clearing phase is an *agent-scoped,
   long-duration, `reapply_policy: renew`* effect (`auction_house`, `effects.yaml`) pinned to
   slot 0 via `target: "0"` from the spawning affordances, so both BID affordances renew ONE
   standing process. Its `on_tick`: a 3-tick window counter (`vfs.auction_timer`), then
   compare (`for_each: all_agents` running max into `vfs.highest_bid`), award/charge
   (`for_each: all_agents`, winner branch `wins += 1`, `credits -= bid`, everyone's bid
   expires), guarded by `vfs.highest_bid > 0.0` so empty windows award nobody.
4. Bids: per-agent `bid` meter (zero passive dynamics — a bid stands until cleared), written
   by `BID_HIGH` (0.3) / `BID_LOW` (0.1) affordance `on_start`, Trial M's pattern. Award/
   charge state: `wins` and `credits` meters. Global scratch (`highest_bid`,
   `auction_timer`): `vfs_profiles.yaml` `global_profile`.
5. Pack compiled on the **first** `validate` attempt; the probe passed every facet on the
   **first** run. No pack debugging was needed at all.
6. Noted post-verdict (2026-08-18, same session): the expression function vocabulary also
   carries aggregations — `max_all`, `argmax`, `count_where`, `masked_set`, … — which look
   like a **third** candidate surface for the clearing step (an aggregate-then-compare
   without the explicit `for_each` running max). Not probed; recorded because "how many
   independent surfaces express this" bears on the calibration question the four
   falsifications raise. The trial's verdict rests on the `for_each` surface alone.

## Verdict

**Leg (a):**

```
$ git diff --stat -- src/townlet/
(empty — exit 0, no output)
$ git status --porcelain src/townlet/
(empty — exit 0, no output)
```

**Leg (b), per facet** (probe: `configs/trial_o_bidding/probe_trial_o.py`; full output in the
session log; key excerpts):

- Facet 1 — one agent bids, only its meter moves:
  `after a0 INTERACT@BID_HIGH, a1 WAIT: a0: bid=0.3 … | a1: bid=0.0 …`
- Facet 2 — simultaneous collection, both live:
  `after simultaneous INTERACT: a0: bid=0.3 … | a1: bid=0.1 …` (window still open)
- Facet 3 — clearing by comparison: window closes → `a0: wins=1.0 credits=0.7 | a1: wins=0.0
  credits=1.0`; **swap test**: same amounts on opposite agents → `a1: wins=1.0 credits=0.7 |
  a0: wins=0.0 credits=1.0` — outcome follows the amounts, not the agent index; **no-bid
  guard**: 3 further empty windows change nothing (no repeat awards).
- Facet 4 — award: `wins` moved only on the winner (both orientations).
- Facet 5 — charge: winner's credits 1.0 → 0.7 = exactly its 0.3 bid; loser's unchanged at 1.0.
- Facet 3/6 artifact evidence, honesty note on the pre-committed command: `inspect --format
  json` turned out to emit a hash/metadata summary only (meters `bid`/`wins`/`credits` appear
  in its `meter_names`; it does not enumerate fields or effects), so the artifact-side checks
  were read from the compiled artifact directly — the same object `inspect` serializes:
  `u.compiled_effect_catalog.get("auction_house")` → scope agent, duration 100000, reapply
  renew, on_tick = [MODIFY, IF(clearing phases)]; `u.observation_spec.fields` → the four
  fields below with offsets.
- Facet 6 — observability: compiled fields `obs_meter_bid` (offset 31), `obs_meter_wins` (32),
  `obs_meter_credits` (33), and even the global `highest_bid` (61) — encoded values track the
  live bids (0.3/0.1), the cleared state (0/0), the win (0.1 = 1 win over the 0–10 bound) and
  the payment (0.7). The auction's own state is publicly observable to every agent.

**Headline: PASS** (binary; every facet passed both legs)
Budget-limited: no

**Prediction vs. actual:** Predicted **FAIL, ABSENT** — actual **PASS on all six facets**. The
fourth falsification in four trials, and the sharpest yet: unlike L/F/M this prediction was
*structural* ("a global resolution phase … nothing else in the corpus needs the engine to run a
clearing step"), not surface-choice — and it still fell, because the effects command vocabulary
(`for_each: all_agents` + global VFS scratch + guarded branches, ticking engine-side every
step) **is** a declarative clearing surface. The `PDR-0082` shape recurs in half: the
first-reached surface (a `scope: global` effect) is genuinely unreachable (filed), and a second
declared surface expresses the whole idea. The aggregate pre-registration was already falsified
at 3-of-3 (`PDR-0084`); this reading moves the north-star to 4 of 4, split 0/0/0.

## Gaps filed

| facet | classification | tracker ID |
|---|---|---|
| 3 (first-reached surface) | INERT — `scope: global` validates, compiles, and is silently spawned agent-scoped; `global_effects` reachable from no YAML (`executor.py:228-231`) | `hamlet-4cd664a955` |
| 3 (bootstrap) | ABSENT — no declarative path to a standing engine-run world process; effects exist only downstream of an agent action, so the clearing process must be bootstrapped by the first bid | `hamlet-77e4f8b3e3` |

## Pack disposition (protocol §9; deadline 2026-10-06)

OUTSTANDING (fourth pack on the clock — L, F, M, O)
